# AiDay 아이데이 — Product Spec v0.3

> 최초 기준: 프로토타입 직접 조작 https://aiday2026.lovable.app/ (2026-06-08)
> 구현 상태는 2026-07-03 GitHub 코드 기준으로 재확인·갱신됨
> 상태: ✅ 구현됨 | 🔧 UI/더미 데이터만 | ❌ 미구현
>
> **문서 역할**: 이 문서는 **기준선(2026-07-03 코드 기준)의 현재 상태**를 기록한다.
> 이후의 목표 상태·변경 계획(6주 고도화 범위)은 [PRD.md](./PRD.md)에서 추적하고,
> 확정된 제품 결정은 [docs/PRODUCT-DECISIONS.md](./docs/PRODUCT-DECISIONS.md)를 따른다. 두 문서와 이 문서가 다르면 그쪽이 우선한다.

---

## 제품 한 줄 정의

**"만 1세~8세 아이를 둔 부모가 매일 아침, 오늘의 환경정보를 내 아이 기준으로 해석한 하루 첫 육아 판단(옷차림·준비물·오늘의 케어 방식)을 AI에게서 먼저 받는 앱"**

---

## 사용자 플로우 전체 순서

핵심 원칙: **도착지는 진입 경로가 아니라 사용자 상태(로그인 여부 × 프로필 유무)로 결정한다.**
모든 인증 성공(이메일 로그인·가입, Google OAuth — 로그인/가입 페이지 어디서든)은 공통 판단 지점
`/auth/landing`을 거친다. (2026-07-13 재정의)

```
랜딩(/) ─ 무료 시작 → 회원가입(/signup) ─┐
        └─ 로그인 →   로그인(/login) ────┤ 인증 성공 (이메일·Google 공통)
                                         ▼
                            /auth/landing (판단 지점)
      ① 게스트 시절 만든 로컬 프로필이 있으면 DB로 1회 이전 (§3 결정)
      ② DB 프로필 있음 → 홈(/home, localStorage 복원)
      ③ DB 프로필 없음 → 온보딩(/onboarding, 7단계) → 완료 화면 → 홈
· 비밀번호 분실: 재설정 메일 → /auth/callback?next=/reset-password → 새 비밀번호 → 홈
· 이메일 가입 후 세션이 없으면(이메일 인증 대기) 게스트 모드로 온보딩 진행,
  첫 로그인 때 판단 지점에서 로컬 프로필이 DB로 이전됨
· 홈 진입 가드: 로그인 상태인데 DB·로컬 어디에도 실프로필이 없으면 온보딩으로 유도
  — 온보딩의 "나중에 이어서 하기 / 먼저 둘러볼게요"는 탭 세션 동안 예외(sessionStorage `aiday:browseHome`).
  데모 프로필(지우·도윤)은 **비로그인 게스트 전용**이 된다
→ [탭] 환경정보 / 옷차림 / 건강팁 / 마이
```

### 게스트 모드 ("먼저 둘러볼게요")

현재 동작 (코드 기준, `lib/profile.ts: loadProfiles`):
- 로그인·가입 없이 `/home` 진입 가능. 저장된 프로필이 없으면 **데모 프로필 2개**(지우 4세 여아 · 아토피/비염, 도윤 2세 남아 · 피부 민감)가 자동 표시됨
- 게스트도 온보딩 진입·프로필 저장 가능 (localStorage에만 — 로그인 상태가 아니면 DB 저장은 조용히 스킵됨)
- ⚠️ 게스트 상태에서도 홈 진입 시 `/api/report`(Claude API)가 **실제로 호출됨** — 인증 게이트 없음, 토큰 비용 발생

결정 확정됨 (2026-07-04, [docs/PRODUCT-DECISIONS.md](./docs/PRODUCT-DECISIONS.md) §3):
- [x] 게스트 → 가입 전환 시 localStorage 프로필을 DB로 이전한다 (가입·로그인 성공 직후 1회 업로드) — ✅ 구현됨 (2026-07-13, `/auth/landing`에서 DB가 비어 있을 때 `uploadLocalProfilesToDb()`)
- [x] 게스트의 AI 리포트 호출은 허용하되 IP 레이트리밋 적용 (기본 제안: 일 10회/IP)
- [x] 데모 프로필에 "예시" 뱃지 표시, 첫 실프로필 등록 시 데모 제거

---

## P-01. 랜딩 페이지 (`/`)

✅ 구현됨 — 카피·섹션 구성·로고 표기('AiDay 아이데이') 코드와 일치 확인 (2026-07-03)

**구성 섹션 (위→아래 순서)**

---

### S1. 네비게이션

로고(AiDay 아이데이) + `로그인` + `무료 시작` 버튼

> **로고 표기 확정:** 로고 이모지(🌤️)는 삭제. 이후 모든 로고 표시는 **'AiDay 아이데이'**로 표기 (전 페이지 공통).
> ✅ 코드 반영 완료 (`components/Logo.tsx` — 벡터 마크 + "AiDay 아이데이", 2026-07-03)

---

### S2. 히어로 섹션

**상단 배지**
> ✨ 바쁜 엄마아빠를 위한 AI 육아 비서

**헤드라인**
```
오늘 우리 아이,
뭘 입히고 뭘 챙겨야 할지
AI가 먼저 알려드려요
```

**서브카피**
```
미세먼지, 꽃가루, 자외선, 일교차까지.
오늘 날씨와 우리 아이 체질을 함께 분석해서,
등원룩·마스크·겉옷·준비물까지
더 쉽게 결정할 수 있게 도와드려요.
```

**CTA 버튼 2개**
- `무료로 시작하기` → `/signup`
- `먼저 둘러볼게요 →` → `/home` (게스트 더미 데이터)

---

### S3. User Pain 섹션

**섹션 타이틀:** 매일 아침 이런 고민 하고 계신가요?

카드 4개 — **이모지 없음** (세로 포인트 바 + 텍스트만, 현재 코드 반영 완료):

1. 날씨 앱, 미세먼지 앱, 꽃가루, 자외선 정보… 따로따로 확인하기 너무 번거로워요
2. 일교차가 심한 날 아이 옷차림을 어떻게 해야 할지 매번 헷갈려요
3. 바쁜 아침에 이것저것 고민하고 챙길 시간이 없어요
4. 비염, 아토피, 감기에 잘 걸리는 아이… 민감한 우리 아이 체질에 맞는 세심한 케어 가이드 없을까요?

---

### S4. 차별점 섹션

**섹션 타이틀**
```
아이데이는 환경 정보를
"우리 아이 기준"으로 해석합니다.
```

아이콘은 이모지가 아닌 **Lucide 벡터 아이콘** 사용 (현재 코드 반영 완료):

| 카드 | 아이콘 (Lucide) | 제목 | 본문 |
|------|--------|------|------|
| 1 | RefreshCw | 복잡한 수치를 육아 언어로 | 미세먼지·꽃가루·습도 같은 정보를 바쁜 엄마아빠를 위한 "육아 번역기"처럼 오늘 어떤 준비가 필요한지 쉽게 알려드려요 |
| 2 | Baby | 우리 아이 맞춤 해석 | 비염, 아토피, 열 많은 아이… 같은 날씨도 우리 아이 상태에 따라 다른 준비가 필요할 수 있어요 |
| 3 | Clock | 시간대별 준비 가이드 | 등원 때는 괜찮아도, 하원 시간엔 추워질 수 있어요. 아이의 하루 일과에 맞춰 시간대별 준비를 미리 알려드려요 |
| 4 | Smartphone | 미리 받아보는 육아 가이드 | 앱을 열지 않아도, 아침마다 오늘 필요한 준비를 미리 받아볼 수 있어요 |

---

### S5. 사용자 후기 섹션

**섹션 타이틀:** 부모님들의 이야기

| 리뷰 | 내용 | 출처 |
|------|------|------|
| 1 | "미세먼지 수치만 봐서는 감이 안 왔는데, 우리 아이 기준으로 알려주니까 오늘 어떻게 준비해야 할지 훨씬 쉬워졌어요." | 6세 아들 엄마 · 비염 아이 |
| 2 | "아침엔 괜찮아 보여서 얇게 입혔다가 하원 때 추워했던 적이 많았는데, 이제 겉옷 챙기는 걸 놓치지 않게 됐어요." | 7세 딸 엄마 · 열 많은 아이 |
| 3 | "등원 준비만으로 정신없는데, 오늘 뭘 챙겨야 하는지 먼저 정리돼 있으니까 마음이 훨씬 편해요." | 5세 아들 엄마 · 맞벌이 |

---

### S6. 푸터

로고(AiDay 아이데이) + 이용약관 + 개인정보처리방침 + admin@aiday.app
+ "환경 데이터 출처: 기상청 단기예보 · 한국환경공단 에어코리아"
+ © 2026 아이데이. All rights reserved.

> 하단 CTA 섹션("아이데이가 대신 챙겨드릴게요")은 제거 완료 — 후기 섹션 다음 바로 푸터.

---

**네비 CTA 동작:**
- 네비 `로그인` → `/signup`
- 네비 `무료 시작` → `/signup`

---

## P-02. 회원가입 (`/signup`)

✅ 구현됨 — Supabase Auth 연동 (이메일/비밀번호 `signUp` + Google `signInWithOAuth`)

**구성 (위→아래 — 이메일 폼이 먼저, 소셜 로그인이 아래):**
- 헤더: 로고 + "홈으로" 링크
- 타이틀: "회원가입 — 우리 아이 맞춤 환경 리포트를 시작해보세요"
- 이메일 인풋 (placeholder: parent@example.com)
- 비밀번호 인풋 (placeholder: 8자 이상)
- 비밀번호 확인 인풋
- 체크박스: [필수] 이용약관 및 개인정보처리방침 동의
- 체크박스: [선택] 마케팅 정보 수신에 동의합니다
- "가입하기" 버튼
- 구분선 "또는"
- 소셜 로그인 1개: G **"Google 계정으로 계속"** (흰 배경, border, Google 브랜드 가이드 준수)
- "먼저 둘러볼게요 →" 링크 → `/home`

**조건:**
- 가입 완료 → 세션 있으면 `/auth/landing`(판단 지점), 없으면(이메일 인증 대기) "인증 메일을 보냈어요" 안내 토스트 후 `/onboarding` (게스트 모드 진행)
- `signUp`에 `emailRedirectTo: /auth/callback?next=/auth/landing` 지정 — 인증 메일 링크가 현재 도메인 콜백으로 복귀 (2026-07-13)
- Google 버튼 → `/auth/callback?next=/auth/landing` (로그인 페이지와 공용 — 기존 사용자가 눌러도 재온보딩되지 않음)
- "먼저 둘러볼게요 →" → `/home` (게스트 모드)

---

### 로그인 (`/login`) ✅ 구현됨 (2026-07-03)

- 구성: 이메일 + 비밀번호 인풋 → `supabase.auth.signInWithPassword` (`app/login/page.tsx`)
  - 자격증명 오류 시 "이메일 또는 비밀번호가 올바르지 않아요." 토스트
  - 이메일 미인증(`Email not confirmed`) 시 한국어 안내 토스트 + "인증 메일 재발송" 액션(`supabase.auth.resend`) (2026-07-13)
- "비밀번호를 잊으셨나요?" → `resetPasswordForEmail` 재설정 메일 발송
  → 메일 링크는 `/auth/callback?next=/reset-password`로 세션 교환 후
  `/reset-password`(새 비밀번호 입력, `updateUser`)로 진입 (`app/reset-password/page.tsx`)
  - 링크 만료·직접 접근(세션 없음) 시 재요청 안내 화면 표시
- Google 버튼은 가입/로그인 공용 (`signInWithOAuth`, 두 페이지 모두 `next=/auth/landing`)
- 로그인 성공 시(이메일·Google 공통): `/auth/landing` 판단 지점에서 게스트 로컬 프로필 DB 이전 →
  DB 프로필 있으면 `/home`(localStorage 복원), 없으면 `/onboarding` (2026-07-13 재정의 — 이전에는 구글 경로만 무조건 `/home`으로 가서 신규 구글 사용자에게 데모 프로필이 표시되는 버그가 있었음)
- 랜딩 네비 `로그인` → `/login`으로 변경. 로그인↔회원가입 상호 링크 추가

**DB 프로필 복원:** `lib/profile.ts: fetchProfilesFromDb()` — 비로그인(no-auth)/조회 실패(error)/성공(ok)을 구분해 반환 (조회 실패를 빈 계정으로 오판해 온보딩으로 보내지 않기 위함). 홈은 이걸로 DB → localStorage 복원 + 진입 가드, 마이는 `syncProfilesFromDb()`(ok일 때만 복원) 사용. 마이 삭제는 `removeProfileFromDb`로 DB에도 반영, 로그아웃(`signOut`)은 로컬 프로필 정리 후 랜딩으로 이동.

**인증 후 판단 지점 (`/auth/landing`) ✅ 신설 (2026-07-13):** 스피너 화면. 비로그인 → `/login`, DB 조회 실패 → 토스트 + `/home`(로컬 fallback), DB 비어 있고 게스트 로컬 프로필 있으면 `uploadLocalProfilesToDb()`로 1회 이전 후 재조회, 최종적으로 프로필 있으면 `/home`(localStorage·activeProfileId 복원), 없으면 `/onboarding`. `/auth/callback`의 기본 `next`도 이곳.

**수용 기준:** 이메일 가입 → 로그아웃(또는 다른 브라우저) → 같은 계정으로 재로그인 → DB 프로필이 복원되어 홈에 표시된다. *(코드 경로 구현·검증 완료 — 실계정 E2E는 이메일 인증 포함 수동 확인 필요)*

---

## P-03. 온보딩 (`/onboarding`, 7단계)

✅ 전체 7단계 구현됨 — 완료 시 localStorage + Supabase DB(`children` 테이블) 저장 (`lib/profile.ts: saveProfile` + `saveProfileToDb`)

**공통 UI:**
- 상단: 뒤로가기 버튼 + "나중에 이어서 하기" 버튼 + `N / 7 N%` 진행률 + 프로그레스 바
- 하단: "다음" 버튼 + "먼저 둘러볼게요" 링크

---

### Step 1 / 7 — 아이 이름 (14%)

> "안녕하세요! 😊 먼저 아이의 이름을 알려주세요."
> 별명도 괜찮아요 (예: 지우, 첫째, 우리 아기)

- 텍스트 인풋 (placeholder: 예) 지우)

---

### Step 2 / 7 — 출생 연월 (29%)

> "{이름}는 언제 태어났나요? 🎂"
> 월령에 따라 적절한 건강 정보가 달라져요

- 드롭다운 2개: 년 (2017~2026) / 월 (1~12월)
- 유효성: 둘 다 선택해야 "다음" 활성화, 미선택 시 "태어난 연도와 월을 선택해주세요" 토스트
- **일(日)은 수집하지 않는다** — 아동 민감정보 최소수집(PIPA). 연령군 규칙 세트는 만 나이 기준이라 월까지면 충분하고, 일은 어떤 판정·표시에도 쓰이지 않는다. 나이 표시는 `calcAge(year, month)`가 "만 N세"로 산출(생일 당월은 아직 생일 전으로 보수적 간주). DB `birth_day` 컬럼은 nullable 유지 → 백일·돌 등 문화 이벤트 기능은 추후 선택 입력으로 재도입 가능.

---

### Step 3 / 7 — 성별 (43%)

> "{이름}의 성별을 알려주세요 👶"

- 버튼 3개 (단일 선택):
  - 👦 남아
  - 👧 여아
  - 🙂 선택 안 함

---

### Step 4 / 7 — 체질 특이사항 (57%)

> "{이름}에게 해당되는 것을 모두 선택해주세요 🏥"
> 해당 항목이 있으면 관련 환경 지표를 더 꼼꼼히 알려드려요

- 체크박스 5개 (다중 선택):
  - 호흡기 민감 (비염, 천식·기관지)
  - 알레르기 체질 (꽃가루·먼지)
  - 민감 피부 (아토피·건조·자외선)
  - 해당없음
  - 기타 → 선택 시 텍스트 인풋 노출
- "기타" 선택 시 직접 입력 인풋 표시
- 유효성: 최소 1개 선택 필수. 미선택 시 "하나 이상 선택해주세요 (없으면 '해당없음')" 토스트

---

### Step 5 / 7 — 체온 민감도 (71%)

> "{이름}는 또래와 비교했을 때 어떤가요? 🌡️"
> 체온 민감도에 따라 옷차림 추천이 달라져요

- 드롭다운 3개:
  - **추위 민감도**: 매우 많이 탐 / 조금 많이 탐 / 보통 / 조금 덜 탐 / 매우 덜 탐
  - **더위 민감도**: 매우 많이 탐 / 조금 많이 탐 / 보통 / 조금 덜 탐 / 매우 덜 탐
  - **땀 분비**: 매우 많음 / 조금 많음 / 보통 / 적은 편

---

### Step 6 / 7 — 하루 일과 (86%)

> "{이름}의 하루 일과를 알려주시면 더 정확한 리포트를 드려요 ⏰"
> 선택 항목이에요. 나중에 설정에서도 입력할 수 있어요

- 드롭다운 6개 (선택 항목):
  - 🌅 등원 시간: 시간 선택 (07:00~10:00, 30분 단위)
  - ☀️ 야외활동 시간대: 시작 / 종료
  - 🏫 하원 시간: 시간 선택
  - 🌇 저녁 야외활동 시간대: 시작 / 종료
- "건너뛰고 나중에 입력할게요" 버튼 (필수 아님)

---

### Step 7 / 7 — 알림 설정 (100%)

> "언제 알려드릴까요? 🔔"
> 나중에 설정에서 언제든 변경할 수 있어요

**알림 항목:**

| 항목 | 기본값 | 옵션 |
|------|--------|------|
| 💬 카카오톡 알림 연동 | 버튼 | — |
| 🌙 전날 밤 알림 | ON | 시간 선택 (기본: 21:00) |
| 🌅 당일 아침 알림 | ON | 등원 N분 전 (기본: 30분 전) |

**상황별 환경 경보 알림** (체크박스, 다중 선택):
- 🤖 AI 종합 환경지수 '주의' 이상 [기본 체크, 추천 뱃지]
- 🌡️ 일교차 10°C 이상
- 😷 초미세먼지(PM2.5) '나쁨' 이상 (35㎍/㎥ 초과)
- 🌳 꽃가루 농도 '주의' 이상
- 💧 건조주의보 발령 (상대습도 35% 미만)
- ☀️ 자외선지수 '높음' 이상 (UV 지수 6 이상)

- "완료" 버튼 클릭 → 완료 화면

---

### 온보딩 완료 화면

> 🎉 {이름}의 첫 번째 리포트가 준비됐어요!
> 지금 바로 오늘의 리포트를 확인해보세요.

- "오늘 리포트 보러가기 →" 버튼 → `/home`

> ✅ 문구 확정·코드 반영 완료: "날씨 리포트" → "리포트" (날씨 삭제, `app/onboarding/page.tsx`, 2026-07-03)

---

## P-04. 홈 (`/home`)

✅ 실시간 날씨/대기질 API + Claude AI 리포트(`/api/report`) 연동. "시간대별 환경" 카드는 실데이터(`buildTimeline` — weather.hourlyForecast·air.hourly·uv.hourly·pollen, 일과 슬롯별 매핑, `lib/weather-mock.ts`는 실데이터 부재 시 폴백만) | 🔧 "추천 아이템" 상품 카드는 여전히 더미(이모지+고정 가격, 쇼핑 연동 없음)

**구성 (위→아래 순서, 2026-07-03 디자인 개편 반영):**

```
헤더
  로고 | 🔔 알림 아이콘(Lucide Bell) | ⚙️ 설정 아이콘(Lucide Settings)

아이 탭
  [👧 지우 6세]  [+ 추가]

위치
  📍 서울 강남구 ∨   ← 하드코딩 텍스트 (버그 현황 참고)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
AI 리포트 카드 (서비스 핵심)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
카드 헤더: "AI REPORT" 아이브로우 + 오늘 날짜 (예: 7월 3일 (금))
  · AI 호출 실패 시 "기본 추천" 라벨 표시 후 규칙 기반 fallback

상태 필 (5초 안에 파악되는 오늘의 결론 — 뱃지 tone에서 도출):
  주의 뱃지 있음 → "오늘은 주의가 필요해요" (warn 칩)
  없음          → "오늘은 무난한 하루예요" (good 칩)

(hook — 카드의 히어로 문장, 19px bold)
  32도 고온 — 땀 관리 철저히
(message — 상세 설명, 문단 분리 + **키워드** 강조 렌더링)
  오늘 하루종일 32°C 고온에 습도도 높아요. ...

뱃지 5개 (가로 나열):
  미세먼지·보통 | 꽃가루·높음 | 자외선·보통 | 습도·적정 | 바람·강함

오늘 챙길 것 (체크 가능한 리스트 + N/M 카운터, 전부 체크 시 "준비 끝 ✓"):
  ☐ 👕 긴팔 + 얇은 가디건
  ☐ 🧣 목수건 (오후 바람 강함)
  ☐ 😷 마스크 (꽃가루 높음)
  ☐ 💧 보습제 (건조 주의)

신뢰 라인 (카드 하단):
  "{이름}을 위한 프로필 기준 해석 · 기상청·에어코리아 실측 데이터"

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
시간대별 환경 (가로 스크롤)  ✅ 실데이터 (buildTimeline, 실값 부재 시 mock 폴백)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
"옆으로 넘겨보세요 →"

카드 4개 (등원 / 야외활동 / 하원 / 저녁):
  등원시간 08:00  ⛅ 12° 체감10°
  미세먼지·보통 | 자외선·낮음 | 꽃가루·높음 | 습도45% | 바람·약함
  (나머지 3개 동일 구조 — 값은 lib/weather-mock.ts 고정값)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
(오늘의 종합 솔루션 — 제거됨, 2026-07-12)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
AI 리포트 카드의 "오늘 챙길 것"과 결론이 중복되고, 규칙 기반 고정 판정이
AI 생성 체크리스트와 상충할 수 있어 홈에서 제거 (CEO 리뷰 P0-3).
components/CharacterReport.tsx와 /images/character-*.png는 보존 — 베타 검증 후 재검토.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
{이름}를 위한 오늘의 추천 아이템  🔧 더미
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
쇼핑 추천 카드 4개 (이모지 + 이름 + 가격):
  🧣 유아 면 목수건 — 9,900원
  😷 키즈 KF94 마스크 — 12,500원
  🧴 민감 피부 보습로션 — 18,000원
  🧥 얇은 가디건 — 29,900원

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
바텀 네비게이션 (고정, Lucide 아이콘)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
홈(Home) | 환경정보(Wind) | 옷차림(Shirt) | 건강팁(Heart) | 마이(User)
활성 탭: 아이콘 뒤 크림 필(pill) + 강조색 아이콘
```

**AI 리포트 프롬프트 설계** (`lib/prompts/report.ts`, 호출: `app/api/report/route.ts`, 모델: `claude-sonnet-5` — 2026-07-12 Haiku 4.5에서 전환, thinking 비활성·max_tokens 1000)

- **톤:** "아이를 키우는 부모의 든든한 육아 친구" — 카카오톡처럼 따뜻하게, 오늘 이 아이에게 꼭 필요한 이야기만
- **금지 사항 (시스템 프롬프트에 명시):**
  - 누구나 아는 일반 조언 ("물 자주 마시게 하세요" 등)
  - 건강 특이사항을 오늘 환경과 실제 무관하게 억지로 연결
  - 2인칭 표현 ("너는", "네가", 이름+야/아 호칭) — 항상 "{이름}는/{이름}이는" 3인칭
  - 코드블록(```) — JSON 한 줄만 반환
- **Few-shot 3개 내장:** 비염×꽃가루, 일교차(특이사항 없음), 아토피×자외선 — 각각 입력 조건과 hook/message/checklist 출력 예시 페어
- **출력 스키마:** `{"hook": "...", "message": "...", "checklist": ["..."]}`
  - `hook`: 25자 이내, "[공감] — [행동]" 구조 (예: "꽃가루 최악이에요 — 마스크 필수")
  - `message`: 250자 이내(공백 포함), 문장마다 `\n` 구분, 핵심 키워드는 `**단어**` 강조
  - `checklist`: 3~4개, "이모지 짧은이름" 형식
- **입력 컨텍스트:** 아이 정보(이름·나이·성별·건강 특이사항·체온 민감도) + 오늘 일정별 날씨(등원/야외활동/하원/저녁 시간대 매핑) + 현재 대기질(PM10/PM2.5/통합대기 등급)
- **파싱 안전장치:** 코드블록 제거 → JSON.parse, 실패 시 `{}` 블록 직접 추출 재시도, 최종 실패 시 빈 응답 반환 → 클라이언트가 `lib/recommendation-engine.ts`의 규칙 기반 fallback 사용
- **캐싱:** 클라이언트에서 아이 ID + 날짜별 localStorage **당일 고정** 캐시 (`aiday:report:v10:{childId}:{YYYY-MM-DD}`). 아침 첫 생성본을 하루 내내 유지하고, 생성 시점 환경 스냅샷(`env`) 대비 **급변**(비 소식 생김/사라짐, 강수확률 ±30%p, 미세먼지 나쁨 경계 통과, 같은 시각 기온 ±3°C)일 때만 자동 재생성. 헤더에 생성 시각 표시 + 수동 새로고침 버튼(60초 쿨다운) 제공 (`app/(main)/home/page.tsx: envSignature/envChanged/refreshReport`)

---

## P-05. 환경정보 (`/env`)

✅ 전 섹션 실데이터. 날씨/대기질/꽃가루/자외선 API 4종 + 주간 날씨(`/api/weather/weekly` 단기+중기예보 병합) 연동. 야외활동 지수는 환경 수치 종합 산식(`lib/outdoor-index.ts`), 시간대별 날씨는 `hourlyForecast`+시간대별 자외선, 새로고침은 실제 재조회. 라우트는 `/env`로 통일되어 바텀탭과 일치함

**구성 (위→아래 순서):**

```
헤더
  ← 뒤로가기 | 로고(AiDay 아이데이) | 🔄 새로고침 아이콘
  · 새로고침 버튼은 4개 API를 실제 재조회 (재조회 중 아이콘 회전·비활성화)

타이틀
  환경정보
  📍 {측정소명(예: 종로구), 없으면 "서울"} ∨ · 실시간

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
{이름}를 위한 맞춤 인사이트  ✅ 실데이터 + 프로필 조건 기반 동적 생성
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
서브타이틀: 프로필 있으면 "{이름}의 건강 정보(…)를 반영했어요"
           없으면 "프로필을 등록하면 더 정확한 추천을 받을 수 있어요"

인사이트 카드 (조건 충족 시 동적 추가):
  😷 미세먼지/초미세 등급 3 이상 → 경고 카드
  🌿 양호 시 → "미세먼지 양호" 카드
  💧 습도 35% 이하 → 건조 카드 (피부 민감 시 문구 강화)
  🧣 바람 5m/s 이상 → 바람 카드
  🌳 호흡기·알레르기 특이사항 보유 시 → 꽃가루 주의 카드

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
오늘의 야외활동 지수  ✅ 실데이터 (lib/outdoor-index.ts 종합 산식)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
{점수} / 100 · {좋음/보통/주의/나쁨}
· 대기질·자외선·꽃가루·강수확률·기온극값·바람을 감점 방식으로 100점에서 차감
· 코멘트: 최대 감점 요인 1~2개 + 등급별 마무리 문구 (감점 요인 자동 서술)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
현재 날씨  ✅ 실데이터
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
18° | 구름많음 (하늘상태·강수형태 아이콘)
습도 38% | 바람 4.2m/s | 강수확률 10%
· 체감온도·최고/최저는 현재 미표시 (프로토타입 대비 축소)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
대기질 · 미세먼지  ✅ 실데이터
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PM10: 62㎍/㎥ · 보통
PM2.5: 28㎍/㎥ · 보통
오존: 0.041ppm · 등급 라벨 실계산 (환경부 O3 1시간 기준: ≤0.03 좋음/≤0.09 보통/≤0.15 나쁨/초과 매우나쁨)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
꽃가루 지수  (출처: 기상청 링크)  ✅ 실데이터
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
3종 카드: 🌳 참나무 | 🌲 소나무 | 🌿 잡초 (등급 라벨: 낮음~매우높음)
· 프로토타입의 5종(+자작나무·잔디류)에서 축소. 실데이터는 참나무·소나무 2종뿐 —
  잡초는 API가 항상 null을 반환해 카드가 상시 "--" 표시 (버그 현황 참고)
· 실패 시 "꽃가루 데이터를 불러오지 못했어요" 카드

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
자외선 지수 / 온·습도  ✅ 실데이터
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
자외선 지수: 7 · 높음 (프로그레스 바)
온·습도: 38% · 쾌적 ("실내 권장 40~60%")

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
시간대별 날씨 (가로 스크롤)  ✅ 실데이터
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
06~21시 6슬롯 (weather.hourlyForecast) — 하늘 아이콘·기온·강수확률 + 시간대별 자외선(☀️)
· 데이터 없으면 "시간대별 예보를 불러오지 못했어요" 카드

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
주간 날씨 (주말 강조)  ✅ 실데이터 (/api/weather/weekly)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
오늘~D+6 (7일). D0~D2는 단기예보(getVilageFcst 일 최저/최고·대표하늘), D+3~D+6은
중기예보(getMidLandFcst 육상 + getMidTa 기온) 병합. 온도 바는 그 주 실제 범위로 정규화
푸터 메모: 주말 강수확률 50% 이상이면 해당 요일 자동 언급 (예: "일요일 비 소식이 있어요")
```

---

## P-06. 옷차림 (`/outfit`)

✅ 날씨 API + 아이 프로필 기반 추천 로직 연동 (`buildOutfit()`, 규칙 기반 — LLM 생성 아님) | 🔧 "오늘의 룩" 캐릭터 착장 미리보기는 정적 이미지 1장(`/ootd-look.jpg`) 고정, 쇼핑 아이템 가격도 더미

**구성 (위→아래):**

```
헤더
  ← 뒤로가기 | 로고

Today's OOTD
"{이름}의 오늘 코디"
아이의 체질과 오늘 날씨를 반영한 맞춤 옷차림이에요.

현재 기온 카드  ✅ 실데이터
  15° (현재 기온만 표시 — 체감/최고/최저는 미표시)
  헤드라인+서브라인: 프로필(땀·추위 민감도)·기온 조건별 분기
  예) "{이름}는 오늘 레이어드 코디가 좋아요.
      기온 변화를 대비해 입고 벗기 쉬운 옷을 입혀주세요."

추천 아이템 (카테고리 카드 그룹: 아우터 → 이너 → 하의 → 악세사리)
· 각 카테고리는 카드 안에 2열 그리드 + 개수 뱃지

아우터 OUTER  2개 (고정)
  🧥 얇은 가디건 — 탈착 쉬운 소재
  🧢 경량 바람막이 — 바람 대비

이너 INNER  2개 (고정)
  👕 긴팔 면 티셔츠 — 땀 흡수 좋은 코튼
  👚 반팔 베이스 티 — 레이어드용

하의 BOTTOM  2개 (고정)
  👖 면 조거 팬츠 — 활동성 ↑
  🩳 스트레치 7부 팬츠 — 통기성

악세사리 ACC  0~4개 (조건부 동적 추가)
  🧣 면 목수건 — 바람 5m/s 이상일 때
  ☂️ 우산 — 강수 예보 시
  😷 KF94 마스크 — 비염 특이사항 보유 시
  🧴 보습 로션 — 피부 민감 특이사항 보유 시
  ⚠️ 단, 특이사항 매칭이 완전일치 비교라 실제로는 매칭 안 됨 (버그 현황 참고)

━━━━━━━━━━━━━━━━━━━━━━━━━━━
오늘의 룩: 추천 코디 미리 보기  [AI코디 추천 뱃지]  🔧 정적 이미지
━━━━━━━━━━━━━━━━━━━━━━━━━━━
"추천 아이템을 실제 코디로 미리 확인해보세요.
매일 날씨와 아이 체질에 맞춰 업데이트돼요."
(/ootd-look.jpg 고정 1장)

━━━━━━━━━━━━━━━━━━━━━━━━━━━
피해주세요  ✅ 조건부 동적 생성
━━━━━━━━━━━━━━━━━━━━━━━━━━━
  🧥 두꺼운 패딩 — 기온 23° 이상일 때
  👕 기모/폴라 이너 — 땀 많은 아이일 때
  🧣 두꺼운 목도리 — 위 조건 미해당 시 기본 표시
```

---

## P-07. 건강팁 (`/tips`)

✅ 구현됨 (정적 콘텐츠)

**구성:**

```
헤더
  ← 뒤로가기 | 로고

타이틀
  "근거 기반 건강팁"
  대학병원·의료학회·보건복지부 등 의학적 근거 기반 가이드를 제공합니다.

아이 프로필 매칭 배너
  👧 {이름}를 위한 오늘의 가이드
  호흡기 민감 (비염, 천식·기관지)  [수정 → /me 링크]

면책 고지 (배너 아래 별도 박스)
  ⚠️ 본 정보는 일반적 건강 정보이며 진료를 대체하지 않습니다.
     증상이 지속되거나 심한 경우 의료기관 진료를 권장합니다.

카테고리 필터 탭 (표시 중인 팁에서 동적 생성)
  전체 | 자외선 | 미세먼지 | 꽃가루 | 건조 | (기온) | 일반

아코디언 카드 (오늘 환경 조건 기준으로 표시):

  ☀️ 자외선 주의 — 자외선 높음, 영유아 피부 보호 필수
    가이드 본문 + 체크리스트 + 출처 (대한피부과학회 등)

  😷 미세먼지 주의 — 미세먼지 보통~주의, 호흡기 보호 가이드
    [내 아이 프로필 매칭: 비염·천식 등 호흡기 민감] 뱃지
    가이드 본문 + 체크리스트 + 출처

  🌳 꽃가루 경고 — 참나무 꽃가루 매우높음, 알레르기 아동 주의
    [내 아이 프로필 매칭: 알레르기 체질 / 호흡기 민감] 뱃지
    가이드 본문 + 체크리스트 + 출처

  💧 건조 정보 — 습도 38%, 건조 환경 보습 가이드
    가이드 본문 + 체크리스트 + 출처

  🧼 일반 정보 — 환절기 기본 위생, 손씻기 30초
    가이드 본문 + 체크리스트 + 출처

하단 출처 고지
  가이드 기준: 대한소아청소년과학회, 대한피부과학회, 대한이비인후과학회,
  대한소아알레르기호흡기학회, 질병관리청, 보건복지부, 환경부 에어코리아
```

---

## P-08. 마이 (`/me`)

✅ 실제 저장된 프로필 데이터 표시 (`loadProfiles()`). 라우트는 `/me`로 통일되어 바텀탭과 일치함
✅ 프로필 편집 (`/me/edit/[id]`, 2026-07-13) — 이름·생년월일·성별·건강 정보·민감도·하루 일과를 한 화면 폼으로 수정. 저장 시 localStorage(목록 전체 교체 `saveProfiles`) + DB upsert 동시 반영, 로컬 id 프로필이 DB에 신규 등록되면 id를 uuid로 교체해 동기화 중복 방지. 알림 설정(Step 7)은 편집 범위 밖 — 알림 설정 화면에서 다룸
✅ 구형 값 정규화 (`lib/profile-options.ts`) — 한국어 라벨 민감도·단일 키워드 건강 정보를 현행 코드값/선택지로 매핑해 표시·편집. 온보딩과 선택지 상수 공유

**구성 (위→아래 순서):**

```
헤더
  로고(AiDay 아이데이) | 🔔 알림 아이콘 | ⚙️ 설정 아이콘

타이틀
  마이페이지
  "온보딩에서 입력한 우리 아이 정보를 확인할 수 있어요"

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
아이 프로필  (N명 뱃지)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
아이 카드 (등록된 아이 수만큼 반복):

  [아이 아바타] 지우  [선택됨 뱃지]
               6세 · 여아
  ──────────────────────────
  생년월일      -
  건강 정보     -
  추위 민감도   -
  더위 민감도   -
  땀 분비       -
  등원 시간     -
  야외활동      -
  하원 시간     -
  저녁 야외활동 -
  ──────────────────────────
  [✏️ 편집 → /me/edit/[id]]  [🗑️ 삭제]

  (두 번째 아이: 도윤 4세·남아 — 동일 구조)

[+ 아이 추가하기] 버튼 (점선 테두리)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
계정
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔔 알림 설정  >   (준비 중 토스트)
📍 위치 설정  >   (준비 중 토스트)
📄 약관 및 정책  >   (준비 중 토스트)
💬 고객 문의  >   (준비 중 토스트)
🚪 로그아웃  >   ✅ 동작 — signOut + 로컬 프로필 정리 후 랜딩 이동 (confirm 확인 포함)
```

---

## N-01. 알림 발송 ❌ 미구현 — **P0 확정** (2026-07-07 승격)

> 제품 한 줄 정의("AI에게서 **먼저** 받는")와 랜딩 차별점 4번("앱을 열지 않아도 미리 받아보는")의 핵심 약속.
> 온보딩 Step 7이 설정을 **수집**하고 `children.notif`(JSON)에 저장까지 하지만, 수집된 설정으로 실제 발송하는 파이프라인은 없다.
> 2026-07-07 P0로 전면 승격 — 우선순위·채널·일정은 [PRD.md](./PRD.md) P0 블록과 [PRODUCT-DECISIONS §3-3](./docs/PRODUCT-DECISIONS.md)이 확정본이며 아래는 그 요약이다.

**발송 채널 (우선순위 순 — PRODUCT-DECISIONS §3-3, 2026-07-07 확정):**
1. 웹 푸시 (PWA) — **P0**. PWA 전환(manifest + Service Worker)과 함께 6주 고도화 범위에 편성. 외부 승인 없이 자급 구현 가능
2. 카카오톡 "나에게 보내기" (카카오 로그인 + talk_message) — **P0**. iOS 웹 푸시의 홈 화면 설치 선행 제약을 보완하는 채널. 온보딩의 카카오 연동 버튼 실동작화
3. 카카오 알림톡 (비즈메시지) — 발신프로필·템플릿 승인 리드타임 문제로 **지표 2(아침 재방문) 검증 후 착수**
- 이메일은 1차 채널에서 **제외** — 아침 준비 알림 특성상 도달·확인의 즉시성이 낮음

**발송 종류 × 트리거:**

| 종류 | 시각 | 데이터 기준 | 내용 |
|------|------|------------|------|
| 🌅 당일 아침 알림 (중심) | 등원 `notif.morningBefore`분 전 (기본 30분) | 당일 실측 + 예보 | 오늘 챙길 것 체크리스트 — FGI 수요 1위(76.5%), 기상 시점에 판단 완료 |
| 🌙 전날 밤 알림 | `notif.nightTime` (기본 21:00) | 익일 예보 (단기예보) | 내일 아침 준비 요약 (AI 리포트 hook 재활용) |
| ⚠️ 환경 경보 | 조건 충족 감지 시 즉시 (1일 1회/항목 상한) | `notif.alerts` 6종 각각의 임계값 (온보딩 Step 7에 정의됨) | 해당 경보 + 행동 1줄 |

**필요 인프라 (설계 결정 필요):**
- [ ] 스케줄러: Supabase Cron(pg_cron) vs Vercel Cron — 아이별 발송 시각이 제각각이므로 "매 10분 폴링 + 발송 대상 질의" 방식 권장
- [ ] 발송 이력 테이블 (`notifications_log`): 중복 발송 방지, 경보 1일 1회 상한 구현
- [ ] 서버가 아이별 위치·프로필로 날씨를 직접 조회하는 서버측 리포트 생성 경로 (현재 리포트 생성은 클라이언트 주도)

**수용 기준 (1차):** 당일 아침 알림 1종이 설정 시각 ±10분 내에 최소 1개 채널로 도착하고, OFF 설정 시 발송되지 않는다.

---

## 데이터·API 계약

### API 라우트 5종

| 라우트 | 메서드 | 파라미터 (기본값) | 응답 필드 | 캐시 |
|--------|--------|------------------|-----------|------|
| `/api/weather` | GET | `lat`(37.5665), `lon`(126.9780) | `temperature, feelsLike, sky, pty, humidity, windSpeed, pop, hourlyForecast[]`(06~21시 3시간 간격 6슬롯: `hour, temp, sky, pty, humidity, windSpeed, pop`) | 30분 |
| `/api/air` | GET | `station`(종로구) | `pm10, pm25, pm10Grade, pm25Grade, khai, khaiGrade, o3, no2, stationName, dataTime` (등급: 1좋음~4매우나쁨) | 1시간 |
| `/api/pollen` | GET | `region`(서울, 시/도명) | `oak, pine, weed, region, date` — ⚠️ `weed`는 **항상 null** (기상청 V3에 잡초 오퍼레이션 없음) | 6시간 |
| `/api/uv` | GET | `region`(서울) | `uvi, hourly{0..21}, region, date` — 발표시각 기준 h0~h75 3시간 오프셋 계산, 당일 자료 없으면 24시간 전 fallback | 1시간 |
| `/api/weather/weekly` | GET | `region`(서울), `lat`(37.5665), `lon`(126.9780) | `week[7]`(`day, date, icon, high, low, rain, weekend, source`) — D0~D2 단기예보(getVilageFcst) + D+3~ 중기예보(getMidLandFcst+getMidTa) 병합. `source`는 short/mid | 단기 30분·중기 3시간 |
| `/api/report` | POST | body: `{child, weather, air}` | `{hook, message, checklist[]}` — 파싱 실패 시 빈 값 반환(클라이언트가 규칙 기반 fallback) | 없음 (클라이언트 localStorage 당일 고정 + 급변 시 재생성) |

- 공통 에러: API 키 미설정 503 / 업스트림 오류 502 / 파싱·기타 500 (+ air는 측정 데이터 없음 404). 클라이언트는 `error` 필드 유무로 판별
- 환경변수: `KMA_API_KEY`(weather·uv), `AIRKOREA_API_KEY`(air), `POLLEN_API_KEY`(pollen), `ANTHROPIC_API_KEY`(report), `ANTHROPIC_BASE_URL`(report, 선택 — AI 게이트웨이/프록시 엔드포인트, 미설정 시 기본 주소), Supabase 키
- **필드 활용 현황:** `/api/weather`의 `hourlyForecast`는 홈 "시간대별 환경"(`buildTimeline`)·환경정보 "시간대별 날씨"·`/api/report` 일정 매핑에서 모두 사용 중. `feelsLike`는 홈 timeline에서 자체 계산(`temp - 0.7*wind`)해 표시하나 API 필드 자체는 UI 직접 참조 안 함
- ⚠️ **인증 없음:** 5개 라우트 모두 인증·레이트리밋 없이 공개. 특히 `/api/report`는 호출당 Claude 비용이 발생하므로 공개 배포 전 보호 필요 (게스트 정책과 함께 결정)

### 클라이언트 저장소 (localStorage)

| 키 | 내용 | 쓰는 곳 |
|----|------|---------|
| `aiweather:profiles` | ChildProfile[] 전체 | `lib/profile.ts` |
| `aiweather:activeProfileId` | 선택된 아이 ID | 홈/환경/옷차림/팁/마이/온보딩 |
| `aiweather:onboarding` | 온보딩 진행 상태 (단계+입력값, 완료 시 삭제) | 온보딩 |
| `aiday:report:v10:{childId}:{YYYY-MM-DD}` | AI 리포트 당일 고정 캐시 (`hook, message, checklist, prep, ts, env` — env는 급변 판정용 환경 스냅샷) | 홈 |
| `aiday:prepFrozen:v1:{childId}:{YYYY-MM-DD}` | 지나간 시간대 슬롯의 AI 준비물 고정값 | 홈 |

> ⚠️ 네임스페이스가 `aiweather:`와 `aiday:`로 혼재 — **`aiday:`로 통일하기로 확정** (1회성 마이그레이션 포함, 프로필 포맷 정규화 작업에서 처리 — [docs/PRODUCT-DECISIONS.md](./docs/PRODUCT-DECISIONS.md) §3-4)

### Supabase `children` 테이블 (lib/profile.ts DbRow 기준)

`id`(uuid, PK) · `user_id` · `name` · `emoji` · `gender` · `birth_year/month/day`(int) · `conditions`(text[]) · `condition_etc` · `cold_sensitivity` · `hot_sensitivity` · `sweat_level` · `schedule`(json) · `notif`(json) · `created_at`

- 쓰기: 온보딩 완료 시 `saveProfileToDb()` (비로그인 시 조용히 스킵). id는 UUID 형식일 때만 전달 — 로컬 생성 id(`c-…`)·데모 id는 빼고 보내 DB가 uuid를 생성 (2026-07-13 수정: 이전에는 `c-…` id를 그대로 upsert해 uuid 파싱 오류로 저장이 조용히 실패)
- 읽기: 홈은 `fetchProfilesFromDb()`(no-auth/error/ok 구분), 마이는 `syncProfilesFromDb()` — DB → localStorage 복원 (비로그인·조회 실패·DB 비어 있으면 로컬 유지)
- 이전: `/auth/landing`에서 DB가 비어 있으면 게스트 로컬 프로필(데모 제외)을 `uploadLocalProfilesToDb()`로 1회 업로드
- 삭제: 마이페이지 삭제 시 localStorage + DB(`removeProfileFromDb`) 동시 반영

### 프로필 값 포맷 규약 ⚠️ 현재 이원화 상태

| 필드 | 온보딩이 저장하는 값 | 데모 프로필 값 | 소비처가 기대하는 값 |
|------|--------------------|---------------|--------------------|
| `conditions` | 긴 라벨 ("호흡기 민감 (비염, 천식·기관지)") | 짧은 라벨 ("비염", "아토피", "피부 민감") | outfit·CharacterReport는 짧은 라벨 완전일치(버그), env·tips는 부분일치(정상) |
| `cold`/`hot` | 코드값 (`very-much`~`very-less`) | 문장 라벨 ("추위를 많이 타요") | outfit `buildOutfit`은 문장 라벨 비교 → **실프로필에서 절대 참이 안 됨** |
| `sweat` | 코드값 (`very-much`~`less`) | 문장 라벨 ("많아요") | 동일 문제 |

> 정규화 방안: 저장은 코드값으로 통일하고 표시 라벨은 매핑 테이블(마이페이지 방식)로 일원화. conditions 매칭 버그 수정 작업과 함께 처리 권장.

### 개인정보 (출시 전 필수)

- `children`은 **아동 건강정보**(민감정보) — 수집 근거·보존 기간·삭제 정책 미정의
- 푸터·가입 화면의 이용약관·개인정보처리방침 링크가 `href="#"` (실문서 없음)
- ~~마이페이지 프로필 삭제 시 DB 반영~~ — 해결됨 (2026-07-03, `removeProfileFromDb` 연결). 계정 자체 삭제(회원 탈퇴) 시 children 연쇄 삭제는 여전히 미정의

---

## 페이지 구현 현황 요약

| 경로 | 페이지 | 상태 |
|------|--------|------|
| `/` | 랜딩 | ✅ 완성 |
| `/signup` | 회원가입 | ✅ Supabase Auth (이메일 + Google OAuth) |
| `/login` | 로그인 | ✅ signInWithPassword + Google OAuth (성공 시 `/auth/landing`) |
| `/auth/landing` | 인증 후 판단 지점 | ✅ 게스트 프로필 DB 이전 + 프로필 유무로 홈/온보딩 분기 |
| `/reset-password` | 비밀번호 재설정 | ✅ 메일 링크 → 새 비밀번호 설정 |
| `/onboarding` | 온보딩 7단계 | ✅ localStorage + Supabase DB 저장 |
| `/home` | 홈 AI 리포트 | ✅ 날씨/AI 리포트 실연동, 🔧 추천 상품만 더미 |
| `/outfit` | 옷차림 | ✅ 규칙 기반 추천 실연동, 🔧 착장 이미지·가격 더미 |
| `/tips` | 건강팁 | ✅ 정적 콘텐츠 (의도적 — PR #33에서 실API 연동을 되돌림) |
| `/env` | 환경정보 | ✅ 날씨/대기질/꽃가루/자외선 API 4종 실연동 |
| `/me` | 마이페이지 | ✅ 실제 프로필 데이터 표시 |
| `/me/edit/[id]` | 프로필 편집 | ✅ 전 필드 수정 + localStorage/DB 동시 반영 |

---

## 미구현·버그 현황 (2026-07-03 코드 기준 재확인)

| 항목 | 심각도 | 내용 |
|------|--------|------|
| 프로필 값 포맷 이원화 (cold/hot/sweat) | 🟡 중간 | 온보딩은 코드값(`very-much` 등)을 저장하는데 `buildOutfit()`은 데모 프로필의 문장 라벨("많아요", "추위를 많이 타요")과 비교 → 실프로필에서 땀·추위 분기가 절대 작동 안 함. conditions 매칭 버그와 동류 — "데이터·API 계약 > 프로필 값 포맷 규약" 참고 |
| /api/report 무인증 공개 | 🟡 중간 | 인증·레이트리밋 없이 호출당 Claude 비용 발생. 게스트 모드도 실호출 중 — 공개 배포 전 보호 필요 |
| 건강 특이사항 매칭 버그 (옷차림·종합솔루션) | 🟡 중간 | `buildOutfit()`(`app/(main)/outfit/page.tsx`)과 `CharacterReport`가 `conditions.includes("비염")`, `includes("피부 민감")`처럼 **배열 완전일치**로 비교하지만, 온보딩이 저장하는 문자열은 "호흡기 민감 (비염, 천식·기관지)", "민감 피부 (아토피·건조·자외선)" — **절대 매칭되지 않아** 마스크·보습로션 추천이 프로필 기반으로 나오지 않음. (env/tips 페이지는 부분일치 `c.includes("호흡기")`라 정상) |
| 위치 텍스트·좌표 불일치 | 🟡 중간 | 홈에 "서울 강남구" 하드코딩 표시(환경정보는 측정소명 표시로 개선됨). 날씨 API 호출 좌표(37.5665, 126.9780)는 중구·시청 부근으로 4곳(home/env/outfit/api 기본값)에 하드코딩. 위치 기반 자동 조회 미구현 |
| 추천 아이템 가격·이미지 더미 | 🟢 낮음 | 홈/옷차림의 쇼핑 추천 카드는 여전히 이모지+고정 가격, 실제 쇼핑 연동 없음 |
| 옷차림 캐릭터 착장 미리보기 정적 | 🟢 낮음 | `/ootd-look.jpg` 고정 이미지, 날씨·프로필과 무관 |
| 꽃가루 잡초 카드 상시 "--" | 🟢 낮음 | `/api/pollen`의 `weed`가 항상 null(기상청 V3에 잡초 오퍼레이션 없음)인데 환경정보 UI에 카드가 노출됨 — 카드 제거 또는 "제공 안 함" 표기 필요 |
| Google OAuth 활성화 여부 미확인 | 🟢 낮음 | 코드상 `signInWithOAuth` 호출은 구현됨. Supabase 대시보드의 Google Provider 설정·클라이언트 ID는 코드로 확인 불가 — 별도로 직접 테스트 필요 |
| 약관·개인정보처리방침 실문서 없음 | 🟢 낮음* | 푸터·가입 화면 링크가 `href="#"`. *아동 건강정보를 다루므로 출시 시점엔 차단 사항 — "데이터·API 계약 > 개인정보" 참고 |

**이전에 기록됐던 아래 항목들은 해결된 상태입니다:**
- ~~구글 로그인 시 데모 프로필(지우) 표시·온보딩 건너뜀~~ — 인증 후 공통 판단 지점 `/auth/landing` 신설, 상태 기반 분기 + 홈 진입 가드 (2026-07-13)
- ~~온보딩 프로필 DB 저장 uuid 오류~~ — `profileToRow`가 UUID 형식 id만 전달하도록 수정 (2026-07-13)
- ~~이메일 재로그인 불가~~ — `/login` 페이지 신설, `signInWithPassword` + 비밀번호 재설정(`/reset-password`) 구현 (2026-07-03)
- ~~DB 프로필 읽기 미연결~~ — `syncProfilesFromDb()` 신설, 홈·마이 진입 시 DB→localStorage 복원. 마이 삭제 DB 반영 + 로그아웃 구현 (2026-07-03)
- ~~라우트 불일치 `/env`~~ — 바텀탭·페이지 모두 `/env`로 통일됨 (`components/BottomNav.tsx`)
- ~~라우트 불일치 `/me`~~ — 바텀탭·페이지 모두 `/me`로 통일됨
- ~~소셜 로그인 비동작~~ — Google OAuth 코드 구현됨 (`app/signup/page.tsx`)
- ~~온보딩 저장 없음~~ — localStorage + Supabase DB 저장 구현됨 (`lib/profile.ts`)
- ~~홈 AI 카드 더미~~ — Claude API(`/api/report`) 실연동
- ~~옷차림 더미~~ — 날씨+프로필 기반 규칙 로직(`buildOutfit`) 실연동 (AI 생성은 아님)
- ~~바텀탭 이모지 아이콘~~ — Lucide 아이콘으로 이미 교체됨 (`components/BottomNav.tsx`)

---

## 개발 우선순위

```
Phase 1 — 동작하는 백본 (완료)
  [x] Supabase Auth → 회원가입/로그인/세션
  [x] 온보딩 데이터 DB 저장 (children 테이블)
  [x] 기상청 API + 대기오염 API 연동

Phase 2 — AI 핵심 기능 (대부분 완료)
  [x] Claude API → 홈 AI 리포트 카드 실제 생성
  [x] 옷차림 추천 실제 데이터 연동 — 단, 규칙 기반 로직(buildOutfit)이며 LLM 생성은 아님
  [x] 환경정보 페이지 구현 (라우트는 /environment가 아니라 /env로 확정)
  [x] 마이페이지 구현 (라우트는 /my가 아니라 /me로 확정)

Phase 3 — 계정·데이터 신뢰성 (진행 중) ← 사용자 데이터가 유실되지 않는 상태 만들기
  [x] 로고 'AiDay 아이데이' 표기 + 온보딩 완료 문구("날씨" 삭제) 코드 반영 (2026-07-03)
  [x] 이메일 로그인 구현 (/login + signInWithPassword + 비밀번호 재설정 /reset-password) (2026-07-03)
  [x] DB 프로필 읽기 연결 (syncProfilesFromDb — 홈·마이 복원, 삭제 DB 반영, 로그아웃) (2026-07-03)
  [ ] 프로필 값 포맷 정규화 (코드값 통일 + 매칭 버그 수정 — 별도 세션 진행 중)
  [ ] /api/report 보호 (인증 또는 레이트리밋) + 게스트 정책 확정

Phase 4 — 데이터 완성도 (미착수) ← 화면의 모든 숫자를 진짜로
  [x] 홈 시간대별 환경 실데이터 연동 (buildTimeline — weather.hourlyForecast·air·uv·pollen 매핑, mock은 폴백)
  [x] 환경정보 더미 섹션 실데이터 연동 (야외활동 지수 / 시간대별 / 주간 날씨) + 새로고침 실동작 + 오존 등급 (2026-07-15)
  [ ] 위치 기반 자동 날씨 조회 (Geolocation → 격자 변환은 구현돼 있음, 측정소 매핑·수동 선택 fallback 설계 필요)
  [ ] 건강팁 AI 생성 전환 (현재 정적 콘텐츠 — PR #33에서 실API 연동을 의도적으로 되돌림)

Phase 5 — 확장 (미착수) ← 알림·PWA는 2026-07-07 P0 승격 (일정·순서는 PRD.md P0 블록이 확정본)
  [ ] 알림 발송 파이프라인 (N-01 — P0 승격, 당일 아침 알림 1종부터. 웹 푸시 → 카카오 "나에게 보내기" 순)
  [ ] PWA manifest + Service Worker (웹 푸시 채널의 선행 조건 — P0 편성)
  [ ] 약관·개인정보처리방침 실문서 + 계정/데이터 삭제 정책
  [ ] PostHog 애널리틱스 연동
```

---

*마지막 업데이트: 2026-07-08 (v0.3.1) | 기준선 문서 역할 명시(이후 변경은 PRD.md 추적), 한 줄 정의를 "하루 첫 육아 판단" 프레임으로 교정, N-01 알림 P0 승격·채널 우선순위(웹 푸시 → 카카오 나에게 보내기) 반영 | 직전: 2026-07-03 (v0.3) 디자인 개편 반영 + 전 페이지 코드 대조 재감사*
