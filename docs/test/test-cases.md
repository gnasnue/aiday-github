# 아이데이(AiDay) 테스트 케이스

기준: `PRD.md` v2.8(코드베이스 동기화) + 실제 코드(`app/`, `lib/`). PRD가 미구현으로 명시한 항목은 N/A로 표기하고 케이스를 만들지 않았다.
**2026-07-24 Playwright 실행 완료 — 아래 "실제 결과"·"통과/실패" 열은 실행 결과다.** 재현 절차·스크린샷·트레이스 경로·근본원인 분류는 `test-report.md` 참조.

**2026-07-24 (수정 후 재검증) — BUG-1·BUG-2 수정 완료 + TC-AUTH-05 자동화 완료.** 아래 P0-2·P0-4·TC-HOME-04·TC-AUTH-05 행은 수정·재검증 후 결과로 갱신했다. 재검증은 라이브 데모가 아니라 **로컬 dev 서버**(`E2E_BASE_URL=http://localhost:3000`)로 수행했다 — 수정 사항은 아직 배포되지 않았으므로 라이브 데모(aiday-demo.vercel.app)는 배포 전까지 이전 동작을 유지한다. 상세는 `test-report.md` §수정 및 재검증 참조.

역할 표기: G=게스트(비로그인), U=로그인 사용자.

## P0 — 핵심 5종

| ID | 제목 | 관련 PRD | 역할 | 사전조건 | 실행 단계 | 예상 결과 | 우선순위 | 실제 결과 | 통과/실패 |
|---|---|---|---|---|---|---|---|---|---|
| P0-1 | 이메일 회원가입 → 로그인 → 세션 유지 | §05 신뢰성 정합, 기준선 로그인 | G→U | 신규 QA 테스트 이메일 | 1) `/signup`에서 `aiday-qa-test+{ts}@example.com`로 가입 2) 약관 동의 체크 3) 제출 4) `/onboarding` 도착 확인 5) 새로고침 후 세션(쿠키) 유지 확인 6) 같은 계정으로 `/login` 재로그인 | 가입 성공, 세션이 있으면 로그인 상태 유지. 이메일 인증 필요 시 BLOCKED 기록 | P0 | **이메일 인증 OFF 확인 — 가입 즉시 세션 발급(`sb-*-auth-token` 쿠키), 새로고침·재로그인 후에도 유지.** 조건부 분기(BLOCKED)는 발생하지 않음 | **PASS** |
| P0-2 | 게스트 vs 로그인 레이트리밋 차등(429) | §05 안전·비용 보호 레이어 | G, U(모킹) | 게스트 IP 버킷 | 1) `/api/report`에 최소 유효 payload로 직접 POST 반복(429까지, 상한 15회) 2) 응답 상태·본문(`error`,`limit`)·`Retry-After` 확인 3) 429 도달 후 실제 `/home` 재진입해 화면이 빈 화면이 아님을 확인 4) 로그인 429는 라우트 모킹으로 문구 차이 검증 | 게스트 10회 도달 시 429+Retry-After+비어있지 않은 화면. 로그인 429는 가입 유도 문구 미노출 | P0 | 서버 로직은 최초부터 정확히 작동. **BUG-2 수정 후(app/api/report/route.ts의 `isGuest` 플래그 + app/(main)/home/page.tsx의 `reportLimitReached` 영구 배너): 게스트 429엔 "가입하면 계속 이용" 문구+CTA, 로그인 429엔 한도 안내만 표시되어 시각적으로 구분됨. 로컬 dev 재검증 완료(TC-RATE-GUEST-CTA·TC-RATE-LOGIN PASS)** | **PASS**(수정 완료, 로컬 재검증) |
| P0-3 | 온보딩 아이 프로필 등록 → 새로고침 후 유지 | §05 판단 엔진 입력 축②③, 기준선 온보딩 5단계 | G | 새 브라우저 컨텍스트 | 1) `/onboarding` 5단계 전체 입력 2) 완료 3) 홈 진입 확인 4) 새로고침 5) 프로필·값 유지 확인 | 5단계 완료 후 홈에 신규 프로필 표시, 새로고침 후에도 유지 | P0 | 5단계 모두 정상 진행, `localStorage.aiweather:profiles`에 즉시 저장 확인, 새로고침 후에도 프로필 유지 | **PASS** |
| P0-4 | 홈 AI 브리핑 생성 + 체크리스트 토글 | §05 판단 엔진, S-001 | G | 데모 프로필 지우 | 1) `/home` 진입(게스트) 2) 실제 `/api/report` 200 확인 3) 훅·메시지·체크리스트 렌더 확인 4) 체크리스트 토글 5) 프로필 전환(도윤) 6) 새로고침 60초 쿨다운 | AI 리포트 표시, 체크리스트 토글 시 카운터 즉시 갱신 | P0 | 실제 리포트 생성·토글·프로필 전환·새로고침 쿨다운 전부 정상 작동. **BUG-1 수정 후(app/(main)/home/page.tsx의 `isDemoProfile` 배지 렌더 추가): 데모 프로필에 "(예시)" 배지 정상 표시. 로컬 dev 재검증 완료(모킹 회귀 TC-HOME-04 mobile+desktop 2건 PASS)**. 실 API 재실행은 비용(Claude 호출) 절감을 위해 생략 — 배지는 순수 클라이언트 렌더 로직이라 모킹 검증으로 충분 | **PASS**(수정 완료, 모킹 재검증) |
| P0-5 | 프로필 편집 → 저장 결과 반영 확인 | §05 프로필 값 포맷 정규화, S-001 KF94 마스크 수락 기준 | G | 데모 프로필 지우 | 1) `/me`에서 지우 편집 2) 이름 변경 + 호흡기 민감 체질 확인/체크 3) 저장(민감정보 동의 게이트 통과) 4) `/me` 반영 확인 5) 새로고침 유지 확인 6) `/outfit`에서 KF94 마스크 확인 | 저장 성공 토스트 + 이름 반영 + KF94 마스크 추천 | P0 | 이름 변경 저장 성공("프로필이 저장됐어요"), `/me` 목록·새로고침 후 모두 반영, `/outfit`에 "KF94 마스크"·"키즈 KF94 마스크" 2개 카드 모두 노출 확인 | **PASS** |

## P1 — 예외·경계·탐색

### 인증/권한

| ID | 제목 | 역할 | 예상 결과 | 실제 결과 | 통과/실패 |
|---|---|---|---|---|---|
| TC-AUTH-01 | 로그인 잘못된 자격증명 | G | "이메일 또는 비밀번호가 올바르지 않아요" 토스트 | 동일 문구 확인, `/login` 유지 | PASS |
| TC-AUTH-02 | 로그인 이메일 빈 값 제출 | G | "이메일을 입력해주세요" 토스트 | 동일 문구 확인 | PASS |
| TC-AUTH-03 | 회원가입 비밀번호 불일치 | G | "비밀번호가 일치하지 않아요" 토스트, 이동 없음 | 확인, `/signup` 유지 | PASS |
| TC-AUTH-04 | 회원가입 약관 미동의 제출 | G | "이용약관을 확인해주세요" 토스트 | 확인 | PASS |
| TC-AUTH-05 | 이메일 저장 옵션 유지 | G | localStorage 기준 이메일 자동 채움 | **자동화 완료(2026-07-24 재검증) — 체크 후 제출 시 `aiday.rememberedEmail`에 저장, 재방문 시 이메일 자동 채움+체크박스 유지 확인. 체크 해제 후 제출 시 저장값 삭제, 재방문 시 빈 값+미체크 확인(로그인 성공 여부와 무관하게 검증 — 실 계정 불필요)** | **PASS** |
| TC-AUTH-06 | Google OAuth 로그인 | G | 실계정 필요 — BLOCKED | 버튼 노출·클릭 가능 확인. **실제 OAuth 완료는 수행하지 않음** | BLOCKED |
| TC-AUTH-07 | 로그인 세션 만료(쿠키 삭제 모킹) | U(모킹) | 게스트 데모 화면으로 정상 강등, 크래시 없음 | 세션 쿠키(`sb-*-auth-token`) 삭제 후 `/me` 재진입 시 `/login` 리다이렉트 없이 게스트 데모 화면 정상 렌더 확인(전역 미들웨어 부재와 일치) | PASS |

### 권한 없는 사용자 접근(비로그인 라우트 접근)

| ID | 제목 | 역할 | 예상 결과 | 실제 결과 | 통과/실패 |
|---|---|---|---|---|---|
| TC-GUEST-01 | 비로그인 `/home` 직접 접근 | G | 리다이렉트 없이 200, 데모 프로필 렌더 | 확인 — `/login` 등으로 리다이렉트되지 않음, 지우/도윤 칩 노출 | PASS |
| TC-GUEST-02 | 비로그인 `/env`,`/outfit`,`/tips`,`/me` 직접 접근 | G | 전부 200, 크래시 없음 | 4개 경로 모두 확인 | PASS |
| TC-GUEST-03 | 비로그인 `/me/edit/[id]` 존재하지 않는 id 접근 | G | 토스트 후 `/me`로 replace | 확인 | PASS |
| TC-GUEST-04 | 비로그인 `/onboarding` 이어하기 | G | `/home` 이동 후 재방문 시 복원 | TC-ONB-07로 대체 검증(아래) — 2/5 단계로 정확히 복원 확인 | PASS(TC-ONB-07 참조) |

### 온보딩 — 입력 검증/경계

| ID | 제목 | 역할 | 예상 결과 | 실제 결과 | 통과/실패 |
|---|---|---|---|---|---|
| TC-ONB-01 | 1단계 이름 미입력 다음 클릭 | G | "아이 이름을 입력해주세요" 토스트, step 유지 | 확인, 1/5 유지 | PASS |
| TC-ONB-02 | 1단계 생년월 미선택 | G | "태어난 연도와 월을 선택해주세요" 토스트 | 확인 | PASS |
| TC-ONB-03 | 2단계 건강 특이사항 전부 미선택 | G | "하나 이상 선택해주세요" 토스트 | 확인 | PASS |
| TC-ONB-04 | 2단계 민감정보 미동의 상태로 실제 건강 특이사항 선택 | G | "건강 정보 활용 동의를 확인해주세요" 토스트 차단 | 확인, 2/5 유지 | PASS |
| TC-ONB-05 | 3단계 온도 민감도 일부만 선택 | G | "세 항목 모두 선택해주세요" 토스트 | 확인 | PASS |
| TC-ONB-06 | 온보딩 중 뒤로가기 | G | 이전 단계로 이동 | 뒤로가기 버튼 클릭 시 2/5→1/5로 정상 이동 확인 | PASS |
| TC-ONB-07 | 온보딩 중간 이탈("나중에 이어서 하기") 후 복귀 | G | 저장된 단계부터 재개 | 2단계 이탈 후 재방문 시 정확히 "2 / 5"로 복원 확인 | PASS |
| TC-ONB-08 | 온보딩 완료 화면 새로고침 | G | 홈으로 정상 이동, 크래시 없음 | 확인 | PASS |

### 홈/리포트 — 정상·중복·데이터 많음/없음

| ID | 제목 | 역할 | 예상 결과 | 실제 결과 | 통과/실패 |
|---|---|---|---|---|---|
| TC-HOME-01 | 프로필 전환(지우↔도윤) | G | 각 프로필의 리포트로 전환 | P0-4 내에서 확인 — 도윤 전환 후 정상 재렌더(크래시 없음) | PASS |
| TC-HOME-02 | 리포트 새로고침 60초 쿨다운 중복 클릭 | G | 2번째 클릭은 "방금 갱신했어요" 토스트만 | P0-4 내에서 확인 | PASS |
| TC-HOME-03 | 체크리스트 토글(체크→해제) | G | 카운터가 정확히 갱신 | P0-4 내에서 확인 — 토글/원복 카운터 정확 | PASS |
| TC-HOME-04 | 데모 프로필 "예시" 표시 확인 | G | PRD S-001 기대: "예시" 배지 표시 | **수정 완료(BUG-1) — 배지 렌더 추가 후 mobile+desktop 모킹 회귀 재실행, 2건 모두 "(예시)" 배지 노출 확인** | **PASS** |
| TC-HOME-05 | 프로필 다건 등록(5개) 데이터 많음 | G | 레이아웃 깨짐 없음 | localStorage에 프로필 3개 추가(총 5개) 후에도 가로 스크롤 없음, 전부 노출 확인 | PASS |
| TC-HOME-06 | 리포트 👍/👎 피드백 제출 | G | 제출 후 상태 변화("의견 감사해요") | P0-4 내에서 확인 | PASS |
| TC-HOME-07 | 홈→환경정보→뒤로가기 캐시 확인 | G | 재요청 없이 캐시 재사용 | 뒤로가기로 홈 재진입 시 `/api/report` 재호출 없음(1회 유지) 확인 | PASS |

### 저녁 결과 피드백 / 발송 기록 (N/A)

| ID | 제목 | 관련 PRD | 비고 |
|---|---|---|---|
| TC-NA-01 | 저녁 20시 이후 결과 피드백 카드(더웠/적당/추웠) | S-003 | **N/A — 미구현.** PRD §05 명시(⏳ 재평가). 코드에 해당 UI 없음(grep 확인) |
| TC-NA-02 | `notifications_log` 수동 발송 기록 조회 | §05 P0-5 | **N/A — 미착수.** UI 미노출 |
| TC-NA-03 | `daily_briefings` 서버 단일 원본 저장 확인 | §05 P0-1 | **N/A — 미구축.** 정합은 클라 localStorage 당일 캐시로 대체 확인(TC-HOME-07로 대체 검증 — PASS) |

### 서버·네트워크 오류(모킹)

| ID | 제목 | 역할 | 예상 결과 | 실제 결과 | 통과/실패 |
|---|---|---|---|---|---|
| TC-ERR-01 | 환경 API 전체 502 | G | "환경 데이터를 불러오지 못했어요" 배너 | 확인, 크래시 없음 | PASS |
| TC-ERR-02 | 날씨 API만 502(부분 장애) | G | 크래시 없이 다른 카드 정상 | 확인 — 리포트 카드(모킹) 정상 렌더 | PASS |
| TC-ERR-03 | `/api/report` 503(설정 오류) | G | "기본 추천" 라벨 + 비어있지 않은 규칙 기반 리포트 | 확인 | PASS |
| TC-ERR-04 | `/api/report` 네트워크 단절 | G | 재시도 후 기본 추천 폴백 | 확인(재시도 지연 포함 최대 21.5s 관측 — 논의 대상) | PASS |
| TC-ERR-05 | `/api/report` 잘못된 JSON 입력(빈 body) | G(API 직접) | HTTP 400 + "필수 입력…" | 확인 | PASS |
| TC-ERR-06 | 오프라인 상태에서 홈 진입 | G | 크래시 없는 네트워크 오류 | 오프라인 최초 진입은 브라우저 레벨 네트워크 오류로 실패(Service Worker 부재, 설계상 예상된 동작) — 크래시(500)는 아님 | PASS(관찰) |

### 반응형(모바일/데스크톱)

| ID | 제목 | 역할 | 예상 결과 | 실제 결과 | 통과/실패 |
|---|---|---|---|---|---|
| TC-RESP-01 | 랜딩 페이지 390px 렌더 | G | 레이아웃 깨짐 없음 | 확인, 가로 스크롤 없음 | PASS |
| TC-RESP-02 | 랜딩 페이지 1280px 렌더 | G | 레이아웃 정상 | 확인 | PASS |
| TC-RESP-03 | 홈 하단 탭바 모바일/데스크톱 | G | 5개 탭 모두 노출·클릭 가능 | 확인(양쪽 프로젝트) | PASS |

### 랜딩/네비게이션

| ID | 제목 | 역할 | 예상 결과 | 실제 결과 | 통과/실패 |
|---|---|---|---|---|---|
| TC-LAND-01 | 랜딩 "무료로 시작하기" → 회원가입 | G | `/signup` 이동 | 확인 | PASS |
| TC-LAND-02 | 랜딩 "먼저 둘러볼게요" → 홈 게스트 진입 | G | `/home` 이동 | TC-GUEST-01로 대체 검증 | PASS |
| TC-LAND-03 | 랜딩 푸터 약관/개인정보 링크 | G | 실문서로 이동(href="#" 아님) | 확인 — `/terms`,`/privacy` 정상 이동 | PASS |
| TC-LAND-04 | 하단 탭 전체 이동 | G | 각 페이지 정상 렌더 | 확인(양쪽 프로젝트) | PASS |

## 요약 카운트 (2026-07-24 최초 실행)

- 총 Playwright 테스트 실행: **55**(모바일+데스크톱 프로젝트 합산)
- 통과: **50** (그중 1건 TC-AUTH-06은 실행상 PASS이나 케이스 완결 기준으로는 BLOCKED로 별도 집계)
- 실패: **5**(모두 테스트 코드 결함이 아닌 실제 발견 — BUG-1 3건, BUG-2 2건. 두 건의 서로 다른 근본원인)
- BLOCKED: **1**(TC-AUTH-06 Google OAuth 실계정 수동 검증 필요)
- N/A(미구현이라 케이스 미작성): 3
- 미자동화(시간 예산 보류, P2): TC-AUTH-05

## 요약 카운트 (2026-07-24 수정 후 재검증 — 최신)

- 총 Playwright 테스트: **56**(TC-AUTH-05 자동화로 +1)
- 통과: **55**
- 실패: **0**
- BLOCKED: **1**(TC-AUTH-06 Google OAuth — 수정 불가 항목, 실계정 필요)
- N/A(PRD 미구현이라 케이스 미작성): 3
- 미자동화: 0 (TC-AUTH-05 자동화 완료)

**"실패 0"의 범위:** BLOCKED(외부 OAuth 계정 필요)와 N/A(PRD가 스스로 미구현·재평가 대상으로 표기한 기능)는 애플리케이션 버그가 아니므로 "수정 대상 오류"에 포함하지 않는다. 발견된 실제 버그(BUG-1, BUG-2)는 모두 수정·로컬 재검증 완료. **단, 재검증은 로컬 dev 서버 기준이며 라이브 데모(aiday-demo.vercel.app)는 배포 전까지 이전 동작을 유지한다** — 상세는 `test-report.md` §수정 및 재검증 참조.
