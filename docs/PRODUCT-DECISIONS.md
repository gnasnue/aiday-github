# AiDay 제품 결정 문서 (Product Decisions)

> 이 문서는 풀 PRD를 대체한다. 문제 정의·기능 명세·디자인은 기존 문서가 담당하고,
> 여기에는 그 문서들에 없는 세 가지 — **성공 지표 · 출시 기준 · 보류됐던 제품 결정** — 만 기록한다.
> 근거 리뷰: [docs/reviews/2026-07-04-ceo-review-prd-작성.md](./reviews/2026-07-04-ceo-review-prd-작성.md) (판정: CUT)
> 결정을 바꿀 때는 이 문서를 갱신하고 하단 변경 이력에 남긴다.

**문서 지도 — 여기 없는 것:**

| 내용 | 담당 문서 |
|------|----------|
| 왜 만드는가 (문제·타겟·포지셔닝·안티패턴) | [MANIFESTO.md](../MANIFESTO.md) |
| 무엇을 만드는가 (화면별 명세·구현 현황·버그) | [SPEC.md](../SPEC.md) |
| 어떻게 보이는가 (디자인 시스템) | [DESIGN.md](../DESIGN.md) |
| 개발 우선순위 Phase | [SPEC.md > 개발 우선순위](../SPEC.md), [README.md > 로드맵](../README.md) |

---

## 1. 성공 지표 (Success Metrics)

**검증하려는 가설:** "부모는 매일 아침, 환경 수치가 아니라 *우리 아이 기준으로 해석된 하루 첫 육아 판단*을 원한다." (MANIFESTO Core Truth)

| # | 지표 | 정의 | 목표 | 무엇을 검증하나 |
|---|------|------|------|----------------|
| 1 | 온보딩 완료율 | 가입 완료 → 온보딩 5단계 완료 비율 *(2026-07 7→5단계 축소 반영)* | ≥ 60% | 가치 제안이 프로필 입력 수고를 넘어서는가 |
| 2 | **아침 재방문율** ⭐ | 첫 방문 후 14일 내, 아침(05~10시 KST) 방문일 3일 이상인 사용자 비율 | ≥ 30% | **아침 판단 습관이 형성되는가 — 북극성 지표** |
| 3 | 체크리스트 인터랙션율 | AI 리포트가 노출된 세션 중 "오늘 챙길 것" 체크 1회 이상 발생 비율 | ≥ 40% | 리포트가 읽을거리가 아니라 *행동 가이드*로 쓰이는가 |

- 북극성은 **2번**이다. 1·3번은 2번이 안 나올 때 원인을 가르는 진단 지표다 (1 낮음 = 진입 문제, 3 낮음 = 리포트 품질 문제).
- **측정 수단 (2026-07-19 갱신):** Supabase 자체 계측으로 측정한다 — `events`·`feedback` 테이블 + `beta_*` 지표 뷰(사용법: [docs/beta-metrics.md](./beta-metrics.md)). 구 결정(PostHog 선행)은 베타 규모(12~15가구)에 과해 폐기하고, PostHog는 공개 베타 확장 시 재검토한다. "수치 없이 감으로 판정 금지" 원칙은 유지.
- 지표는 3개를 유지한다. 추가하고 싶어지면 기존 것을 빼고 넣는다. **하루 루프(저녁 회수·알림장) 사용률은 이 3종에 넣지 않고 §3-8의 기능 존치 게이트로만 쓴다** — 아침 판단이 북극성이라는 사실을 흐리지 않기 위함.
- **베타 한정 보조 진단(3지표 체계 외):** 리포트 유용성 — 리포트 하단 👍/👎 중 👍 비율(`beta_report_usefulness_daily`). 행동 로그가 답하지 못하는 "판단이 맞았는가"를 가르는 품질 신호로, 목표치 판정 대상은 아니다.

## 2. 출시 기준 (Definition of Done)

**"출시"의 정의:** 지인 테스트를 넘어 링크를 공개적으로 공유할 수 있는 상태 (공개 베타). 앱스토어·마케팅 이전 단계.

### 출시 차단 (Blocker) — 이것 없이는 공개하지 않는다

- [x] **`/api/report` 보호** — IP·사용자 기준 레이트리밋 적용 (2026-07-22, `lib/rate-limit.ts` + `report_usage`). 게스트 IP 일 10회 / 로그인 사용자 일 20회, 초과 시 429. ⚠️ **배포 시 Vercel에 `SUPABASE_SERVICE_ROLE_KEY`를 반드시 설정할 것** — 없으면 리밋이 비활성(fail-open)되고 이 블로커는 해소되지 않은 상태가 된다
- [x] **프로필 값 포맷 정규화 + 특이사항 매칭 버그 수정** — "우리 아이 맞춤"이 실프로필에서 작동. *(2026-07-24 코드 확인: `lib/profile-options.ts` `normalizeSensitivity`/`normalizeSweat`/`normalizeConditions` + `lib/domain/child-conditions` 매칭으로 실프로필에서 호흡기 민감 → KF94 마스크 추천 작동)*
- [x] **홈 "시간대별 환경"·종합솔루션 실데이터 연동** — *(2026-07-24 코드 확인: `home/page.tsx`가 `/api/weather` `hourlyForecast` 실데이터로 시간대별·케어 플랜을 구성하고 `weather-mock`은 미사용 폴백만. 종합솔루션 섹션은 AI 브리핑과 중복이라 제거됨)*
- [x] **이용약관·개인정보처리방침 실문서** — *(2026-07-24 코드 확인: `app/terms/page.tsx`·`app/privacy/page.tsx` 존재)*. 아동 건강정보(민감정보) 수집 근거·보존 기간·삭제 정책 포함
- [x] **게스트 레이트리밋 적용** — 결정 3-1 구현 완료 (2026-07-22). 게스트는 IP 기준 일 10회. IP 원문은 저장하지 않고 솔트 해시만 버킷 키로 쓴다
- [x] **홈 "추천 아이템" 섹션 — 홈에서 제거** — *(2026-07-24 코드 확인: `item-recommend`는 `outfit/page.tsx` 전용, 홈에는 추천 아이템 섹션이 렌더되지 않음)*. ⚠️ **PRD S-001은 "더미 카드 유지"로 서술돼 있어 코드와 상충 — PRD 후속 정정 필요**(코드=제거를 정답으로 확정). MANIFESTO 안티패턴("쇼핑 추천 앱이 아니다")과도 정합
- [x] **건강팁(tips) 목데이터 제거 + 정직한 출처 표기(M2)** — *(2026-07-24 코드 확인: `lib/tips/select.ts`가 공유 `fetchEnvData` 결과로 severity 판정, `lib/domain/child-conditions` 프로필/나이 매칭, 결측 시 fail-closed(상시 팁만); `lib/tips/content.ts` 출처는 "문서명+발간연도"로 격상·미검증 초안 플래그. 커밋 #158 기온 기반 폭염·한파 신호 포함)*. 출처 딥링크 강화(M3)·검증 자동화(M4)·개인화 확대는 도메인전문가+법률 검토 게이팅으로 이 블로커 범위 밖 유지
- [ ] **이메일 인증(Confirm email) 재활성화 + 커스텀 SMTP 연결** — 지인 테스트 기간 한정으로 인증을 임시 비활성화(2026-07-13 결정). 인증이 꺼진 상태로 공개하면 타인 이메일 선점 가입 → Google 계정 자동 연결(identity linking)을 통한 계정 탈취 경로가 열리므로, 공개 전 반드시 인증을 다시 켜고 커스텀 SMTP(Supabase 기본 메일은 시간당 2~4통 제한 + naver 도달률 낮음)를 연결한다. Site URL/Redirect URLs도 배포 도메인으로 설정

### 출시 후 개선 (Fast-follow) — 출시를 막지 않는다

1. **알림 발송 (당일 아침·전날 밤 브리핑 — 웹 푸시 + 카카오톡)** — 서비스의 핵심 약속이지만, 검증 가설(아침 판단의 가치)은 알림 없이도 지표 2로 측정 가능하므로 출시를 막지는 않는다. 단, 6주 고도화 계획([PRD.md](../PRD.md))에서는 발송 2종·카카오 채널까지 P0로 편성해 출시 직후 제공을 목표로 함 (2026-07-07 확장). 당일 아침을 중심에 둔 근거: 사전 가설검증 설문 수요 1위(당일 아침 자동 알림 76.5%, 13/17 — [원본](./validation/2026-05-사전-가설검증-설문.md)) + 판단 입력(당일 시간대별 예보)의 신선도 (채널·범위 결정: §3-3)
2. ~~환경정보 더미 섹션 3종 실데이터화 (야외활동 지수 / 시간대별 / 주간 날씨) + 새로고침 실동작~~ — ✅ 완료 (2026-07-15, 오존 등급 포함). 주간 예보는 `/api/weather/weekly`로 분리되어 하루 탭 '이번 주 컨디션 예보'의 입력으로도 쓰인다
3. 위치 기반 자동 날씨 조회 (현재 좌표 4곳 하드코딩)
4. 오존 등급 계산, 꽃가루 잡초 카드 정리
5. 옷차림 착장 미리보기 실연동 (현재 고정 이미지)

## 3. 확정된 제품 결정 (구 "미정의" 항목)

SPEC.md에 "미정의 — 스펙 결정 필요"로 보류되어 있던 항목들을 아래와 같이 확정한다.

### 3-1. 게스트의 AI 리포트 호출 — **허용 + 레이트리밋**

- 게스트도 `/api/report` 실호출을 허용한다. "먼저 둘러볼게요" 경험이 곧 핵심 가치의 시연이므로, 차단하면 가입 전환의 가장 강한 근거를 스스로 없애는 셈.
- 대신 IP 기준 레이트리밋을 둔다 (기본값 제안: **일 10회/IP** — 데모 프로필 2개 × 재시도 여유. 구현 시 조정 가능).
- **게스트 → 가입 전환 시** localStorage 프로필을 DB로 이전한다 (가입·로그인 성공 직후 1회 업로드, 이후 DB가 원본).

### 3-2. 데모 프로필 표기 — **"예시" 뱃지 표시**

- 게스트 홈의 데모 프로필(지우·도윤)에 "예시" 뱃지를 표시해 실제 아이처럼 보이지 않게 한다.
- 사용자가 첫 실프로필을 등록하면 데모 프로필은 목록에서 제거한다.

### 3-3. 알림 1차 채널 — **웹 푸시 (PWA)** *(2026-07-07 변경 — 구 결정: 이메일)*

- N-01 파이프라인의 1차 채널은 **웹 푸시**로 한다. 선행 작업이던 PWA 전환(manifest + Service Worker)을 6주 고도화 범위([PRD.md](../PRD.md) P0 — 선제 알림)에 함께 편성해 부담을 해소했다. 외부 승인 없이 자급 구현이 가능하고, "홈 화면에 추가" 설치 경험이 리텐션에 유리하다.
- **이메일을 1차에서 제외한 사유:** 아침 준비 알림 특성상 도달·확인의 즉시성이 낮아 "먼저 알려주는" 약속에 부합하지 않음.
- 카카오톡 **"나에게 보내기"**(카카오 로그인 + talk_message)는 **P0로 승격한다** *(2026-07-07 2차 변경 — 구: P1)*. 근거: iOS 웹 푸시는 PWA 홈 화면 설치가 선행되어야 작동하므로, 웹 푸시 단독으로는 아이폰 사용자에게 "선제적으로 먼저 알려주는" 핵심 약속이 도달하지 않음. 선제 알림은 부가 기능이 아니라 서비스 본질의 전달 수단이라는 판단(PRD.md P0 참조). 온보딩의 카카오 연동 버튼은 실동작화한다.
- 카카오 알림톡(비즈메시지)은 발신프로필·템플릿 승인 리드타임 문제로 지표 2(아침 재방문) 검증 후 착수 — 기존 결정 유지 ("나에게 보내기"와 별개 채널).
- **일정 컨틴전시:** 카카오 채널(Phase 5)이 지연되면 공개 베타는 웹 푸시만으로 개시하고 카카오 채널은 베타 직후 첫 fast-follow로 이월한다.
- **(2026-07-12 착수 게이트 추가 — PRD v2.4)** "나에게 보내기"의 P0 지위는 유지하되 **무조건 구현하지 않는다.** 4주차 알림 퍼널 실측(PWA 설치율·권한 허용률)에서 iOS 도달 병목이 확인된 경우에만 Phase 5에 착수하고, 아니면 베타 직후 첫 fast-follow로 이월한다(해당 주는 골든 케이스 완성·판단 품질 개선에 투입). 채널 운용은 **사용자당 주 채널 1개 + 실패 시 폴백** — 아침·밤 × 2채널 중복 발송(하루 최대 4회)은 알림 피로를 만들므로 하지 않는다. 근거: 토큰 캡처·갱신·폐기·연동 해제·탈퇴 삭제까지 포함한 실제 구현 공수 재평가(Supabase는 프로바이더 토큰을 보관·갱신하지 않음) + 알림 피로 방지.

### 3-4. localStorage 네임스페이스 — **`aiday:`로 통일**

- 현재 `aiweather:`와 `aiday:`가 혼재. **`aiday:`로 통일**하고 기존 키는 1회성 마이그레이션(읽기 시 구키 발견 → 신키 복사 → 구키 삭제)으로 이전한다.
- 시점: 프로필 값 포맷 정규화 작업에 포함해 한 번에 처리한다 — 저장 스키마를 건드리는 작업을 두 번 하지 않기 위함.
- **현황 (2026-07-29): ✅ 통일 완료.** 프로필·활성 선택·온보딩 3개 키를 `aiday:`로 옮겼다(`lib/storage-keys.ts`).
- **왜 두 번이나 미뤄졌는지 — 진단 정정:** 문제는 이름이 아니라 **출처**였다. `activeProfileId`가 화면 14곳에 문자열 리터럴로 흩어져 있어서, 새 화면(하루·리뷰)이 기존 화면을 복사할 때 구 접두어가 따라왔다. 그래서 이번에는 키 개명보다 **단일 출처화 + 린트 강제**를 본체로 삼았다: eslint `no-restricted-syntax`가 `app/**`·`components/**`에서 ① `aiweather:` 리터럴 ② 호출 자리에 키 문자열을 적는 `localStorage.getItem("…")`을 막는다. 규칙을 문서에만 두면 또 번진다(§3-4가 2026-07-24·07-29 두 번 "부분 완료"로 기록된 이유).
- **마이그레이션 방식:** 읽기 시점 자기치유(신키 없고 구키 있으면 그때 복사). `useEffect`(provider)에서 옮기면 React가 자식 effect를 먼저 실행해 페이지가 이미 구키를 읽고 지나간 뒤라 첫 렌더가 빈 프로필로 뜬다. 검증: `lib/storage-keys.test.ts`(12건) + `tests/e2e/11-storage-migration.spec.ts`(TC-STORE-01·02).
- **남은 일 — 다음 릴리스:** 지금은 신키·구키에 **함께 쓴다**(미러). 배포 직후 bfcache에 남은 구버전 번들 탭이 구키에 쓴 변경을 잃지 않기 위한 안전장치이며, 다음 릴리스에서 `LEGACY` 맵·미러 로직을 지우고 구키를 삭제한다. 게스트는 로컬이 유일한 정본이라 이 안전장치의 실질 수혜자다(로그인 사용자는 DB가 정본).
- **범위 밖으로 남긴 것:** `sessionStorage` 키(`aiday:perf:navToHome` 등)와 화면별 로컬 상수(`aiday:probe:*`·캐시 키 빌더)는 이원화 대상이 아니라 그대로 뒀다. 다만 `aiday:perf`처럼 파일 두 곳에 리터럴로 있던 것은 단일 출처(`lib/perf.ts: perfEnabled()`)로 모았다.

### 3-5. 앱 형태 — **단계적 전환: PWA(P0) → 래퍼앱(P1) → 네이티브 재개발(Post-MVP)** *(2026-07-07 신설)*

- 최종 형태는 애플리케이션을 지향하되, 단계적으로 전환한다:
  1. **PWA (P0, 6주 내):** manifest + Service Worker — 설치 아이콘 + 웹 푸시. 외부 승인 없이 자급 구현 가능
  2. **래퍼앱 (P1):** WebView/Capacitor로 기존 웹앱을 감싸 앱스토어·플레이스토어 배포 + **네이티브 푸시(FCM/APNs)** — iOS 웹 푸시의 설치 제약을 근본 해소. 개발자 계정·스토어 심사 리드타임 때문에 6주 P0에서 제외
  3. **네이티브 재개발 (Post-MVP):** React Native/Flutter 등 — 공개 베타 검증(북극성 지표) 후 별도 결정
- 6주 내 iOS 도달 공백은 래퍼앱이 아니라 **카카오톡 "나에게 보내기" 채널(§3-3)**로 보완한다.

### 3-6. 연령 타깃 — **만 1~8세 전체 유지 + 연령군별 규칙·검증 분리** *(2026-07-12 신설 — PRD v2.7.2)*

- 핵심 타깃은 **만 1~8세 자녀를 둔 맞벌이 부모**다. 대표 페르소나(만 4세)는 사용 장면 구체화용 사례일 뿐 타깃을 제한하지 않는다.
- **v2.4의 "베타 모집 만 3~6세 우선" 결정을 폐기한다.** 판단 조건이 정형화된 연령대만 먼저 모집하면 전체 타깃이 사실상 축소되고, 다른 연령군의 정확도·수요를 검증하지 못한 채 확산 판단을 내리게 된다.
- **연령군별 규칙 세트 분리는 정확도를 위한 설계 결정이다.** 만 1세(유모차·체온조절)와 만 7세(도보·체육·자가 조절)는 판단 논리가 다르므로, 하나의 평균 규칙은 세 연령대 모두에게 어중간한 리포트를 낸다. 안전·예측 규칙과 액션 카탈로그를 **연령군(만 1~2·3~6·7~8) 키로 분리**하고, 규칙마다 적용 연령군·근거·버전을 기록한다.
- **검증은 전체 타깃을 축소하지 않고 표본만 연령층별로 나눈다.** 첫 7일 컨시어지 테스트 목표: 만 1~2세 3~4가구 · 만 3~6세 6~7가구 · 만 7~8세 3~4가구(총 12~15가구, 모집 상황에 따라 조정).
- **얇은 연령군은 배제하지 않고 낮은 신뢰도로 표시한다** — 성숙도를 정직하게 드러내되 타깃에서 빼지 않는다.
- **별도 To-Do:** 상한 만 8세 근거인 육아휴직 제도 연령 경계는 서비스 수요의 직접 인과로 단정하지 않는다 — 최신 법령 확인 + 만 7~8세 부모 사용자 조사를 별도 수행한다.
- 상세: [PRD.md](../PRD.md) §04(연령별 판단 규칙)·§05(입력 축·골든 케이스)·§08(연령군별 규칙 세트).

### 3-7. 첫 유료 상품 — **환절기 케어 패스 월 990원, 결제 없는 사전예약으로 먼저 측정** *(2026-07-29 신설)*

- 첫 유료 상품은 **환절기 케어 패스 월 990원 구독**(2026년 9월 시작)으로 한다. 시장선별 v2의 확정안(8주 9,900원 / 월 4,900~6,900원)보다 **낮은 진입 가격**이며, 이는 매출 최적화가 아니라 **결제 전환 자체를 먼저 측정**하려는 선택이다.
- 가구당 LLM 원가는 월 300~500원이므로 990원의 매출총이익률은 확정안보다 낮다. **가격 상향은 전환율을 확인한 뒤 판단**한다 — 가격을 먼저 올려 전환이 안 나오면 "가격 탓인지 가치 탓인지"를 가를 수 없다.
- **지금 결제를 받지 않는다.** 화면에 "출시 시 안내를 받아보는 예약"임을 명시하고, 수집은 기존 `feedback` 채널 재사용(신규 테이블 0). 측정은 `feedback`에서 `props->>'product'` 카운트. **결제 연동 전까지 "사전예약" 표기를 바꾸지 않는다** — 의향과 결제를 같은 숫자로 읽지 않기 위함.
- **진입은 마이 > 계정 첫 행.** 홈 배너에서 옮겼다(2026-07-29) — 홈은 아침 판단 화면이고, 거기에 상시 판매 배너를 두면 아침 표면이 판매 표면이 된다(MANIFESTO 안티패턴).
- 구현: SPEC [P-10](../SPEC.md), `app/(main)/pass/page.tsx`.

### 3-8. 하루 루프·알림장 최소 구현 — **측정 쐐기로 착수, 게이트 미달 시 제거** *(2026-07-29 신설)*

- 하루 탭(저녁 회수·이번 주 컨디션 예보·돌봄 카드)과 알림장 루프(아침 전달 문구 + 저녁 대화 거리)는 **기능 완성이 목적이 아니라 행동률 측정이 목적**이다. 수요 증거가 창업자 n=1인 상태에서 풀 루프를 짓지 않기 위해, 가장 얇은 형태로 켜고 실제로 쓰는지 본다.
- **게이트 — 동의 베타 기간 판정:**
  - **알림장 루프:** 주 3회 이상 붙여넣기하는 사용자 ≥ 30% → 풀 루프(준비물·행정 판단 통합, MANIFESTO Post-MVP) 착수. **미달이면 카드를 제거한다** — 다듬지 않는다.
  - **저녁 회수:** 로컬 회수의 실제 사용률을 보고 서버 영속화(`daily_reflections`)·프롬프트 실연동 착수 여부를 결정한다. **쓰지 않는 데이터를 위해 테이블부터 만들지 않는다.**
- 측정 수단: `noteboard_shown`(분모)·`noteboard_submitted`(핵심 행동)·`generated`·`error`·`shared` + `morning_message_copied` (`lib/analytics.ts` + `events` CHECK 화이트리스트 확장 마이그레이션). 저녁 회수(리뷰) 계측은 아직 없다 — P1.
- **§1 성공 지표 3종은 그대로 둔다.** 이 게이트는 기능 존치 판정용이며, 북극성 지표(아침 재방문율)를 대체하지 않는다 — 저녁 회수가 아침 판단을 대체하는 순간 제품이 기록 앱이 되기 때문이다(MANIFESTO §3).
- 구현: SPEC [P-09](../SPEC.md), CHANGELOG `[Unreleased]`.

---

## 변경 이력

| 날짜 | 내용 |
|------|------|
| 2026-07-04 | 최초 작성 — 성공 지표 3종, 출시 기준(Blocker 6 / Fast-follow 5), 보류 결정 4건 확정 |
| 2026-07-07 | §3-3 알림 1차 채널 변경: 이메일 → 웹 푸시(PWA). PWA 선행 작업을 6주 고도화 PRD(prd.md) P0에 편성하며 제약 해소. 카카오 "나에게 보내기"는 P1, 알림톡은 지표 2 검증 후 유지 |
| 2026-07-07 | 알림 발송 1종의 종류 변경: 전날 밤 → 당일 아침 브리핑. 근거: FGI 수요 1위(76.5%) + 문제정의 프레임워크 v2("기상 시점에 판단이 완료되어 있어야 함"). 전날 밤은 P1로 이동 |
| 2026-07-07 | 알림 P0 전면 확장: 전날 밤 알림·카카오톡 "나에게 보내기" 모두 P0 승격 (§3-3 갱신). 근거: 선제 알림은 서비스 본질의 전달 수단 + iOS 웹 푸시 도달 제약 보완. 일정 컨틴전시(카톡 지연 시 fast-follow 이월) 명시 |
| 2026-07-07 | §3-5 신설 — 앱 형태 단계적 전환 확정: PWA(P0) → 래퍼앱(P1) → 네이티브 재개발(Post-MVP) |
| 2026-07-12 | §3-3 카카오 채널 착수 게이트 추가: 무조건 P0 구현 → 4주차 알림 퍼널 병목 실측 시에만 착수(주 채널 1개 + 폴백 원칙, 중복 발송 폐기). 근거: PRD v2.4 외부 검토 반영 — 구현 공수 재평가·알림 피로 방지 |
| 2026-07-12 | §3-6 신설 — 연령 타깃 결정: **v2.4 "만 3~6세 우선 모집" 폐기, 만 1~8세 전체 유지 + 연령군(1~2·3~6·7~8)별 규칙 세트·검증 분리**(정확도 설계). 검증 표본만 연령층별로 나눔(총 12~15가구). 근거: PRD v2.7.2 — 전체 타깃 축소 방지 + 연령별 판단 논리 차이로 인한 정확도 |
| 2026-07-13 | 이메일 인증 정책 확정: **최종 상태는 인증 활성(B) + 커스텀 SMTP**, 지인 테스트 기간만 임시 비활성. 근거: 이메일·Google 로그인 공존 시 미인증 가입이 identity linking 계정 탈취 경로가 됨 + 아동 건강정보(민감정보) 취급. 재활성화를 출시 Blocker에 추가 |
| 2026-07-19 | §1 측정 수단 변경: PostHog 선행 → **Supabase 자체 계측**(events·feedback + beta_* 뷰, docs/beta-metrics.md). 지표 1 정의를 온보딩 5단계로 현행화, 베타 한정 보조 진단(리포트 유용성 👍 비율) 추가 — 3지표 체계는 유지. 근거: 베타 규모(12~15가구)에 외부 분석 도구는 과함 + 계측·피드백 UI 구현 완료 |
| 2026-07-20 | **케어 플랜 준비물 칩 엔진 = 규칙 엔진(lib/prep.ts) 유지 확정, `?prep=ai` 실험 은퇴.** 홈 준비물은 표면별로 다른 엔진이 채우는데(체크리스트·hook·message = AI, 케어 플랜 칩 = 규칙), 칩만 규칙으로 두는 것을 데이터로 확정. 근거: rule vs AI 칩 A/B(scripts/chip-ab.test.ts, 48슬롯) — 커버리지 갭 규칙 0 vs AI 10(AI는 message에서 짚은 슬롯만 채워 뒷시간대 지속 위험이 빈칸), 결정성 규칙 0 vs AI 7슬롯 흔들림. 칩은 매 슬롯 빠짐없이·흔들림 없이 보여야 하는 표면이라 규칙이 적합. AI의 풍부한 뉘앙스는 message·checklist(이미 AI 기본)에서 살린다. 원자료 docs/report-eval/chip-ab.md |
| 2026-07-22 | §2 출시 차단에 **"건강팁(tips) 목데이터 제거 + 정직한 출처 표기(M2)"** 추가. 근거: `/plan-eng-review`+`/plan-ceo-review` — tips가 기존 블로커 "홈 '시간대별 환경'·종합솔루션 실데이터 연동"과 동일한 위반(실측/근거 표기 + 목데이터 병존)이라 일관성상 같은 취급. 출처 딥링크 강화(M3)·검증 자동화(M4)·개인화 확대는 도메인전문가+법률 검토 게이팅으로 이 블로커 범위 밖 유지 |
| 2026-07-29 | **§3-4 완료 — localStorage 네임스페이스 `aiday:` 통일 + 재발 방지 린트.** 두 번 미뤄진 원인을 "이름 문제"에서 **"출처 문제"**로 재진단해(키가 화면 14곳에 리터럴로 흩어져 새 화면이 복사) `lib/storage-keys.ts` 단일 출처 + eslint 금지 규칙 2종을 본체로 삼았다. 마이그레이션은 읽기 시점 자기치유(provider effect는 자식 effect보다 늦게 돌아 첫 렌더가 빈 프로필이 됨). 구키는 이번엔 미러로 남기고 다음 릴리스에서 삭제 — bfcache 잔존 구번들 탭 보호 |
| 2026-07-29 | **§3-7 신설 — 첫 유료 상품: 환절기 케어 패스 월 990원, 결제 없는 사전예약으로 먼저 측정.** 시장선별 v2 확정안(8주 9,900원 / 월 4,900~6,900원)보다 낮은 진입 가격을 택한 이유는 매출이 아니라 전환 측정이며, 가격 상향은 전환율 확인 후 판단. 진입을 홈 배너 → 마이 > 계정 첫 행으로 이동(홈은 아침 판단 화면) |
| 2026-07-29 | **§3-8 신설 — 하루 루프·알림장 최소 구현은 측정 쐐기이며 게이트 미달 시 제거.** 알림장은 주 3회+ 붙여넣기 ≥30%가 풀 루프 착수 게이트, 미달 시 카드 제거. 저녁 회수는 로컬 사용률을 보고 서버 영속화·프롬프트 실연동을 판단(쓰지 않는 데이터를 위해 테이블부터 만들지 않는다). **성공 지표 3종·북극성(아침 재방문율)은 불변** — 저녁 회수가 아침 판단을 대체하면 기록 앱이 된다. 근거: 수요 증거가 창업자 n=1인 상태에서 풀 루프 선건설 회피 |
| 2026-07-24 | **코드베이스 대조로 §2 출시 차단 현황 동기화 (PRD v2.8 동기화 연동).** 코드로 완료 확인된 블로커 4건을 `[x]`로 갱신하고 각 근거(파일·커밋)를 명시: ① 프로필 값 포맷 정규화(`profile-options.ts` normalize*·`child-conditions` 매칭, 실프로필 마스크 추천 작동) ② 홈 시간대별·종합솔루션 실데이터(`hourlyForecast` 연동·`weather-mock` 미사용·종합솔루션 제거) ③ 약관·처리방침 실문서(`app/terms`·`app/privacy`) ④ 건강팁 M2(`tips/select.ts` fetchEnvData·severity·매칭·fail-closed + `content.ts` 출처 문서명+연도, 커밋 #158). ⑤ **홈 "추천 아이템"은 숨김이 아니라 홈에서 제거됨**으로 확정(`item-recommend`는 outfit 전용) — PRD S-001의 "더미 카드 유지" 서술과 상충하므로 **코드=제거를 정답으로** 확정, PRD 후속 정정 필요. §3-4 localStorage `aiday:` 통일은 **부분 완료**(온보딩 잔여)로 현행화. 이메일 인증 재활성화는 운영 설정이라 코드 확인 대상 아님 → 미체크 유지 |
