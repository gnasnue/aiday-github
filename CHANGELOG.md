# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/).

## [Unreleased]

### Added
- **`/api/report` 레이트리밋 — 출시 차단 항목 해소** (`lib/rate-limit.ts`, `app/api/report/route.ts`, `supabase/migrations/20260722021500_report_rate_limit.sql`) — 인증 없이 열려 있어 호출당 Claude 비용이 그대로 노출되던 공개 엔드포인트에 하루 상한을 적용. 결정 3-1대로 게스트 호출은 계속 허용하되(둘러보기 = 핵심 가치 시연) **게스트는 IP 기준 일 10회, 로그인 사용자는 `user_id` 기준 일 20회**로 나눈다 — 사무실·공용 와이파이(CGNAT)에서 여러 가구가 같은 IP를 쓸 때 서로의 한도를 잡아먹는 오탐 방지. 카운터는 Supabase `report_usage`에 두어 서버리스 인스턴스가 늘어나도 한도가 정확히 지켜지고, 증가·판정을 단일 RPC(`bump_report_usage`)로 묶어 왕복 1회만 든다. IP 원문은 저장하지 않고 솔트 해시만 버킷 키로 쓴다. 판정 불가 상황(설정 누락·DB 오류)에서는 통과시키되 프로덕션 설정 누락은 매 호출 error 로그를 남긴다. 한도 소진 시 429 + 전용 안내 문구(홈 토스트가 "잠시 후 다시"라는 거짓 안내를 하지 않도록 서버 문구를 그대로 노출).
- **꽃가루 잡초류 연동** (`app/api/pollen/route.ts`) — "기상청 V3에 잡초 오퍼레이션 없음"이라는 종전 주석·문서 서술은 사실이 아니었다. 공식 명세(공공데이터포털 15085289 첨부 설명서)와 실호출 모두 `getWeedsPollenRiskndxV3`의 존재를 확인해(철자 주의 — 잡초류만 `Riskndx`, 참나무·소나무는 `RiskIdx`) 참나무·소나무와 함께 3종을 병렬 조회하도록 변경. 잡초류 자료제공 기간이 8~10월이라 8월부터 실제 값이 들어온다. 응답 스키마(`oak/pine/weed`)는 그대로라 홈 칩·env 행·야외활동 지수·AI 리포트는 변경 없이 값을 받는다.
- **환경 데이터 공유 페처** (`lib/env-data.ts`) — 홈·환경정보·옷차림이 같은 4개 API(기상청·에어코리아·꽃가루·자외선)를 서로 다른 3가지 방식으로 호출해 위치 파라미터·타임아웃·결측 처리가 화면마다 갈라져 있던 것을 단일 창구로 통합. 위치→파라미터 매핑(`envRegion`), 소스별 타임아웃·재시도, 기상청 결측 센티널(-998/-999) 범위 검증을 한 곳에서 책임진다. 개별 소스 실패는 전체를 실패시키지 않고 부분 결과를 돌려주며, 쓸 수 없는 신호는 `missing`으로 알린다(응답은 왔지만 핵심 값이 결측인 경우 포함). 환경정보 화면을 첫 소비자로 이전했고 홈·옷차림은 후속 과제.
- **건강팁 안심 안내** — 조건에 해당하는 팁이 없을 때 "오늘 자외선·대기질·습도는 주의 수준이 아니에요"로 무엇을 확인했는지 밝힌다. 결측 신호는 확인 자체를 못 한 것이므로 이 목록에서 제외한다.
- **CI 워크플로 — PR마다 lint·test 자동 게이트** (`.github/workflows/ci.yml` 신설) — 지금까지 `npm run lint`·`npm test`(vitest 241개)를 사람이 기억해서 돌려야 했고 머지 전 기계적 강제가 없었다(홈이 "코드 수정 후" 깨지는 회귀의 구조 원인). GitHub Actions로 PR·main push마다 lint+test를 강제해 회귀를 머지 전에 차단한다. 빌드 게이트는 `/signup` 프리렌더가 Supabase 공개 env를 요구해(repo 밖 `.env.local` 의존) secrets 설정 후 활성화하도록 주석·안내로 남겼다. (2026-07-23 홈 harness 엔지니어링 리뷰 마일스톤 A)
- **weather 라우트 502 판정 순수 함수 추출 + #154 라우트 계약 회귀 테스트** (`lib/weather-response.ts` 신설, `lib/weather-response.test.ts` 신설, `app/api/weather/route.ts`) — #154 수정에서 파서 절반(`extractFcstItems` NO_DATA→빈 배열)은 테스트됐으나, "현재값·예보가 모두 없으면 502·부분 성공은 200"이라는 **라우트 레벨 판정**은 무검증이었다. 그 결정을 `isWeatherUnavailable`로 분리해 5개 계약 케이스(전면 실패→502, 부분 성공→200, 0°C는 결측 아님)로 핀한다. 라우트는 이 함수를 호출하도록 배선 — 동작 불변, DRY.
- **건강팁 기온 기반 폭염·한파 신호** (`lib/timeline.ts`, `lib/tips/select.ts`, `lib/tips/content.ts`, `app/(main)/tips/page.tsx`) — tips가 자외선·미세먼지·꽃가루·건조만 다루고 여름 최대 관심사인 기온(폭염·한파)엔 팁이 없던 공백 해소. 판정은 기온이 아니라 습도·바람이 얹힌 **체감온도**로 하고(홈 카드와 같은 `feelsLikeC`), `heatLabel`/`coldLabel`(4단계)을 레벨 단일 출처로 추가. 폭염은 하루 최고, 한파는 하루 최저 체감을 피크로 본다. 계절 밖 신호는 아예 평가하지 않는 `activeMonths` 게이트(7월엔 한파 미평가 — "한파 아님" 안내조차 안 띄움)와, 미검증 초안을 dev에서만 렌더하는 `draft` 게이트를 신설. 팁 2건의 출처는 질병관리청 국가건강정보포털 「기후변화에 의한 폭염」·「겨울철 한파대비 건강수칙」으로, 2026-07-24 원문을 대조해 권고가 원문과 일치함을 확인했다.
- **건강팁 감염병(유행병) 팁 유형 — Layer 1(런타임 모델)** (`lib/tips/content.ts`, `lib/tips/select.ts`, `app/(main)/tips/page.tsx`) — 환경(자외선·기온 등)이 아니라 역학 감시가 트리거인 감염병 팁 유형을 추가. `감염병` 카테고리(`requires:null`)와 유행 만료 게이트 `activeUntil`("YYYY-MM-DD")을 신설 — KST 만료일 당일 23:59까지 노출되고 익일 만료된다. 결측·형식오류·가짜 날짜(2026-02-30 등)는 **만료로 간주해 숨긴다(fail-closed)**. 감염병 팁은 환경 신호가 아니라 안심·결측 배너를 만들지 않는다 — 폴링하지 않으므로 "유행 없음"을 암시할 자격이 없기 때문. 실제 팁 작성은 사람이 KDCA 유행 보도자료를 검증해 넣으며(자동 수집은 별도 스코프로 보류), 예시 항목은 `draft`로 dev에서만 렌더된다. 무결성 테스트로 감염병 항목의 `activeUntil` 존재·실제 달력 날짜·질병관리청 계열 출처를 강제. `/plan-eng-review` 검토 후 구현(계획: `docs/01-plan/features/infectious-disease-tips.plan.md`).
- **홈 env 즉시 페인트 스냅샷 캐시** (`lib/env-cache.ts` 신설, `app/(main)/home/page.tsx`) — 콜드 재진입 시 시간대별 환경·케어 플랜·"지금 날씨" 카드가 weather+air 네트워크(콜드 ~5s)를 기다리며 스켈레톤에 갇히던 문제를 없애는 클라 SWR 캐시. 마지막 성공 env 응답을 위치별로 저장해 두고 마운트 즉시 그 값으로 그린 뒤 백그라운드로 조용히 재검증한다. 스테일 상한 90분(케어 밴드와 정렬)을 넘거나 위치가 다르면 즉시 페인트하지 않고 스켈레톤으로 둔다 — 오래된 값을 실측인 척 그리지 않는 정확도 우선 원칙. 판정은 순수 함수 `isEnvSnapshotFresh`로 분리해 경계·타 위치·미래 ts 방어를 유닛 테스트(`lib/env-cache.test.ts`, 9케이스)로 핀한다.
- **아침 프리워밍 크론** (`app/api/cron/warm-env/route.ts` 신설, `vercel.json`, `.env.example`) — 부모가 아침에 처음 여는 순간의 콜드 지연을 서버가 대신 지불한다. weather/air/uv/pollen 라우트의 상류 fetch 캐시(`next.revalidate`)가 밤새 만료돼 아침 첫 사용자가 콜드 공공 API를 그대로 기다리던 구조를, 매일 06:00 KST(Vercel Cron) 프리워밍으로 데워 둔다. 기본 기준지(서울 중구)+서울 uv/pollen을 미리 호출해 첫 진입의 재검증이 데워진 캐시로 빠르게 끝난다. `CRON_SECRET`이 설정되면 `Authorization: Bearer`로 공개 남용을 막는다. Hobby 크론 1일 1회 제한에 맞춰 아침 1회(Pro면 러시 내내 늘릴 수 있음).
- **브리핑 카드 준비물 총량 한정 — 결정적 검사 추가 + 프롬프트 조임 (결함은 미해소, 근본 원인은 체크리스트 하한)** (`scripts/eval-report.mjs`, `lib/prompts/report.ts`) — "야외활동 전엔 **물통**만 챙겨 보내주세요"를 읽은 부모가 바로 아래 체크리스트에서 물통·여벌 옷·모자 3개를 본다. 카드가 스스로를 반박하는 결함으로, 기존 R4(표면 간 교차 정합)가 `hook ⊆ checklist`·`prep ⊆ checklist`를 강제해온 것과 같은 계열인데 **message·hook ↔ checklist 축만 비어 있었다**. **추가한 것:** eval에 결정적 검사 `준비물 총량 한정 없음`. 검사 범위는 hook + message(부모가 카드에서 함께 읽는 텍스트)이고, 한정 조사("물통만", "여벌 옷 한 벌 정도만")와 충분 단정("겉옷 하나면 끝", "반팔 한 장이면 충분")을 문장 단위로 본다. 정상 통과: 위치·대상 조사("우산은 등원 가방에만" = 시점 한정), 같은 문장에 다른 체크리스트 아이템이 함께 있는 경우("아침엔 우산만 챙기고, 마스크는 하원용으로" = v25가 노리는 시점 교차 문장). **프롬프트:** 총량 한정 금지를 표면 간 교차 규칙으로 두고 대체 표현을 준다(중요한 하나는 강조로, 덜어낼 땐 추가분 부재로), 준비물 이명 규칙을 네 표면 전체로 강화, few-shot 예시 6의 hook `"얇은 겉옷 하나면 끝이에요"` 교체 — **이 예시가 위반을 직접 가르치고 있었다**(자기 검사에 걸려 발견). **미해소:** 프롬프트로는 닫히지 않는다. 검사를 강화해 재채점한 결과 16 시나리오당 모순은 v25 이전 2건 → v25 3건 → 프롬프트 조임 4회 반복 후에도 2~3건으로, **유의미하게 줄지 않았다**(종전 CHANGELOG가 "1건→3건→0건"으로 적은 것은 약한 검사가 놓친 측정 오류였다). 재발은 무난한 날(S03·E-AHA-4)에 집중된다 — 오늘 정말 필요한 게 없는 날 "체크리스트 최소 3개" 하한과 정직한 문장이 충돌하는 구조 문제다. **근본 수정은 하한을 어떻게 볼지에 달려 있다:** 체크리스트를 "오늘의 판단"으로 보면 무난한 날은 0~1개여야 하고(Codex 리뷰 T1), "매일의 준비물 목록(물통·여벌 옷은 날씨와 무관한 기본)"으로 보면 3개가 맞고 message가 한정하지 않아야 한다. 제품 결정 사안으로 남긴다. Codex 엔지니어링 리뷰 T1 + 후속 재지적(검사가 hook을 보지 않고 동사 목록에 의존해 "물통만 채워"·"넉넉히 챙겨"를 놓친다)을 반영.

### Changed
- **브리핑 시점 단정 금지 — 입력이 뒷받침하지 않는 시각 확정 제거 (프롬프트 v27)** (`lib/prompts/report.ts`, `app/(main)/home/page.tsx` 캐시 키 v27, `scripts/eval-report.mjs`) — 꽃가루는 기상청 API가 일 단위라 "오늘 최고 등급" 하나만, 대기질은 에어코리아 실시간 측정값만 프롬프트에 들어간다(`app/api/report/route.ts` `pollenSummary`·`airSummary`). 즉 **이 지표들에는 시간에 따라 어떻게 변하는지가 입력에 아예 없다.** 그런데 출력은 "비가 그친 뒤 낮 동안 마르면서 꽃가루가 다시 날리는데 … 하원(17:00)길이 조심 구간"처럼 특정 시각의 재비산을 사실로 확정했다 — 모델 환각이 아니라 **제품 규칙(v25가 허용한 메커니즘 목록)이 입력보다 강한 결론을 내리는** 문제다(Codex 엔지니어링 리뷰 2026-07-27 지적). 메커니즘 추론 자체는 판단의 값이므로 그대로 두고 **서술의 확정도만** 낮췄다: ① 시스템 프롬프트 금지 사항에 "시점 단정" 1줄 추가 — 데이터 형태(일 단위/현재값)를 밝히고 가능성 보존형을 요구("그친 뒤 오후에 다시 날려요"❌ → "그친 뒤 오후엔 다시 날리기 쉬워요"⭕), 기온·강수·자외선은 시간대별 입력이 있으므로 단정 허용을 명시 ② **같은 위반을 시연하던 few-shot 예시 9를 가능성 보존형으로 교체** — 예시 문구는 출력으로 그대로 복사되므로 규칙만 고치면 안 된다(v21 "이번 주 최고" 누출과 같은 계열) ③ eval에 시간대 입력이 없는 지표에 변화 서술이 단정형으로 붙으면 FAIL하는 자동 체크 추가(절 단위 판정 — 뒷절의 무관한 완화 표현이 앞절 단정을 가리지 못하게). 검사식은 **누적 산출물 358건에 소급 적용해** 교정했다 — 적발 6건은 전부 v25 E-AHA-1의 실제 단정이고 오탐 0(중간에 실제 출력 "다시 날리기 쉬워,"를 오탐으로 잡은 케이스가 있어 완화 표현을 어미 열거에서 구문 매칭 `-기 쉽다`로 바꿨다). **v25는 E-AHA-1 11회 중 6회(55%)가 단정형**이었고, **v26으로 생성한 49건(16 시나리오 2회 + 꽃가루 케이스 반복 7회)은 전부 PASS**(`docs/report-eval/timeassert-*`) — 메커니즘·시점 교차 판단은 유지된 채(우산은 등원, 마스크는 하원) 확정 표현만 사라진 것을 문장으로 확인했다. 결론마다 입력 출처를 추적하는 구조적 해결(서버가 `CareFact[]` + `evidenceIds` 제공, 리뷰 T5·T8)은 V2 스키마 범위로 남겨둔다 — Zod는 형식만 검증하므로 사실성 해결책이 아니다. 브리핑 문장 형태가 바뀌므로 **캐시 키 v25→v27**로 구캐시 무효화 — v26은 PR #174(캐시 정합성·profileSig)가 선점했으므로 같은 번호를 쓰면 그 PR이 먼저 머지될 때 키가 변하지 않아 무효화가 일어나지 않는다.
- **브리핑 판단 깊이 개편 — "뻔한 소리"에서 "비서의 판단"으로 (프롬프트 v25)** (`lib/prompts/report.ts`, `app/(main)/home/page.tsx` 캐시 키 v25, `app/api/report/route.ts` 일과 미입력 헤더 1줄, `scripts/eval-report.mjs`) — CEO 리뷰에서 히어로 근거·'자세히' 본문이 "부모가 날씨 앱 숫자만 봐도 스스로 내릴 결론"(일교차 10도→가디건 류) 수준으로 판정된 문제. **구조적 원인은 few-shot**: 8개 예시 전부가 `1순위 지표 → 표준 처방 + 체질 수식어`의 1단계 추론만 시연해 모델이 그 천장을 넘을 수 없었다. ① 시스템 프롬프트에 존재 이유("숫자 하나만 본 부모가 놓치는 것 — 지표의 상호작용·시점의 교차·이 아이라서 달라지는 처방 — 을 대신 계산") + 뻔함 금지 + 환각 가드 강화(시설 정보·지역 특성·통계 인용 금지, 근거는 생활 상식 메커니즘까지만, 건강 결과 단정 금지 — aha 압박은 환각 압박이므로) ② 판단 순서에 **5단계 "뻔함 테스트"**(지표 상호작용 / 등급 전환 시각×일정 교차 / 처방을 바꾸는 개인화 / 되돌림 비대칭 — 옷 기준은 아이가 실제 바깥에 서는 시각의 기온)와 **7단계 출력 전 자가 검증** 삽입 ③ message를 **3문장 역할 구조**(문장1 판단 / 문장2 이 아이의 근거 — 이름은 여기에만, 홈 `supportLine`(이름 든 첫 줄 발췌)과의 화면 계약 / 문장3 실행)로 교체 ④ **실행 디테일 사전** 신설(겉옷은 단추보다 지퍼 / 땀이 원인인 날 여벌은 상의만 / 꽃가루 날 겉옷은 현관에서 벗기기 / 선크림은 나서기 전(흡수 시간) / 알림장 한 줄 위임) ⑤ **few-shot 5개 교체 + 1개 신규(총 9개)**: 노출 전 차단 타이밍·되돌림 비대칭·젖은 옷 체인·덜어내는 판단·마스크→통풍 처방 전환·비 그친 뒤 꽃가루 재비산(우산은 등원, 마스크는 하원). 반복 누출은 프롬프트 조임과 함께 **입력에서 제거**(v21 원칙): 일과 미입력 헤더의 "등원·하원 시각을 알 수 없음" 문구가 문장2 근거 압력에 그대로 되읽혀 라벨 없는 날 등원·하원을 발화시키던 것을 헤더에서 라벨 단어 제거로 해소. eval은 **E-AHA 판단 깊이 케이스 4종**(message+checklist 키워드군 AND 판정: 시점 교차/상호작용/판단 원리/억지 aha 금지 음성 대조)과 **message 3줄·이름 2번째 줄 구조 체크**(전 시나리오)를 추가하고, 기존 체크 3건의 어휘 갭을 정정(40~59% "비 소식" 조건절 인정 — 우산 절제 규칙이 수치를 금지하므로 / "마스크 씌우기 어려운 나이" 부정 설명 오탐 제외 / METRIC에 통합대기 추가). **16 시나리오 × 8회 실호출 반복**으로 새 프롬프트가 만든 누출 모드(대조형 안심문장·준비물 이명·허가형 말투·사전 문구의 "등원" 주말 복사)를 하나씩 잡아 **2회 연속 전 항목 PASS**(`docs/report-eval/aha-baseline` → `aha-final3`·`aha-confirm` 대조). 브리핑 문장 형태가 바뀌므로 캐시 키 v24→v25로 구캐시 무효화.
- **AI hook 계약 개정 — 히어로 2절 렌더에 맞춤** (`lib/prompts/report.ts`, `app/(main)/home/page.tsx` 캐시 키 v24, `scripts/eval-report.mjs`) — hook을 `25자 1절`에서 **`40자 "[조건 8~15자] — [행동 15~24자]"`**로 늘렸다. 홈 히어로가 앞 절은 배지(13/600), 뒤 절은 결론(28/800)으로 나눠 렌더하는데, 종전 계약에서는 행동절에 **6~11자**만 남아 28px 대형 타입이 8자를 담당하고 "우산 챙겨요"처럼 뭉툭한 결론이 나왔다. 규칙 2줄을 함께 넣었다 — ① 앞 절은 **1순위 지표명 + 등급·수치**("일교차 10도"를 "아침 16도"로 바꿔 부르지 않기, 자외선만 수치 없이 등급, 주의 지표가 없는 날만 "모처럼 무난한 날"류 허용) ② 뒤 절은 **준비물·행동 목적어를 하나 이상 포함한 완결 청유문**(뒤 절만 읽어도 할 일이 성립해야 한다). few-shot 9개의 hook도 새 길이로 재작성했다 — 모델은 지시보다 예시를 모방하므로 예시를 고치지 않으면 규칙이 먹지 않는다. **eval 3회 대조**(`docs/report-eval/hook-before·hook-after·hook-after2`, 12 시나리오 × 실호출): 행동절 평균 **7.9 → 16.5자**, 히어로 2줄 렌더 **0/12 → 7/12**, 키프레이즈 하이라이트 밴드 **7/12 → 12/12**, 지표성 조건절 **10/12 → 11/12**. 품질 규칙(질병명·안심문장·좋음·보통 등급 미언급·자외선 수치·마스크 연령·주말 등원·`prep ⊆ checklist`)은 3회 모두 통과. hook 형태가 바뀌므로 **캐시 키를 v23 → v24로 올려** 구형 캐시(짧은 hook)를 무효화한다. eval 자동 체크도 새 계약으로 갱신하고 `hook 2절 구조`·`① 조건절 = 지표 + 등급·수치`·`② 행동절 ≥ 12자` 3건을 추가해 회귀를 고정했다.
- **홈 화면 재설계 — AI 판단 Decision Brief** (`components/HeroDecisionBrief.tsx`·`components/PrepChecklistCard.tsx`·`components/PrepIcon.tsx`·`lib/hero-brief.ts` 신설, `app/(main)/home/page.tsx`, `app/globals.css`, `tailwind.config.ts`) — 히어로가 "정보 카드"가 아니라 "판단이 완료된 브리프"로 읽히게 구조를 바꿨다. 요소 순서가 곧 논증이다: **조건(pill) → 결론(28/800) → 아이 특성 근거 → 판단 근거 칩 2~3개**. 데이터를 맨 앞에 놓는 날씨 앱과 반대 순서이며, 수치가 결론보다 시각적으로 강해지지 않는다. 카드 상단의 피치 masthead 밴드를 없애고(화면에서 가장 넓은 유채색 면을 행동 정보가 0인 chrome에 쓰고 있었다) 아이 이름·발행 시각·새로고침·공유를 페이지 헤더로 올렸다. 체크리스트는 히어로 밖 L1 카드로 분리했고(카드 안 카드 제거), 준비물 강조 타일은 **헤드라인이 지시한 준비물 1개**만 — 결론과 실행이 같은 단어로 이어진다. 히어로가 608.6px → 약 280px로 줄어 첫 화면에 체크리스트 헤더와 첫 준비물 행이 들어온다. AI 프롬프트·캐시 스키마는 **변경하지 않았다**: hook의 `"[공감] — [행동]"` 구조를 `toBrief()`로 갈라 조건절은 pill, 행동절은 결론으로 쓴다. 파생 규칙(강조 구간 매칭·근거 칩 선정·상태 판정·준비물 사유)은 `lib/hero-brief.ts`로 분리해 유닛 테스트 28건으로 고정했다. 시안·명세: `docs/reviews/2026-07-25-home-decision-brief-design.html`, 제작 지시서: `…-figma-handoff.md`, 동작 프로토타입: `…-prototype.html`.
- **홈 판단 근거를 "판단 기준 슬롯"에서 도출** (`app/(main)/home/page.tsx`) — 히어로 근거 칩·상태·준비물 사유를 `badges`(weatherData 스칼라)가 아니라 사용자 일과 첫 슬롯의 실측 지표에서 뽑는다. 시간대별 환경·케어 플랜 카드가 같은 슬롯 값을 쓰기 때문에, badges로 만들면 한 화면에서 "히어로는 특이사항 없음 / 아래 카드는 자외선 매우강함"처럼 어긋난다(2026-07-21 "홈=판단 / env=근거" 결정이 해소한 문제와 같은 류). 임계값은 `slotNotables()` 경고급 조건과 동일하게 맞췄다. 1순위는 AI가 정한 것(hook 조건절)을 따라 pill 아이콘·칩 순서를 정렬한다 — 프롬프트 규칙 5(hook은 1순위 이슈로 쓴다)와 화면이 같은 순서를 말하게 된다.
- **준비물 사유를 실측 신호로 생성** (`app/(main)/home/page.tsx`) — AI 체크리스트는 "이모지 짧은이름"만 주므로(report.ts 출력 규칙) 사유 줄이 비어 있었다. 준비물마다 그것을 정당화하는 환경 신호를 매칭해 "등원 09:00 · 자외선 매우강함"처럼 만든다(선크림↔자외선, 우산↔비 소식, 마스크↔미세먼지·꽃가루). 매칭되는 신호가 없으면 체감 온도로 — 물통·여벌 옷은 더위가 실제 근거다. 일과 미입력 슬롯에서는 시각을 쓰지 않는다(지어낸 시각에 거짓 정밀도를 얹지 않고, "기본 시간" 노출도 금지 범주다).
- **홈 히어로 우상단 "현재" 기준값 블록 + 근거 칩 정리** (`components/HeroDecisionBrief.tsx`, `app/(main)/home/page.tsx`) — 판단 카드에서 "지금 어떤가"를 확인할 곳이 근거 칩뿐이었는데, 칩은 성격이 다른 두 가지(판단에 쓴 이슈 신호 / 현재 기준값)를 한 줄에 섞어 담고 있었다. 카드 우상단에 **하늘 아이콘(32) + `현재 N°`(17/700) + `체감 N°`(13)** 블록을 두어 기준값을 분리하고, 근거 칩에서 `현재`·`체감` 기온을 뺐다 — 같은 지표가 한 카드에서 두 번, 그것도 다른 값(현재 31° / 체감 32°)으로 보이면 어느 쪽이 판단 근거인지 흐려진다. 칩은 **이슈 신호만** 남아 "무엇을 근거로 판단했나"만 말한다(신호가 1개뿐인 날은 pill이 이미 그 이슈를 말하므로 `pickEvidence` 최소 2개 규칙에 걸려 칩 행이 사라진다 — 의도된 동작). 아이콘은 시간대별 환경 카드와 같은 `skySlotIcon`을 재사용해 두 카드의 하늘이 어긋나지 않는다. 수치는 17px 보조 위계라 결론(28/800)과 경쟁하지 않는다 — 큰 기온을 화면 중앙에 놓는 날씨 앱과 다른 점이 이 위계다. 기온 실측이 없으면 블록 자체를 그리지 않는다(추정값 폴백 금지). 쓰이지 않게 된 `pickEvidence`의 `pin` 옵션은 함께 제거.
- **홈 판단 카드 단일화 — 상세·오늘 챙길 것을 히어로 안으로 흡수** (`app/(main)/home/page.tsx`, `components/HeroDecisionBrief.tsx`, `components/PrepChecklistCard.tsx`, `components/ReportFeedback.tsx`) — 홈 상단이 카드 3개(판단 / 상세 진입 / 오늘 챙길 것)로 갈려 있어 "판단 → 그 판단이 지시한 실행"이 한눈에 하나로 읽히지 않았다. **하나의 L2 카드**로 합치고 구분은 전체폭 헤어라인만 쓴다 — 안쪽(inset) divider는 "목록의 행 구분", 전체폭 divider는 "같은 카드의 다음 섹션"으로 문법을 갈랐다. `PrepChecklistCard`에 `embedded`(표면 없음) 모드를 신설해 **카드 안 카드는 만들지 않는다**. embedded에서는 전원 완료 시 배경 tint를 쓰지 않고 카운터만 "준비 끝"으로 바꾼다 — 유일한 L2 표면의 일부만 색면이 되면 다시 "카드 두 개"로 읽힌다. 상세 진입은 "자세한 리포트" 텍스트 토글 → 64px 펼침 카드를 거쳐 **근거 칩과 같은 행 오른쪽의 `자세히 ⌄`**(타깃 44px)로 정착했다: 근거(왼쪽)와 더 읽기(오른쪽)는 둘 다 판단이 끝난 뒤의 확인용이라 같은 위계이고, 행을 따로 쓰면 카드가 40px 길어지는 대가로 얻는 정보가 없다. 펼침 본문은 그 행 바로 아래 헤어라인 아래로 열린다. 상세 본문에서 히어로 근거 문장으로 이미 쓴 줄은 제외한다 — 같은 카드 안에서 같은 문장이 네 줄 아래 또 나오면 복사해 붙인 글처럼 읽힌다. 종전 진입 행이 상시 노출했던 판단 기준 보조문("○○ 체질 · 등원 09:00 기준")은 이 정리로 사라졌고, 같은 정보는 펼친 뒤 trust line이 담당한다. 피드백 행의 자체 divider·`px-0.5`를 제거해 카드가 divider를 소유하게 했다(좌측선 x=40 정렬, 선 두 겹 제거). 로딩 스켈레톤도 새 골격(기준값 블록·칩+CTA 한 행·체크리스트 섹션)으로 맞춰 로딩→실물 전환의 높이 점프를 없앴다. **실측**: 첫 준비물 행 하단 692 → **615px**, 둘째 행까지 678px로 Safari 가용 높이(~671) 근처까지 들어온다.
- **건강팁(tips) 근거 기반 재구현 — 목데이터 제거·출처 문서 특정** (`lib/tips/*`, `app/(main)/tips/page.tsx`) — "의학적 근거 기반"을 표방하면서 환경 데이터는 하드코딩 mock이고 출처는 기관 홈페이지 링크뿐이던 상태를 해소. 콘텐츠를 선언적 테이블(`lib/tips/content.ts`)로, 판정을 순수 함수(`lib/tips/select.ts`)로 분리했고, 팁의 사실·출처는 런타임 생성 없이 authoring 시점에 원문을 확인해 넣는다. 출처는 "기관 「문서명」 (연도)"로 문서를 특정하고 원문 확인일과 비승인 고지를 함께 표기하며, 하단 기준 표기는 실제 인용한 출처에서 생성한다. 종전 인용 중 아토피 가이드라인의 발행 기관 오류(대한피부과학회 → 대한아토피피부염학회)를 바로잡았다. 자체 숫자 cutoff를 없애고 공인 등급(`dustLabel`·`pollenLabel`·`uvLabel`·`humidityLabel`)에서 심각도를 도출해 환경정보 화면과의 등급 모순을 제거했으며, 프로필 매칭은 `lib/domain/child-conditions` 공유로 전환(구형·데모 프로필의 짧은 라벨을 놓치던 버그 해소), 마스크 권고는 만 2세 미만에 외출 자제로 대체한다. 필요한 환경 신호가 결측이면 해당 팁을 노출하지 않고(fail-closed) 그 사실을 안내한다.
- **건강팁 판단 기준을 오늘 하루 피크로** — '지금 이 순간' 값으로 판단해 새벽·저녁에는 자외선 팁이 구조적으로 뜨지 않던 문제(새벽 자외선은 언제나 0)를 해소. 이미 받고 있던 시간대별 데이터로 자외선·대기질은 하루 최댓값, 건조는 하루 최저 습도를 기준으로 판단한다.
- **건조 판정 기준 통일** (`lib/timeline.ts`) — 화면마다 다르던 건조 임계값(환경정보 30% / 준비물 45% / 건강팁 40%)을 `humidityLabel`로 통합하고, 비공개였던 자외선 라벨 함수를 `uvLabel`로 공개.

### Changed
- **홈 시각 완성도 폴리싱 (F1~F7)** (`app/(main)/home/page.tsx`, `DESIGN.md`) — "실제 유료 육아 앱 수준"의 정돈감을 목표로 `/design-review` 승인 게이트(진단→개선안→항목별 승인→원자 커밋)로 진행. **(F1)** hook 아래가 전부 bold이던 '볼드 고원' 해소 — 체크리스트 항목명 14.5px/bold→16px/medium(리스트행 문법), 카드 라벨 15→14px로 스케일 정렬해 hook→섹션→라벨→항목 3단 위계 확보. **(F2)** 케어 플랜 카드만 회색 1px 보더가 있어 환경 타임라인 카드와 표면이 다르던 문제 해소 — resting을 `border-transparent`로 두고 base를 `border-[1.5px]`로 올려 그림자만 있는 면으로 통일(지금/곧 강조 시 레이아웃 시프트 0). **(F3)** AI 카드 내부 `px-0.5` 제거로 카드 내부 좌측선 x40 통일. **(F5)** 섹션 간격이 모두 `mt-8`이던 것을 중요도 차등(히어로→환경 48px / 환경↔케어 32px)으로. **(F6)** hook 위 현재환경 한 줄 값을 `medium`/`muted-foreground`로 톤다운해 결론(hook) 강조(5개 지표·WCAG AA 유지). **(F7)** 히어로 헤더 밴드를 크림(`secondary`)→브랜드 피치(`primary-tint`)로 바꿔 아이콘 타일과 웜톤 통일 — 한 카드 안 두 웜 액센트(크림+피치)가 탁해 보이던 문제 해소, `secondary`는 홈에서 미사용이 됨(다른 화면 히어로·이벤트 강조 토큰으로 존치). 전부 DESIGN.md 토큰 내에서 해결하고 Decisions Log에 기록. F4(케어 카드 레일 들여쓰기)는 정당한 타임라인 패턴이라 유지.

### Fixed
- **리포트 당일 캐시가 프로필 수정·PM2.5·습도 변화를 놓치던 결함** (`app/(main)/home/page.tsx` 캐시 키 v26, `SPEC.md`) — 캐시 키가 `childId + 날짜`뿐이라 **같은 날 아이 체질·민감도·일과를 수정해도 구 판단을 그대로 재사용**했다(me 화면에서 "추위 많이 탐"으로 바꾸고 홈에 돌아와도 어제 기준 브리핑). 급변 판정(`envSignature`)도 PM10만 봐서 PM2.5·통합대기만 나빠진 경우와 습도 급변을 놓쳤다. **수정:** ① 판단에 실제 들어가는 프로필 입력만 정규화한 `profileSig`(체질·민감도·일과·나이)를 캐시에 저장해 불일치 시 재생성 — 이름·성별 같은 표시용 필드는 제외해 판단이 같은데 Claude를 다시 호출하지 않고, `age`·`birth`는 서버의 마스크 연령 게이트(만 2세 미만) 판정에 쓰이므로 포함한다. `JSON.parse` 유래 객체의 키 순서 흔들림을 막기 위해 필드를 명시 나열한다. ② `envSignature`에 `pm25Bad`·`khaiBad`·시각별 습도를 추가하고, 습도는 **같은 시각 예보끼리** ±20%p로 비교해 하루 주기의 자연 변동(아침↔낮)이 재생성 비용을 유발하지 않게 한다. ③ 마운트 프라임도 `profileSig`를 확인해 구 판단을 잠깐이라도 보여주지 않는다. 캐시 키 v25→**v26**(v25는 브리핑 3문장 구조가 이미 점유). dev 실측 검증: 저장된 캐시에 `profileSig`·`pm25Bad`·`khaiBad`·시각별 습도 6개가 기록되고, 시그니처를 오염시킨 뒤 재진입하면 실제로 재생성된다. Codex 엔지니어링 리뷰 T3.

### Changed
- **은퇴한 AI prep 실험 경로 제거 — 케어 플랜 칩은 규칙 엔진 단일 소스** (`app/(main)/home/page.tsx`, `lib/prep.ts`, `SPEC.md`, `docs/report-and-prep-architecture.md`) — 제품 결정(2026-07-20)은 케어 플랜 칩을 규칙 엔진(`lib/prep.ts`)으로 확정했는데 클라이언트에 `?prep=ai` 실험 경로가 남아 홈 준비물 엔진이 세 갈래였다(AI 체크리스트 / 규칙 폴백 / 규칙-또는-AI 칩). `prepVariant` state·`aiday:prepVariant`·prep 프리즈(`aiday:prepFrozen`)·`aiPrep` state와 그 effect를 제거해 칩은 `buildPrepKeywords` 단일 소스가 된다. 리포트 API의 `prep` 필드와 A/B 하네스(`scripts/chip-ab.test.ts`)는 회귀 비교용으로 유지 — 화면 분기만 사라진다. 히어로 준비물(`prepItems`)은 체크리스트에서 파생하므로 영향 없다. dev 실측 검증: 브라우저에 `aiday:prepVariant = "ai"`가 남아 있어도 칩이 규칙 엔진 어휘로 렌더된다. Codex 엔지니어링 리뷰 T7.

### Fixed
- **새로고침이 한도(429)를 맞으면 멀쩡한 아침 리포트가 사라지던 결함 + 규칙 폴백 문장 깨짐 2건** (`app/(main)/home/page.tsx`, `lib/recommendation-engine.ts`, `lib/recommendation-engine.test.ts` 신설, `tests/e2e/09-rate-limit.spec.ts`) — 2026-07-27 실사용 제보: 새로고침 클릭 한 번에 히어로가 "도준이에게는 오늘 이에요. …"로 깨진 "기본 추천"으로 추락. 세 결함의 연쇄였다. ① **429 캐시 미복원:** 새로고침(강제 재생성)은 화면을 비우고 캐시를 건너뛰는데, 서버가 하루 한도 429를 돌려주면 fatal 분기가 localStorage에 멀쩡히 남은 당일 캐시를 되살리지 않고 규칙 폴백으로 떨어졌다 — 한도는 "새 생성"만 막힌 것이므로 프로필 시그니처가 일치하는 당일 캐시가 있으면 복원하고 한도 배너만 표시한다(폴백은 캐시조차 없을 때만, PR #152의 일시 실패 재시도가 커버하지 않던 경로). ② **폴백 템플릿 빈 사유 깨짐:** `buildRecommendation` 메시지가 발동 사유 배열을 무조건 문장에 끼워 넣어, 사유 0개인 날(32° 폭염이지만 습도가 땀 임계 미달 + 꽃가루·미세먼지·바람 양호)엔 "오늘 이에요", 준비물까지 0개면 "를 꼭 챙겨주세요"로 깨졌다 — 원설계부터의 잠복 결함이 429 폴백 노출로 처음 드러난 것. "무난한 날" 문장으로 분기(체질 준비물이 있으면 뒤에 잇고, 없으면 "평소대로 준비하면 충분해요") + 회귀 테스트 4건 신설(실사용 재현값으로 깨짐 2종을 핀하고 정상 경로 2종 보존). ③ **폴백 히어로 제목·본문 중복:** hook이 없으면 headline이 본문 첫 줄(`plainLines[0]`)을 그대로 쓰는데 supportLine(이름 포함 줄 발췌)이 같은 한 줄을 또 골라 같은 문장이 두 번 렌더됐다(7/26 Decision Brief 개편의 supportLine 발췌가 한 줄짜리 폴백 메시지를 미고려) — 마크다운 표기만 다른 동일 문장이면 support를 숨긴다. e2e `TC-RATE-CACHE`(모킹) 신설: 캐시 키·profileSig는 페이지 내부 구현이라 직접 시드하지 않고, 성공 모킹으로 앱이 스스로 캐시를 쓰게 한 뒤 429로 재라우팅 + 새로고침 클릭으로 캐시 복원·한도 배너·"기본 추천" 부재를 검증(기존 TC-RATE-LOGIN·TC-RATE-GUEST-CTA·TC-ERR-03/04 통과 유지 — 캐시 없는 429는 종전대로 폴백). dev 실측: 503 모킹 폴백 렌더에서 "지우에게는 오늘 무난한 날이에요. 호흡기가 예민하니 보습제를 꼭 챙겨주세요." 단일 노출 확인.
- **한 화면에서 같은 물건을 두 이름으로 부르던 결함 — 여벌 어휘를 "젖는 원인"으로 갈랐다** (`lib/prep.ts`, `lib/recommendation-engine.ts`, `lib/prep-vocab.ts`, `scripts/eval-report.mjs`, 테스트 3파일) — 케어 플랜 칩은 "여벌 옷"(규칙 엔진), 오늘 챙길 것 체크리스트는 "여벌 상의"(AI 프롬프트 v25 예시 3)로 같은 물건을 다르게 부르던 문제(2026-07-27 dev 실측). **예외가 아니라 기본 경로의 상시 결함이었다** — `prepVariant` 기본값이 `"rule"`이라 일반 사용자의 칩은 항상 규칙 엔진 출력이고 체크리스트는 항상 AI 출력이라, 땀 나는 날마다 반드시 갈렸다. **별칭 병합은 오답이다:** "여벌 옷"과 "여벌 상의"는 표기 변형이 아니라 부모가 가방에 넣을 양이 다른 처방이라(땀이면 상의 1장, 비면 하의·양말까지) `PREP_ALIASES`로 합치면 어휘는 맞아도 처방이 틀린다. **수정:** ① 규칙 엔진이 발생 지점에서 AI와 **같은 기준으로** 이름을 가른다 — 기준은 추측이 아니라 v25 eval 실측에서 도출했다(`docs/report-eval/aha-*` 9개 파일): AI가 "여벌 상의"를 쓰는 건 **땀 체질 아이(`sweat=very-much`)의 땀 체인이 그날 처방일 때**뿐이고(해당 시나리오 2개에서 매 실행 일관), 땀 체질이 아니면 비 없는 폭염날("주말 × 폭염+자외선 매우강함")에도 "여벌 옷"을 쓴다. few-shot도 상의 사례(예시 3)의 전제에 "땀 많음"이 들어 있고 비가 겹친 예시 8은 "여벌 옷"이다. 그래서 **`sweatProne && 비 없음`일 때만 "여벌 상의", 그 외엔 "여벌 옷"**으로 맞췄다(비 판정은 `rainWindow` 또는 창 max ≥60%, 우산 확정 기준과 동일 — 예비 신호 40~50%로는 부풀리지 않는다). **프롬프트는 건드리지 않았다** — v25가 넣은 실행 디테일 시연("여벌은 상의 1장이면 충분해요")을 보존하기 위해, 어긋난 쪽인 규칙 엔진을 AI에 맞췄다. 첫 시도는 "비 유무"만으로 갈랐다가 eval 실측 대조에서 땀 체질 없는 고온·고습 날에 새 갈림을 만드는 것을 확인하고 조건을 좁혔다. ② 폴백 체크리스트(`buildRecommendation`)도 같은 기준으로 정렬하고 사유도 "비·땀 대비"/"땀 대비"로 가른다(`RecoSlot`에 강수 필드를 선택 필드로 추가 — mock `TimeSlot`엔 없어 없으면 땀 기준). ③ `prep-vocab.ts`에 "여벌 상의"를 **별개 표준명**으로 등록(`여벌 티셔츠`·`여벌상의` → `여벌 상의`)하고 "여벌 옷"과 병합하지 않는 이유를 주석으로 고정. ④ eval `ITEM_ALIASES`에 별개 항목으로 추가 — 종전엔 `hook 아이템 ⊆ checklist`가 `ITEM_ALIASES` 키만 순회해 예시 3의 hook("…여벌 상의로 갈아입혀 주세요")이 검사 밖에 있었고, hook은 상의·checklist는 옷인 갈림이 그대로 통과했다. **남은 한계:** eval의 `prep ⊆ checklist`는 같은 AI 페이로드 안을 비교하므로 규칙 엔진 대 AI 갈림은 구조적으로 못 잡는다 — 이번엔 발생 지점 정렬과 유닛 테스트(원인별 이름 5건 + 표준명 분리 1건)로 고정했다. **선행 의존:** 병행 작업 중인 `fix/briefing-item-exclusivity`가 "준비물 총량을 한정하지 않기"(message의 "X 하나면 충분" 문형 금지) 규칙을 추가하는데, 이 수정의 근거인 few-shot 예시 3의 "여벌은 상의 1장이면 충분해요"가 그 문형에 해당한다. 그쪽 랜딩 후 **예시 3에 "여벌 상의"가 남아 있는지 재확인**해야 하며, 사라졌다면 `sweatProne && 비 없음 → "여벌 상의"` 분기를 함께 재검토한다(그 경우 남는 것은 어휘 사전·eval·테스트 정비).
- **홈 히어로 근거 칩이 하나도 안 보이던 결함 — 후보 풀이 warn 전용이 되어 공급이 끊겼다** (`lib/hero-brief.ts`, `app/(main)/home/page.tsx`, `lib/hero-brief.test.ts`, `DESIGN.md`) — 히어로의 판단 근거 칩(`자외선 강함`·`강수 60%` 등)이 시간대에 따라 통째로 사라지던 문제(7/26 18:30 제보). **원인:** 같은 날 anatomy 개정이 우상단에 기준값 블록을 만들면서 근거 칩에서 `현재`·`체감` 기온을 뺐는데, 그 둘이 후보 풀의 **유일한 non-warn 공급원**이었다. 풀이 warn 전용이 되자 warn 신호가 1개 이하인 시간대에는 `EVIDENCE_MIN=2`에 걸려 칩이 0개가 되고, `heroState`는 warn ≥1이면 caution이라 **"주의색 카드 + 근거 칩 0개"가 구조적으로 발생**했다(16:22 warn 2개 → 18:30 1개가 그 경계). 개정 근거 문서가 "칩이 3개 → 2개가 됐다(2~3개 규칙 안)"고 적은 것은 그날 데이터에서만 참인 문장이었고, warn 개수가 보장되지 않는다는 점을 아무도 검산하지 않았다. **수정:** ① 계량 지표(일교차·강수)를 뉴트럴 칩으로 복원 — 등급 지표(미세먼지·꽃가루·자외선·바람·건조)는 warn일 때만, 계량 지표는 등급 없이 값 그대로라는 규칙으로 분리했다. `현재`·`체감` 기온 칩 금지는 유지(일교차는 "기온의 폭"이라 우상단 블록과 중복이 아니다). ② 일교차 표본은 `envRaw.weather.hourlyForecast`(06~21시 6점) — 슬롯 기온으로 계산하면 하루 최저가 흔한 06시가 빠져 AI가 말한 일교차와 수 도(度) 어긋난다. 정식 TMX/TMN은 홈이 호출하지 않는 주간예보에만 있어 근사임을 명시. ③ `EVIDENCE_MAX` 3→2 — 명세의 "3개 합 285px, 310 안에 1줄 수납"은 근거 행이 칩만 쓰던 시절 계산이고, `자세히 ⌄`가 같은 행에 들어온 뒤 재계산되지 않았다. 빌드된 Tailwind + Chromium 390px 실측 결과 칩 가용 폭은 **234px**, 3개는 266px로 2줄로 밀린다. ④ 칩과 히어로 상태를 `buildHeroEvidence` 단일 순수 함수로 통합하고 caution일 때만 하한을 1로 분기(결측·평상·fallback은 명세대로 2 유지). **재발 방지:** 칩 도출이 `page.tsx` 인라인이라 유닛 테스트가 불가능했고 그래서 테스트 309건을 전부 통과한 채 배포됐다 — 순수 함수로 추출해 불변식(caution ⇒ 칩 ≥1 / warn 칩 ⇒ 카드 ≠ normal)을 **양방향으로** 고정했다(15건 추가). 규칙이 리뷰 HTML·Decisions Log·코드 주석에 흩어져 재발한 것이라 DESIGN.md Component Grammar에 현행 계약 표를 신설해 단일 진실로 삼고, 구 리뷰 문서 3건에는 개정 배너를 달았다. 일교차 warn 승격(임계 8°C)은 `heroState` 판정을 바꾸는 별건이라 제외했다.
- **홈 히어로 조건 pill과 결론 헤드라인 사이 여백 결함 — 기준값 블록 세로 스택 → 가로 정렬 (56.5→18.5px)** (`components/HeroDecisionBrief.tsx`, `app/(main)/home/page.tsx`, `DESIGN.md`) — 조건 배지("낮 30도 자외선 강함")와 결론(28/800) 사이가 광학 약 70px로 벌어져 "조건 → 결론"으로 이어져야 할 시선이 끊기던 문제. **원인은 여백 값이 아니라 행 높이였다:** 상단 행이 `flex items-start`라 높이가 둘 중 큰 쪽을 따르는데, 우상단 기준값이 세로 스택(하늘 32 + 6 + 현재 17 + 6 + 체감 13 = **74px**)인 반면 조건 pill은 33.5px(`py-2` + 13/1.35)여서 **pill 아래 40.5px가 자동으로 비었다**. 여기에 결론 `mt-4`가 더해져 박스 56.5px가 되고, `mt`를 0으로 줄여도 40.5px는 남아 여백 토큰으로는 고칠 수 없는 구조였다. **수정:** 기준값 블록을 `아이콘 위 / 온도 2줄 아래`에서 `아이콘 왼쪽 / 온도 2줄 오른쪽` 가로 정렬로 바꿔 블록을 36px로 낮춘다 — pill(33.5px)과 나란해져 남는 여백이 2.5px가 되고 결론은 의도한 `mt-4` 자리로 돌아온다. 하늘 아이콘 32px·`현재` 17/700·`체감` 13·무채색은 그대로라 2026-07-26 anatomy 결정의 값과 위계는 변하지 않는다(#169 계승). 스켈레톤 자리표시자도 같은 가로 배치로 맞춰 로딩→실물 전환 시프트 0을 유지. 부수 효과로 히어로가 38px 짧아져 Safari 가용 높이(~671px) 여유가 늘었다. 수치는 전부 빌드된 Tailwind CSS + Chromium 390px 실측(카드 내부 폭 310px). 대안 2건은 기각 — **한 줄 압축**은 아이콘 32→24 되돌림에 폭 합계 323px로 내부 폭 310px를 넘겨 줄바꿈이 나고, **float 감싸 흐르기**는 가장 조밀하나 헤드라인 첫 줄이 244px로 좁아져 결론 2줄 고정이 긴 hook에서 깨진다.
- **홈 랜딩 지연 — 콜드 env 네트워크에서 히어로·env 카드 분리 + uv/pollen 캐시미스 사이클 해소 + 랜딩 동의 동기화 비동기화** (`app/(main)/home/page.tsx`, `app/auth/landing/page.tsx`) — 프로덕션 `?perf=1` 실측에서 홈 셸 표시가 weather+air 네트워크 완료(`env_primary_gate`, 콜드 5.3s/웜 0.24s)에 묶여 있고, 리포트가 당일 캐시로 프라임돼도 `loading`이 히어로 카드 전체를 스켈레톤으로 가려 콜드 초기 진입에서 프라임 최적화가 무력화되던 것을 확인. **① 히어로 카드를 `loading`에서 분리**(`loading && !reportPrimed`) — 당일 캐시 리포트가 있으면 env 도착 전에도 즉시 노출(카드 내 "현재 환경 한 줄"은 도착 전 자동 숨김이라 스테일 수치 노출 없음). weather 간헐 실패(기상청 502) 시 프라임된 진짜 리포트를 "기본 추천"으로 오표기하지 않도록 가드. **② uv/pollen 클라 타임아웃 5s→9s** — 클라(5s)가 서버 라우트 상한(`AbortSignal.timeout(8000)`)보다 짧아, 상류가 5~8초 걸리는 순간 클라가 서버의 `revalidate` 캐시 적재 완주 전에 끊어 캐시가 영영 안 데워지고 매 진입 5초 타임아웃+uv/pollen 결측 폴백에 갇히던 사이클 해소(프로덕션에서 `pollen_timeout` 실재 확인). 재시도는 두지 않는다(최악 ~19s로 uv/pollen이 새 게이트 병목이 되는 것 방지). **③ 랜딩 동의 동기화 `await` 제거** — 홈 표시와 무관한 백그라운드 이력 기록(`syncLocalConsentsToDb`)이 프로필 조회 왕복 뒤에 직렬로 쌓여 랜딩 스피너를 늘리던 것을 비동기(fire-and-forget)로 전환. dev 재검증: env 게이트 5.7s→2.4s(콜드)→216ms(웜 캐시히트), 재진입 cache_hit 총 348ms.
- **홈 "환경 데이터를 불러오지 못했어요" 반복 오류 — 단기예보 NO_DATA를 성공으로 오인하던 결함** (`app/api/weather/route.ts`, `lib/kma-forecast.ts`, `lib/kma-time.ts`) — 상단 현재값 칩은 뜨는데 "시간대별 환경"·"오늘의 케어 플랜"만 오류가 나고, 새로고침해도 30분간 그대로이던 반복 제보의 근본 원인. **원인:** "날씨"는 서로 다른 두 데이터(현재값=초단기실황 / 시간대별 예보=단기예보)인데, 라우트가 단기예보 응답의 `resultCode`를 검증하지 않아(uv·pollen 라우트엔 있던 검증이 weather에만 없었다) data.go.kr이 NO_DATA·부분 장애를 **HTTP 200 + resultCode "03"** 으로 돌려주면 빈 예보를 그대로 받았다. 실황으로 현재값은 채워지니 앱은 "날씨 정상"으로 판정(부분 실패를 완전 성공으로 오인)하고, 예보가 필요한 시간대별 환경·케어 플랜만 조용히 비었다. 게다가 이 빈 응답이 `revalidate: 1800`으로 30분 캐시에 고정돼 수동 새로고침도 같은 캐시를 되돌려줬다. **수정:** ① `extractFcstItems`로 `resultCode`·빈 body를 실패로 판정 ② 최신 발표본이 비면 직전 발표본들로 순차 폴백(`recentFcstBases`, 최대 4개 — 발표본별 URL이 달라 캐시 오염을 우회) ③ 예보 fetch를 `.catch`로 감싸 단일 실패가 라우트 전체를 500으로 떨구지 않게 함 ④ 현재값·예보가 모두 없으면 200으로 위장하지 않고 502로 돌려 클라이언트가 재시도하게, 현재값만 있는 부분 성공은 200으로 정직하게 처리 ⑤ 부분 성공 시 어느 발표본이 비었는지 경고 로그. 회귀 테스트 추가(`lib/kma-forecast.test.ts`·`lib/kma-time.test.ts`). (근본 대책 B — 홈의 `env-data.ts` 통합 이관·통합 페처의 hourlyForecast 검증·자동 검증 스크립트 확장은 후속 A로 분리.)
- **AI 리포트 준비물 정합성 런타임 강제 — 여름 마스크 오권장 차단 + prep⊆checklist** (`lib/report-sanitize.ts` 신설, `app/api/report/route.ts`, `lib/prompts/report.ts`, `app/(main)/home/page.tsx`) — 습도 93%·미세먼지 좋음인 여름날 AI가 "눅눅한 공기 → 마스크"로 메시지·준비물(체크리스트·prep)에 마스크를 권하던 문제. **근본 원인:** #131(2026-07-20)이 마스크 규칙·`prep⊆checklist` 등 정합성 규칙을 세웠으나, 그 강제가 프롬프트(확률적)와 오프라인 eval(`scripts/eval-report.mjs`, 12 시나리오, dev 전용)에만 있었고 실사용 `/api/report` 응답 경로엔 런타임 가드가 없었다 — 12 시나리오 밖 입력에서 모델이 규칙을 어기면 그대로 화면·캐시로 샜다. 그 빠진 **런타임 층**을 신설: 구조 필드(checklist·prep)에 ①마스크 정책(근거 게이트: 미세먼지 나쁨 등급≥3/꽃가루 높음 지수≥2일 때만 — 습도·더위는 사유 아님. 연령 게이트: 만 2세 미만은 마스크 대신 "실내놀이" 대체) ②`prep⊆checklist`(케어 칩이 "오늘 챙길 것"에 없는 아이템을 내보내면 제거 — 어휘 별칭은 `canonicalPrep`으로 흡수)를 결정적으로 강제한다(규칙 엔진 `lib/prep.ts`와 같은 임계값, 사유별 관측 로그). 본문(hook·message)은 자유 문장이라 재생성 없이 결정적 재작성이 불가능하므로 프롬프트가 담당 — 마스크 규칙에 "습도·더위 사유 금지" 명시 + 호흡기 민감×여름 고습+비인데 마스크를 권하지 않는 few-shot 예시 추가(#151의 민감 체질 프레이밍과 정합). 계절이 아니라 입자 농도가 기준이라 여름이어도 미세먼지 나쁨·꽃가루 높음이면 마스크는 정상 권고된다. 홈 리포트 캐시 키(v22→v23)·prep 동결 키(v1→v2)를 올려 구캐시 무효화. 회귀 테스트 `lib/report-sanitize.test.ts` 추가.
- **홈 AI 리포트가 "기본 추천" 폴백에 갇히던 회복력 결함 — 리포트 fetch 재시도 + 함수 타임아웃 상한** (`app/(main)/home/page.tsx`, `app/api/report/route.ts`) — 환경 데이터 fetch는 #135에서 실패 시 1회 재시도를 받아 "한 번 끊기면 폴백에 갇히던 문제"를 해소했지만, 정작 핵심인 리포트 생성 fetch는 그 보호에서 빠져 있었다. 콜드 스타트·게이트웨이 순간 오류·네트워크 끊김으로 스트림이 한 번 실패하면 즉시 규칙 기반 "기본 추천"으로 떨어지고 자동 복구가 없어, 하루 첫 진입(=당일 캐시 미스, 콜드 함수)인 아침에 폴백이 반복 노출됐다(당일 캐시는 성공본만 저장하므로 재방문·새로고침 후엔 정상 — "아침엔 기본 추천, 새로고침하면 괜찮아지는" 패턴의 원인). 리포트 fetch에 순간 실패 한정 재시도(최대 2회·900ms 간격)를 추가하고 — 한도 소진(429)·입력 오류(4xx)·설정 오류(503)·요청 취소(프로필 전환·언마운트)는 재시도 대상에서 제외 — `/api/report`에 `maxDuration = 60`을 명시해 스트리밍 생성이 Vercel 기본 타임아웃에 절단되지 않게 한다. 서버·게이트웨이·레이트리밋은 정상임을 확인(프로덕션 실호출·Supabase 카운터·실데이터 재현)했고, 클라이언트 회복력 결함이 근본 원인이었다.
- **Vercel 함수 리전을 `icn1`(서울)로 고정 — 홈 리포트 hook 약 1.2초 단축** (`vercel.json` 신설, `docs/perf-home-latency.md`) — `vercel.json`이 없어 기본값 `iad1`(미국 동부)에서 돌고 있었다. 이 앱의 의존성은 전부 한국에 있어(Supabase `ap-northeast-2` · AI 게이트웨이 `gw.letsur.ai` · 기상청 · 에어코리아) 매 호출이 태평양을 왕복하던 구조. 프리뷰에서 리전만 바꿔 실측한 결과 카운터 RPC 왕복 227~893ms → **39~55ms**, 모델 첫 토큰 2559~3298ms → **1645~1862ms**, 사용자에게 보이는 hook 3297~4262ms → **2036~2977ms**. 레이트리밋 도입 과정에서 드러난 기존 구조 문제로, 레이트리밋과 무관한 구간까지 함께 개선된다.
- **꽃가루 API 지점코드·발표시각 형식을 공식 명세에 맞춤** (`app/api/pollen/route.ts`, `app/api/uv/route.ts`, `lib/kma-area.ts`) — 기상청 자체 2자리 지역코드(서울 `11`)와 `YYYYMMDD`를 보내던 것을 명세의 10자리 법정동코드(서울 `1100000000`)·`YYYYMMDDHH`로 교체. 이 계열 API는 2자리 코드를 받지 않는다(`areaNo=11` → `99 검색결과가 없습니다. [11]`, 실측 확인). 자외선 API와 같은 코드 체계이므로 `lib/kma-area.ts` 단일 출처로 통일하고 자외선 라우트의 중복 상수를 제거 — 자외선 라우트 주석의 "꽃가루와는 다른 체계" 서술도 정정. 2026-07-01 행정구역 개편(광주·전남 → 전남광주통합특별시 `1200000000`)이 두 API에 한 번에 반영된다.
- **꽃가루 자료제공 기간 표기 정정** (`app/(main)/env/page.tsx`, `SPEC.md`) — 환경정보 "제공 기간 아님" 안내를 "참나무·소나무 4~6월" → "참나무·소나무 3~6월 · 잡초류 8~10월"로 수정(3월 시작은 API 응답 메시지로 확인).
- **홈 하이드레이션 불일치(React #418) 해결 — 화면 깜빡임·모바일 로딩 실패의 근본 원인** (`lib/useLocation.ts`, `lib/profile.ts`, `app/(main)/home/page.tsx`) — 위치·프로필 상태를 `useState` 초기값에서 localStorage로 읽어(위치 `loadLocation`, 프로필 `loadProfiles`, 활성 아이 `localStorage.getItem`), 서버(SSR엔 localStorage 없음 → 기본값 '중구'·`defaultProfiles`)와 클라이언트 첫 렌더(저장값, 예: '송파구'·저장 프로필)의 헤더 위치 라벨·프로필 세그먼트가 어긋나 하이드레이션이 실패(React #418)하고 루트 전체가 클라이언트 재렌더되던 문제. 초기값을 SSR 안전한 상수(`DEFAULT_LOCATION`·`defaultProfiles`)로 두고 실제 저장값은 마운트 후 effect에서 주입하도록 변경(`defaultProfiles` export 추가). 같은 클래스의 `prepVariant`(초기값에서 `window`·localStorage 접근) A/B 잠복 버그도 effect 주입으로 수정. `useLocation`을 공유하는 env·outfit·me 화면의 위치 하이드레이션 리스크까지 함께 제거.
- **느린 공공 API에서 홈이 '데이터 지연' 폴백에 갇히던 회복력 결함** (`app/api/weather/route.ts`, `app/(main)/home/page.tsx`) — 실황(T1H) 결측 시 순차 재시도 타임아웃을 8s→3s로 줄여 라우트 최악 지연이 클라이언트 abort(9s)를 넘기지 않게 하고(실패해도 예보 최근접값 폴백 존재), 클라이언트 weather·air 요청에 실패 시 1회 무음 재시도(1.2s 후, 그땐 서버 캐시가 데워져 대개 즉시 성공)를 추가. 콜드 캐시 첫 로드가 한 번 끊기면 수동 새로고침 전까지 폴백에 갇히던 문제(자동 재시도 없음) 해소.
- **환경정보·옷차림·건강팁·마이 탭의 동일 하이드레이션 불일치(React #418) 해결** (`app/(main)/env·outfit·tips·me/page.tsx`) — 네 탭 모두 `profiles`(프로필 목록)·활성 아이(`activeId`/`active`)를 `useState` 초기값 또는 렌더 중 localStorage로 읽어, 홈과 동일하게 서버(기본 프로필)와 클라 첫 렌더(저장 프로필)가 어긋나 #418을 내던 문제. 초기값을 SSR 안전한 `defaultProfiles`로 두고 저장값은 마운트 effect에서 주입하도록 통일(env·outfit·tips는 렌더 계산값이던 activeId를 상태로 전환).

### Security
- **DB 함수 노출면 축소 — 보안 어드바이저 경고 2건 해소** (`supabase/migrations/20260722022535_security_advisor_hardening.sql`) — `public.rls_auto_enable()`의 PUBLIC 기본 EXECUTE 권한을 `public`·`anon`·`authenticated`에서 회수(lint `0028`). 조사 결과 이 함수는 `returns event_trigger`로 이벤트 트리거 `ensure_rls`에 연결되어 public 스키마 CREATE TABLE 시 RLS를 자동 적용하는 안전장치였고, 반환 타입상 PostgREST RPC로는 호출되지 않아 경고는 린터의 보수적 휴리스틱에 가까웠다. 다만 이벤트 트리거는 EXECUTE 권한과 무관하게 소유자 권한으로 발화하므로 회수해도 자동 RLS 기능은 유지된다(적용 후 `evtenabled=O` 확인). 함께 `public.set_updated_at()`의 search_path를 `''`로 고정(lint `0011`) — 본문이 `now()`와 NEW 필드만 쓰므로 pg_catalog 암묵 해석으로 동작에 영향이 없다. `report_usage`의 RLS Enabled No Policy(INFO)는 service_role 전용 접근이라 의도된 상태로 유지.

## [0.3.19.2] - 2026-07-24

> 이번 릴리스는 위 `[Unreleased]`에 쌓여 있던 항목들과는 별개로, QA(Playwright E2E 55건 실행 → 발견 2건 수정·재검증)에서 나온 결과만 담은 소규모 핫픽스다. `[Unreleased]`의 나머지 항목은 계속 다음 릴리스를 기다린다.

### Added
- **Playwright E2E 테스트 스위트 신설** (`playwright.config.ts`, `tests/e2e/*.spec.ts` 9개 + `fixtures.ts`) — PRD v2.8과 실제 코드를 대조해 P0 5종 + P1 예외/경계/탐색 케이스를 자동화(mobile 390×844 + desktop 1280×800). `docs/test/`에 테스트 계획(`test-plan.md`)·케이스(`test-cases.md`)·결과 리포트(`test-report.md`)·최종 요약(`최종보고서.md`)을 문서화. `npm run test:e2e`로 실행.

### Fixed
- **홈 데모 프로필 "예시" 배지 미표시** (`app/(main)/home/page.tsx`) — `lib/profile.ts`의 `isDemoProfile` 판별 함수는 있었으나 이를 참조해 화면에 표시하는 코드가 없어(PRD S-001 수락 기준 미충족) 데모 프로필(지우·도윤)이 실프로필과 구분되지 않던 문제. 프로필 세그먼트 버튼에 `(예시)` 배지를 조건부 렌더하도록 추가.
- **게스트 429(레이트리밋) 화면에 가입 유도 문구·CTA 부재** (`app/api/report/route.ts`, `app/(main)/home/page.tsx`) — 게스트·로그인 사용자가 일일 한도(각각 10회·20회)에 도달했을 때 서버가 동일한 문자열을 반환하고 클라이언트는 이를 사라지는 토스트로만 표시해, PRD S-001이 요구한 게스트 전환 유도("가입하면 계속 이용할 수 있어요" + [무료로 시작하기])가 전혀 노출되지 않던 문제(게스트→가입 전환 퍼널의 핵심 지점 공백). 서버 응답에 `isGuest` 플래그를 추가하고, 홈 화면에 게스트/로그인별로 다른 문구 + 게스트 전용 CTA를 담은 영구 배너를 신설.

## [0.3.19.1] - 2026-07-19

### Changed
- **베타 동의 절차 최소화** — 가입 전 4개 필수 동의를 이용약관 확인 1개로 축소하고, 계약 이행에 필요한 일반 개인정보·국외 위탁은 개인정보처리방침 고지로 전환. 건강 관련 정보는 온보딩 실제 입력 직전에 선택 동의하며 거부 시 해당 정보 없이 계속할 수 있도록 변경.
- **행동분석 선택 참여로 전환** — 베타 행동분석을 서비스 필수 조건에서 제거하고 첫 리포트 확인 뒤 선택적으로 제안. 동의 전에는 계측·피드백 전송을 차단하며, 마케팅 동의 UI는 제거.
- **개인정보 문서 v2** — 개인정보 처리 근거, 선택적 건강정보, 계약 이행에 필요한 국외 처리위탁·보관 방식을 현행 흐름에 맞게 수정.

## [0.3.19.0] - 2026-07-19

### Changed
- **홈 프로필 전환을 칩 → 세그먼트 컨트롤로 교체** (`app/(main)/home/page.tsx`, `DESIGN.md`) — 배경색(primary-tint)만 남은 활성 칩이 "누를 수 있는 버튼"으로 읽히지 않는다는 피드백 반영. `bg-muted` 트랙 위에서 활성 아이만 흰 카드(bg-card + shadow-soft)로 떠오르는 토스식 세그먼트 컨트롤로 교체 — 색 신호가 아니라 형태로 어포던스를 전달(이름은 계속 중립 잉크, 붉은 계열 없음). 프레스 시 `active:scale-[0.97]` 마이크로 피드백. DESIGN.md Component Grammar에 세그먼트 문법 신설.

## [0.3.18.0] - 2026-07-19

### Added
- **베타 행동 계측 인프라** (`supabase/migrations/004~006`, `lib/analytics.ts`, `components/AnalyticsTracker.tsx`) — `events`(행동 로그)·`feedback`(평가·의견) 테이블(RLS insert-only, 게스트 user_id null)과 `track()` 헬퍼로 이벤트 9종 수집(session_start·page_view·가입·온보딩 단계/완료·리포트 노출/새로고침/오류·체크리스트 체크). 지표 뷰 7종(`beta_*`)으로 일일 개요·아침 재방문(북극성)·온보딩 퍼널·리포트 유용성·체크리스트 인터랙션을 SQL 한 줄로 조회 — dev 트래픽 제외·KST 집계 내장. 하드닝: 컬럼 단위 INSERT grant(created_at 위조 차단), 이벤트명 화이트리스트, 퍼널 뷰 캐스트 가드(anon 중독 방어), FK 인덱스, 뷰 security_invoker. 운영 가이드 `docs/beta-metrics.md` 신설.
- **리포트 유용성 평가 + 의견 보내기** (`components/ReportFeedback.tsx`, `components/FeedbackDialog.tsx`) — AI 리포트 하단 "이 리포트가 도움이 되었나요?"(아이·날짜당 1회, 선택 이유 입력, 실패 시 재시도 허용)와 마이페이지 "의견 보내기" 다이얼로그. 전송 8초 타임아웃으로 UI 잠김 방지.
- **베타 필수 동의 수집** (`lib/consent.ts`, `components/ConsentFields.tsx`, `supabase/migrations/20260719132734_beta_consents.sql`) — 약관·베타 분석·아동 민감정보·국외 이전 4종 필수 동의를 가입 폼·온보딩 진입·로그인 랜딩에서 강제(기존 계정도 문서 버전 기준 재확인). 동의 이력은 `user_consents`(RLS owner-only)에 버전별 보관, 비로그인 동의는 로그인 시 동기화. **행동 계측은 beta_analytics 동의 후에만 동작.** 이용약관·개인정보처리방침 베타 v1 실문서 게시.

### Changed
- **신뢰 라인을 리포트 본문 최하단으로 이동** (`app/(main)/home/page.tsx`) — "○○를 위한 프로필 기준 해석 · 기상청·에어코리아 실측 데이터"를 카드 하단에서 본문(message) 바로 아래로.
- **PRODUCT-DECISIONS §1 측정 수단 현행화** — PostHog 선행 → Supabase 자체 계측(베타 규모 기준), 온보딩 5단계 반영, 리포트 유용성을 베타 보조 진단으로 추가.
- **outfit AI코디 배지 토큰화** (`app/(main)/outfit/page.tsx`) — `text-red-500` 하드코딩(위험 오독)을 `bg-primary-tint text-accent`로 (Apple HIG 갭 분석 P1).

## [0.3.17.0] - 2026-07-19

### Changed
- **홈 AI 리포트 헤더 — 라벨 강조·날짜 톤다운·'기준' 표기** (`app/(main)/home/page.tsx`) — 헤더 크림 밴드 안에서 'AI 리포트' 라벨과 날짜·시간이 둘 다 15px/bold 블랙이라 위계가 없던 것을, 날짜·시간 메타를 caption 톤(13px/medium muted-foreground)으로 낮춰 라벨이 주인공으로 읽히게 함. 날짜·시간 뒤에 '기준' 접미어를 추가해 리포트 생성 시점임을 명확히 하고 현재 시각으로 오독되지 않게 함(공유 텍스트·공유 이미지 카드 동일 반영).

## [0.3.16.0] - 2026-07-19

### Changed
- **홈 프로필 칩 간소화 — 이니셜 아바타·나이 표기 제거, 이름 중립 잉크 표기** (`app/(main)/home/page.tsx`) — 그린 파스텔 이니셜 아바타('도')가 오렌지 primary-tint 칩 안에서 웜 뉴트럴 원칙(hue 24~36)과 충돌해 톤이 깨졌고, 이니셜은 바로 옆 이름과 중복 정보라 제거. 나이(만 N세)도 선택 칩의 역할(아이 전환)에 불필요해 제거 — 상세는 마이 페이지에서 확인. 활성 칩 이름은 accent(#C2540A)가 붉은 계열로 읽힌다는 피드백에 따라 foreground(본문 잉크)로 표기하고, 선택 상태는 primary-tint 배경만으로 표현(홈 프로필 칩 한정 예외, 코드 주석에 사유 기록).

## [0.3.15.0] - 2026-07-19

### Fixed
- **AI 리포트가 자외선 피크를 엉뚱한 시간대에 붙이는 오귀속** (`app/api/report/route.ts`, `lib/prompts/report.ts`) — 프롬프트에 자외선을 "하루 최댓값 숫자 하나"로만 전달해(시간 정보 없음) 모델이 임의 시간대에 붙였음(실측: 정오 UVI 8 매우강함·하원 17시 UVI 1 낮음인 날, 리포트가 "하원 무렵 자외선 매우강함"이라고 안내 — 시간대 카드의 "낮음"과 정면 모순). 일정별 날씨 줄에 해당 시각 자외선 등급을 추가하고, 요약을 "자외선 오늘 최고 등급 (H시경)"으로 피크 시각 명시(수치 미포함 — 입력에 숫자를 주면 hook/message 수치 금지 규칙이 있어도 출력으로 샐 위험이 있어 등급만 전달), 프롬프트에 "강함 이상인 시간대에만·그 시간대에 붙여 언급" 규칙 추가. 같은 원칙으로 미세먼지(μg/m³)·꽃가루(위험지수) 수치도 프롬프트 입력에서 제거하고 등급만 전달. 구형 리포트 무효화를 위해 리포트 캐시 키 v17 → v18.

### Added
- **위치 기능 v1 — 홈 위치 버튼으로 실위치 기반 조회** (`lib/locations.ts` 신설, `app/(main)/home/page.tsx`) — 위치 버튼 탭 → Geolocation(사용자 제스처 안에서만 권한 요청) → 서울 25개 구 최근접 매핑 → 날씨는 실좌표, 미세먼지는 해당 구 측정소로 재조회. 라벨("서울 ○○구")과 데이터 기준지가 항상 일치. 서울 밖(20km 초과)·권한 거부·실패는 기본 기준지(서울 중구) 유지 + 정직한 토스트 안내. 선택은 localStorage에 유지. 기본 미세먼지 측정소도 종로구 → 중구로 교정해 라벨과 정합.

### Changed
- **홈 무표기 mock 폴백 전면 제거 — 데이터 부재를 정직하게 표시** (`app/(main)/home/page.tsx`) — ① 초기 mock 상태(기온 15° 등)가 실측인 척 렌더될 수 있던 구조 제거(실측 도착 전 null) ② API 부분 실패 시 mock 값과 필드 단위로 섞이던 블렌딩 제거(실측만으로 구성, 습도 결측은 경고 미판정 중립값) ③ 시간대별 환경·하루 케어 플랜의 mock 슬롯 폴백을 "환경 데이터를 불러오지 못했어요" 안내 카드로 교체 ④ 실측 없을 때 규칙 기반 추천도 mock 근거 대신 정직한 안내 문구로. 2026-07 조사에서 확정한 "지표 부정확" 4대 구조 원인의 마지막 항목 해소.

## [0.3.13.0] - 2026-07-18

### Fixed
- **체감온도 공식을 기상청 기준으로 교체** (`lib/feels-like.ts` 신설, `app/api/weather/route.ts`, `lib/timeline.ts`) — 종전 `기온 − 0.7×풍속` 근사는 겨울용 감산식이라 여름엔 방향 자체가 반대였음(실측: 습한 7월 저녁 앱 24°C vs 기상청 공식 27.3°C). 여름철(5~9월)은 습구온도(Stull) 기반, 그 외는 풍속 기반 wind chill(기온 10°C 이하·풍속 1.3m/s 이상일 때만)로 계산. 홈 현재값·시간대별 카드 공용. "매일 지표가 부정확하게 느껴진다" 반복 제보의 최대 원인.
- **홈 위치 라벨 정직화** (`app/(main)/home/page.tsx`) — 하드코딩 "서울 강남구"가 실제 데이터 기준지(날씨 좌표: 시청 부근 중구)와 달랐던 표기를 "서울 중구"로 교정 (SPEC 리스크 항목 부분 해소, 위치 기능 도입 전 임시).

### Changed
- **홈 '현재' 지표를 초단기실황(관측값) 우선으로 교체** (`app/api/weather/route.ts`, `lib/kma-time.ts` 신설) — 종전엔 발표 후 최대 3시간 지난 단기예보의 최근접 정시값을 "현재"로 표시해 실황 기반 앱(네이버 날씨 등)과 상시 1~3°C 어긋났음. 기온·습도·풍속·강수형태를 기상청 초단기실황(매시 관측)에서 우선 취득하고 실황 실패 시에만 예보로 폴백. 응답에 출처(`currentSource`)·기준시각(`currentBaseTime`) 필드 추가. KMA 결측 센티널(±900대) 범위 검증, 실황 PTY 코드(0~7)의 예보 코드(0~4) 정규화, 발표 지연 시 직전 시각 재시도 포함.

### Added
- **환경 지표 정합성 검증 스크립트 + 매일 자동 QA** (`scripts/verify-env-accuracy.mjs`) — 앱 API의 현재 지표를 기상청 실황·에어코리아 원본과 직접 비교해 "겉으로 멀쩡한 오답"(공식 오류·소스 이탈·센티널 오염)을 수치로 검출. 캐시 시차로 인한 오탐은 허용 폭 확대·WARN/SKIP으로 흡수. 매일 07시 Claude 스케줄 태스크가 프로덕션 대상으로 자동 실행하고 FAIL 시 조사 리포트를 남김.
- **lib 유닛 테스트 도입 확대** (`lib/feels-like.test.ts`, `lib/kma-time.test.ts`, `lib/timeline.test.ts`) — 체감온도 공식(계절·임계값 경계 포함), 실황 base_time 계산(자정·연 경계 롤백), 타임라인 체감 위임 회귀 테스트 21건 추가 (총 39건).

## [0.3.12.0] - 2026-07-18

### Added
- **환경(env) 화면 주간날씨 하단 '이번 주말 나들이' 판단 카드 + 인앱 수요 프로브** (`lib/weekend-outing.ts`, `app/api/feedback/route.ts`, `app/(main)/env/page.tsx`) — 주말(토/일) 날씨로 실내/실외를 판단(강수확률·기온·하늘상태, 홈 케어플랜 우산 기준과 정합: 강수확률 ≥60% 또는 강수형태 예보 → 실내, 40~50% → 대비)하고, 그 조건에 맞는 서울 큐레이션 나들이 장소 2~3개(네이버 지도 검색 링크)를 추천. 아이 체질은 공용 헬퍼 `hasRespiratory`·`hasSkin`로 반영(구형 라벨까지 매칭). 장소 "검색"이 아니라 판단 지원(코어)의 연장 — 손 큐레이션 정적 시드로 시작하고 푸시·외부 장소 API는 범위 밖(서울 외 지역은 판단만 노출). 수요 검증용 인앱 프로브("더 보고 싶어요")는 클릭 시 `/api/feedback`로 이벤트만 기록(DB 없이 서버 로그, 비식별 meta)하고 정직한 문구만 노출(가짜 기능 없음). 설계: office-hours 세션(슬림 A안 + fake-door 프로브).

### Changed
- **환경(env) 화면 상단 날씨 카드를 옷차림(outfit) 공용 카드로 통일** (`app/(main)/env/page.tsx`) — 자체 마크업이던 '현재 날씨' 히어로(큰 온도 + 습도·바람·강수 3칸 그리드, 브랜드 오렌지 accent)를 삭제하고, outfit과 동일한 공용 `WeatherNowCard`로 교체. env에는 옷차림 조언 데이터가 없어 메시지 없는 컴팩트 레이아웃(아이콘+온도+체감, 하단 바람·습도·강수 인라인)으로 렌더 — 바로 아래 '야외활동 지수' 카드와의 문구 중복을 피함. 카드 전용이던 미사용 헬퍼(`skyIcon`·`skyDesc`) 제거, 체감온도(`feelsLike`) 데이터 연결.

### Fixed
- **홈 재방문 프라임 캐시 무효화 회귀** (`app/(main)/home/page.tsx`) — 0.3.11.0의 마운트 프라임 effect가 리포트 캐시 키를 `v15`로 하드코딩해, #108(0.3.10.0)에서 `v17`로 올라간 실제 캐시를 못 읽어 재방문 즉시 표시가 무효화되던 문제(0.3.11.0과 0.3.10.0 리베이스 과정에서 발생한 버전 스큐). 캐시 키를 단일 헬퍼 `reportCacheKey()`로 추출해 생성·프라임 두 effect가 항상 같은 키를 쓰도록 하고 재발을 차단. 프로덕션 실측에서 재방문 `cache_hit` 444ms·스켈레톤 0 확인.

## [0.3.11.0] - 2026-07-17

### Fixed
- **홈 "구버전 화면 잔상 → 전환" 제거** (`app/providers.tsx`, `app/api/version/route.ts`, `next.config.ts`) — 서비스워커 부재 상태에서 bfcache/PWA 홈스크린이 직전 배포 번들로 렌더된 페이지를 복원해, 승인 전 옛 화면이 잠깐 뜬 뒤 리로드로 전환되던 문제. 기존 `pageshow` 무조건 리로드(같은 번들에도 매번 리로드·옛 프레임 노출)를 걷어내고, 현재 라이브 배포 식별자(`/api/version`, no-store)와 번들에 인라인된 `NEXT_PUBLIC_BUILD_ID`(Vercel 커밋 SHA)를 비교해 **버전이 실제로 다를 때만** 마스킹 오버레이 후 리로드하도록 변경. 로컬·미배포는 양쪽 "dev"로 일치해 리로드 없음.

### Changed
- **홈 재방문 리포트 지연 제거** (`app/(main)/home/page.tsx`) — 당일 캐시가 있어도 리포트를 uv·꽃가루 게이트(공공 API 콜드미스 시 1s+) 뒤에서만 읽어 재방문자가 매번 기다리던 워터폴 해소. 마운트 즉시 캐시를 읽어 리포트를 바로 노출(`reportPrimed`)하고, 환경 도착 후 `envChanged`만 조용히 재검증해 급변 시에만 재생성. 기존 stale/single-flight 방어는 보존.

## [0.3.10.0] - 2026-07-17

### Fixed
- **케어 플랜 우산 칩 남발 — 강수확률 30% 흐린 여름날 전 슬롯 최우선 강조 발화** (`lib/prep.ts`, `lib/timeline.ts`) — 원인: #98에서 상단 AI 체크리스트와 정합을 위해 우산 임계값을 60%→30%로 완화했는데, 기상청 강수확률 30%는 여름 흐린 날 배경 수준이라 신호가 죽음. 노출 창(슬롯~다음 슬롯) 기반 2단계 판정으로 교체: 강수형태 예보 또는 창 max ≥60%면 확정 신호(priority 100), 창 max 40~50%는 예비 신호(priority 55 — 더 급한 신호에 밀려남), 그 미만 침묵. 창 max 방식은 정시값만 보면 놓치던 시점 어긋난 소나기 예보도 잡는다(합집합 확률 ≥ max ≥ 평균). AI 리포트 프롬프트에도 동일 우산 기준 명시(캐시 키 v16→v17), 우산·창 집계 단위 테스트 추가(`lib/prep.test.ts`, `lib/timeline.test.ts`)

### Changed
- **AI 리포트 이슈 우선순위 규칙 도입** (`lib/prompts/report.ts`) — 한여름에 "미세먼지는 보통이라 마스크는 필수는 아니에요" 같은 무정보 문장이 메시지 지분을 차지하던 문제. ①오늘 실제 문제인 환경 이슈 1~2개만 다루고 좋음·보통·낮음·적정 등급 지표는 언급 금지(안심 정보는 뱃지·상태 필이 담당) ②한여름 few-shot 2개 추가(폭염×특이사항 없음 — 미세먼지 보통 무언급, 호흡기 민감 영아×폭염+미세먼지 나쁨 — 외출 조정) ③마스크 규칙 — 미세먼지 나쁨 이상·꽃가루 높음 이상일 때만 권고하고, **만 2세(24개월) 미만은 마스크 대신 외출 자제·시간 단축·실내 대체로 안내**(질식 위험 연령 가드). 프롬프트 변경으로 리포트 캐시 키 v15→v16 갱신
- **SPEC.md 문서 드리프트 청산** — 리포트 캐시 키 기재(v10→v16), 프롬프트 설계 절(few-shot 5개·우선순위/마스크 규칙·`prep` 스키마) 실코드 정합화. CEO 리뷰: `docs/reviews/2026-07-17-ceo-review-report-message-priority.md`

## [0.3.9.0] - 2026-07-17

### Changed
- **홈 AI 리포트 hook·현재환경 한 줄 가독성 개선** (`app/(main)/home/page.tsx`) — ①'자세한 리포트 보기'→'자세히'로 축약하고 hook 히어로 마지막 줄 우측에 얹어 한 덩어리로 읽히게 배치(펼침 시 '접기'). ②'현재 환경 한 줄'을 라벨(옅게)+값(진한 볼드, 숫자는 `.num`) 대비 구조로 바꿔 스캔성 강화, 기온 앞에 '현재날씨' 라벨 추가(11.5→12px).

### Fixed
- **AI 리포트 이름 조사 받침 오류** (`lib/prompts/report.ts`) — 모음으로 끝나는 이름에 "이"를 붙여 "지우이는"처럼 어색하게 나오던 문제. 프롬프트에 받침 규칙과 `withTopicParticle`로 계산한 이 아이의 올바른 주제 표현("지우"→"지우는", "도윤"→"도윤이는")을 주입. 프롬프트 변경으로 리포트 캐시 키 v14→v15 갱신.

## [0.3.8.0] - 2026-07-17

### Fixed
- **홈 현재 날씨 지표 null → mock 무음 폴백 해소** (`app/api/weather/route.ts`) — 자정~02:30 구간에 아직 발표되지 않은 당일 0200 단기예보를 요청하고, 현재값을 정확 정시(예: 0000)로만 찾다가 그 정시가 비면 기온·체감·습도·강수 스칼라가 통째로 null이 돼 홈 상단이 오류 표시 없이 mock(15°/습도 42%)으로 새던 문제. `getBaseDateTime`을 전날 2300 발표본으로 롤백하고, 현재값을 '지금'에 가장 가까운 예보 정시로 최근접 매칭하도록 개선 — 상단 현재 환경 한 줄이 항상 실측(시간대별 카드·AI 리포트와 정합)으로 표시됨
- **미세먼지 등급 null 폴백 불일치** (`lib/timeline.ts`, `app/(main)/home/page.tsx`) — 측정 실패 시 상단 한 줄은 "좋음"(거짓 안심), 시간대 카드는 "보통"으로 달랐다. `dustLabel`을 export해 양쪽이 공유하도록 하고 null→"보통"으로 통일

### Changed
- **AI 리포트 자외선 등급 표기 + checklist 접두어 제거** (`lib/prompts/report.ts`, `app/(main)/home/page.tsx`) — hook·message에서 "자외선 9" 같은 수치 대신 "자외선 강함"/"자외선 매우강함" 등급으로 표기하고, checklist 이름의 건강 특이사항 접두어 제거("아토피 자외선차단제"→"자외선차단제"). 프롬프트 변경 반영 위해 리포트 캐시 키 v12→v14 갱신
- **홈 AI 리포트 카드 헤더 크림 밴드** (`app/(main)/home/page.tsx`) — 화면당 하나뿐인 히어로 카드 상단에 크림(`secondary`) 풀-블리드 띠 도입으로 "AI 리포트" 앵커링·가독성 강화. 라벨·날짜를 섹션 헤더("오늘 챙길 것")와 동일한 15px/bold 블랙으로, 새로고침·공유 아이콘 블랙, "자세한 리포트 보기" 우측 정렬

## [0.3.7.0] - 2026-07-17

### Changed
- **전 화면 디자인 일관성 — 상단바 통일** (`components/PageHeader.tsx`, home·env·outfit·tips·me) — 5개 탭이 각자 인라인 헤더를 들고 있어 문법이 셋으로 갈리던 문제 해소. 공용 `PageHeader`로 통일(로고 좌 + 우측 컨텍스트 액션), env·outfit·tips의 뒤로가기 화살표 제거(최상위 탭엔 부모 없음 — 토스·당근 패턴), 헤더 아이콘 버튼 44px 터치 타겟 통일. env h1 승격, outfit 영문 라벨(TODAY'S OOTD·INNER/BOTTOM/ACC) 제거. 리뷰: `docs/reviews/2026-07-16-design-review-cross-page-consistency.md`
- **홈 상단 재구성 + 날씨 중복 정리** (`app/(main)/home/page.tsx`, `components/WeatherNowCard.tsx`, `app/(main)/outfit/page.tsx`) — home·env·outfit 날씨 3중 노출 정리. 홈 AI 리포트 카드는 흰색 유지하고, 라벨 행과 hook 사이에 현재 환경 한 줄(현재날씨·체감·강수·미세먼지·습도, 있는 값만) 삽입 + message 아래 환경지표 칩 삭제. "시간대별 환경" 섹션 유지, "지금 날씨" 결론형 카드 컴포넌트(`WeatherNowCard`)를 outfit과 공용화(옷차림 조언 유지). env는 전체 날씨 상세 목적지로 유지
- **환경정보 화면 재배치** (`app/(main)/env/page.tsx`) — 타이틀 "맞춤 인사이트" → "맞춤 환경 정보". 현재 날씨 카드를 최상단으로, 그 다음 오늘의 야외활동 지수, 맞춤 인사이트는 아래로. 주간 날씨 강수확률 ≥60% 강조색을 홈 시간대별 환경과 동일(status-warn)하게 통일. 주간 하단 안내문("주말은 나들이 계획에…") 제거

## [0.3.6.1] - 2026-07-16

### Changed
- **환경정보 화면 v3 톤 정렬** (`app/(main)/env/page.tsx`) — 다른 탭과 어긋나던 v2 잔재 제거. 인사이트·야외활동 지수·대기질·꽃가루·자외선 카드의 크림 면과 틴트 배경+보더를 순백 카드로 통일, 상태 텍스트를 브랜드 오렌지에서 시멘틱 색(status-warn/bad/neutral)으로 교정, 주간 날씨 요일 달력 관례(토=파랑·일=빨강)·온도 바 뉴트럴·강수 status-info, 10~11px 텍스트 12px 수렴. 리뷰: `docs/reviews/2026-07-16-design-review-env-v3-tone.md`

## [0.3.6.0] - 2026-07-16

### Changed
- **상품 추천 섹션을 홈 → 옷차림 하단으로 이동** (`app/(main)/home/page.tsx`, `app/(main)/outfit/page.tsx`) — 홈은 "판단·실행"에 집중하고 커머스성 정보는 옷차림 페이지 최하단 "오늘 필요한 아이템"으로 분리. 일러스트 카드 가로 스크롤 → 텍스트+가격 심플 리스트(이름·근거·가격)로 교체. 추천은 오늘 코디(plan.items) 파생으로 규칙 엔진(`buildItemRecommendations`)을 재사용해 코디와 상품이 어긋나지 않게 유지. 실연동 전이라 탭 동작을 제거하고 "가격은 참고용 · 구매 연동 준비 중" 명시(가짜 affordance 제거)

## [0.3.5.0] - 2026-07-16

### Changed
- **디자인 시스템 v3 "White Report" 전면 개정** (`app/globals.css`, `tailwind.config.ts`, `DESIGN.md`, 주요 화면 전체) — 베타 평가의 "전문성 부족" 피드백에 대응해 토스(정보 신뢰 문법)·당근(오렌지 온기) 벤치마크로 개정. ① 배경 베이지→웜 그레이 `#F6F4F2` + 순백 카드 면 분리(카드 외곽 보더 폐지, 3단 elevation), ② 브랜드 오렌지 `#F5A623`→`#F97316` 심화 + 역할 3분리(solid CTA / 텍스트 `#C2540A` / 틴트 배경), ③ 타이포 7단 스케일(AI 결론 26px 히어로, 섹션 헤더 17px, 11px 미만 금지), ④ 마이·설정을 당근식 리스트 행 문법으로 전환, ⑤ env 그라데이션 게이지→단색 상태 fill. 근거·비교: `docs/reviews/2026-07-16-DESIGN-v3-proposal.md`, `docs/reviews/2026-07-16-design-v3-toss-karrot-before-after.html`
- **UI 이모지 전면 제거** (10개 파일 71건) — raw 이모지 아이콘을 Lucide·LineIcon 단일 세트(strokeWidth 1.75)로 교체, 아바타는 이니셜 원으로 통일. OS별 이모지 렌더링 편차 제거
- **마이 아이 프로필 카드 압축** (`app/(main)/me/page.tsx`) — 상세 9행 나열을 헤더(이니셜 아바타·요약 한 줄·선택됨 배지) + 핵심 2행(민감도, 등원·하원)으로 압축. 상세는 편집 화면에서 확인
- **홈 알림 벨의 상시 강조 도트 제거** (`app/(main)/home/page.tsx`) — 실제 unread 상태가 없는데 항상 표시되던 거짓 긴급 신호 제거

### Added
- **홈 지연 계측(`?perf=1`)** (`lib/perf.ts`, `app/(main)/home/page.tsx`, `app/api/report/route.ts`, `app/auth/landing/page.tsx`) — 로그인 후 home 진입~AI 리포트 안정 구간을 분해 측정하는 경량 계측. `?perf=1`로 켜면(이후 세션 유지, `?perf=0`로 해제) 클라이언트 콘솔에 구간별 Δ/누적 Σ, 서버 Vercel 로그에 `received→firstDelta→hook→done` 분해가 남고, `perfId`로 클라이언트·서버 로그를 같은 요청으로 매칭. 일반 사용자 요청에는 로그를 남기지 않음(`x-perf-id` 헤더 게이팅). 사용법은 `docs/perf-home-latency.md`

### Fixed
- **구형 iOS(Safari 17.4 미만)에서 홈 로딩이 멈추던 문제** (`app/(main)/home/page.tsx`) — 환경 API 호출에 Safari 17.4+ 전용 `AbortSignal.any()`를 쓰고 그 호출이 `try` 밖에 있어, 미지원 브라우저에서 throw 시 `setLoading(false)`에 도달하지 못해 스켈레톤이 영구 노출될 수 있었다. 요청별 `AbortController` + `setTimeout` + 부모 취소 리스너로 교체(광범위 호환)하고, 요청 시작·await를 모두 `try` 안으로 이동
- **프로필 전환 시 이전 아이 리포트가 현재 화면을 덮어쓰던 문제** (`app/(main)/home/page.tsx`) — AI 리포트 스트리밍 중 다른 아이로 전환하면, 늦게 끝난 이전 요청의 결과가 현재 아이 화면·캐시를 덮어쓸 수 있었다. 요청 세대(gen) + 활성 프로필 id(`activeIdRef`) 비교로 stale 응답의 화면·캐시 갱신을 차단. hook 도착 전 전환에서도 안전하도록 프로필 변경 무효화를 effect 실행 순서에 의존하지 않게 처리
- **superseded 리포트 요청의 Claude 토큰 비용** (`app/(main)/home/page.tsx`, `app/api/report/route.ts`) — 프로필 전환·언마운트로 밀려난 리포트 요청이 서버에서 계속 생성돼 토큰이 청구되던 문제. 클라이언트가 이전 요청 fetch를 취소하고, 서버는 `req.signal`로 진행 중인 Anthropic 스트림까지 취소(`client_aborted`). 같은 아이 중복 요청은 single-flight로 시작 자체를 차단
- **리포트 API 실패 경로 관측성** (`app/api/report/route.ts`) — 잘못된 JSON body·필수 입력(child/weather) 누락·프롬프트 구성 예외가 로그 없이 500으로 끝나던 경로를 계측(`input_error`/`build_error`)하고 400으로 명확히 처리

## [0.3.4.1] - 2026-07-15

### Fixed
- **홈 규칙 기반 폴백이 mock 기준으로 계산돼 상단 칩과 모순 + 조용히 노출** (`app/(main)/home/page.tsx`, `lib/recommendation-engine.ts`) — 날씨 API 실패·지연 시 홈이 AI 리포트 대신 규칙 기반 기본 추천을 띄우는데, ① 그 추천이 항상 `mockWeather.timeline`을 근거로 계산돼(상단 칩은 실측) "꽃가루 낮음(칩) vs 꽃가루 높음(체크리스트)"처럼 모순됐고, ② 날씨 실패 시 `aiError`가 세팅되지 않아 "기본 추천" 표기 없이 정상 리포트처럼 보였다(둘러보기 진입에서 구버전처럼 보이던 원인). 폴백 근거를 실측 슬롯(`displaySlots`)로 전환해 칩과 일치시키고, 날씨 실패 시 `aiError`로 "기본 추천"을 명시. 상단 칩(badges)은 종전대로 실측 스칼라값에서 도출(변경 없음)
- **규칙 기반 폴백 메시지 조사 오류** (`lib/recommendation-engine.ts`) — 준비물 나열 시 받침을 무시하고 "와"·"을"로 고정해 "가디건와" 같은 비문이 생기던 문제. 마지막 한글 음절의 받침으로 "와/과"·"을/를"을 판정하도록 수정

## [0.3.4.0] - 2026-07-15

### Changed
- **홈 AI 리포트 스트리밍 전환** (`app/api/report/route.ts`, `app/(main)/home/page.tsx`) — 비스트리밍 one-shot JSON 응답을 SSE 스트리밍으로 교체. 서버가 모델 스트림을 누적하며 `hook`·`message` 필드가 완성되는 즉시 이벤트로 방출하고, 완료 시 `done` 이벤트로 전체 페이로드(checklist·prep) 전송. 클라이언트는 `hook` 도착 즉시 헤드라인(아침의 결론)을 노출하고 본문은 도착까지 스켈레톤 유지. 헤드라인 체감 노출이 전체 생성 완료(~5.5초) 후에서 **~1.9초**로 단축. 당일 localStorage 캐시 재진입은 기존대로 즉시 표시(호출 없음)

### Fixed
- **외부 환경 API 지연 상한** (`app/api/weather`·`air`·`uv`·`pollen/route.ts`, `app/(main)/home/page.tsx`) — data.go.kr 호출에 타임아웃이 없어 공공 API가 느린 날 AI 리포트 착수가 무한정 지연되던 문제. 서버 fetch에 8초 안전 캡, 클라이언트 env fetch에 타임아웃 추가(weather·air 9초, uv·꽃가루 5초). uv·꽃가루가 상한을 넘으면 null로 진행("데이터 없음" 처리)해 리포트 착수를 막지 않고, 서버 캐시는 다음 진입에 채워짐

## [0.3.3.0] - 2026-07-14

### Added
- **하루 케어 플랜** (`app/(main)/home/page.tsx`) — 시간대별 환경 아래 세로 타임라인 섹션 신설. 온도와 "특이사항" 지표(6개 지표 중 주의 수준)만 요약해 노출하고, 아이 프로필에 따라 주의 지표를 함께 반영(호흡기·알레르기 → 미세먼지·꽃가루, 피부 → 자외선·건조는 '보통' 단계도 미리 노출). 준비물 칩은 환경 대응(우산·마스크·선크림)만 컬러 강조, 나머지는 아웃라인. 진행 중 슬롯은 카드 배경·도트로 강조

### Changed
- **홈 AI 리포트 카드 개편** (`app/(main)/home/page.tsx`) — 크림 헤더 밴드·상태 필을 제거하고 외곽선 있는 단일 흰 카드로 정리. 헤더에 생성 메타("7월 14일 (화) 07:30 생성" — 요일 포함·24시간제)와 이미지 저장·공유 CTA(우상단, 로직은 후속)를 배치. 환경 칩은 주의(warn)만 컬러 강조·나머지는 플랫 그레이. "오늘 챙길 것"을 체크박스 + 아이콘 사각형 + 제목/사유 2줄 형식으로 변경. hook(히어로 헤드라인)을 두 줄(쉼표·대시 분리) + 800 weight로 승격
- **저녁 시간대 기본 노출** (`lib/timeline.ts`, `lib/weather-mock.ts`) — 온보딩에서 '저녁 외출 시간'을 설정하면 그 시각, 미설정 시 21:00(기상청 예보 제공 시각 06·09·12·15·18·21시)으로 시간대별 환경·하루 케어 플랜에 항상 노출. 이전엔 미설정 시 저녁 슬롯이 아예 표시되지 않던 것을 개선

## [0.3.2.0] - 2026-07-13

### Fixed
- **홈 상단 환경 칩의 꽃가루·자외선이 mock 고정값 표시** — `setWeatherData`가 두 값을 실데이터로 갱신하지 않아, 실제 API 응답과 무관하게 꽃가루 "높음"·자외선 "보통"이 고정 노출되던 문제. 자외선·꽃가루 응답 도착 시 실데이터로 칩을 갱신(없으면 안전 기본값 "낮음"/UV 0). AI 리포트·시간대 카드는 이미 실데이터 반영 중 (`app/(main)/home/page.tsx`, `lib/timeline.ts`)

### Added
- **환경정보 꽃가루 "제공 기간 아님" 안내** — 기상청 꽃가루농도위험지수 비제공 기간(참나무·소나무 4~6월, 잡초 8~10월 외)에 세 카드가 "--"로만 떠 오류처럼 보이던 것을, 제공 월을 안내하는 카드로 대체. 조회 실패("불러오지 못했어요")와 구분 (`app/(main)/env/page.tsx`)

### Changed
- **`.env.example`에 자외선(생활기상지수) 서비스 명시** — `/api/uv`가 `KMA_API_KEY`로 호출하는 기상청 생활기상지수(3.0) 승인이 필요한데 목록에서 빠져 있어, 4개 서비스·키·라우트 매핑을 함께 표기 (동작 변경 없음)

## [0.3.1.0] - 2026-07-13

### Changed
- **AI 리포트가 자외선·꽃가루를 실제 반영** (`app/api/report/route.ts`, `lib/prompts/report.ts`, `app/(main)/home/page.tsx`) — 기존엔 리포트에 weather·air만 넘겨, 프롬프트 few-shot에 있던 "자외선 강함→차단제"·"꽃가루 높음→마스크" 판단을 정작 실데이터 없이 추론만 했다. 리포트 입력에 자외선지수(오늘 최댓값)·꽃가루 위험지수(참나무·소나무 최댓값)를 추가해, 아토피·비염 등 체질과 실제 연관될 때 근거 있게 언급하도록 함. #83의 당일 고정 캐시 급변 감지(`envSignature`)에 자외선 강함(6)·꽃가루 높음(3) 경계 통과를 추가하고, 리포트 캐시 키 v10→v11로 구캐시 무효화
- **홈 로딩 구조 개선** — 화면 셸·상단 카드·시간대 카드는 weather·air가 도착하면 즉시 표시(체감 로딩 단축)하고, AI 리포트는 자외선·꽃가루까지 4개 입력이 준비된 뒤 착수. 카드 렌더가 리포트 입력 대기에 막히지 않음

### Added
- **홈 로딩 지연 측정 스크립트** (`scripts/measure-home-latency.mjs`) — 각 외부 API 라우트와 Claude 리포트 호출을 라우트별로 실측. dev 서버 + `.env.local` 환경에서 실행

## [0.3.0.0] - 2026-07-13

### Added
- **마이 프로필 편집 화면** (`/me/edit/[id]`) — 온보딩에서 입력한 아이 정보(이름·생년월일·성별·건강 정보·추위/더위/땀 민감도·하루 일과)를 마이페이지에서 직접 수정 가능. 저장 시 localStorage + Supabase DB 동시 반영, 마이 카드의 ✏️ 편집 버튼 연결(기존 "준비 중" 토스트 대체). 알림 설정(온보딩 Step 7)은 별도 알림 설정 화면 작업으로 분리
- **구형 프로필 데이터 정규화** (`lib/profile-options.ts`) — 한국어 라벨로 저장된 민감도("보통이에요" 등)와 단일 키워드 건강 정보("아토피"·"비염" 등)를 현행 코드값·선택지로 매핑. 마이 카드에서 "-"로 보이던 구형 값이 제대로 표시되고, 편집 진입 시 기존값이 채워짐. 온보딩과 편집이 선택지 상수를 공유하도록 추출

### Fixed
- **편집 저장 시 다른 아이 프로필 유실** — localStorage에 실체화되지 않은 데모 목록 상태에서 `saveProfile`(원본 병합)로 저장하면 편집하지 않은 프로필이 사라짐. 목록 전체 교체 저장(`saveProfiles`)으로 수정
- **온보딩 프로필 DB 저장 실패가 조용히 넘어감** — `saveProfileToDb`가 게스트(no-auth)/실패(error)/성공(ok)을 구분해 반환하도록 변경, 로그인 상태에서 저장 실패 시 온보딩·편집 화면 모두 사용자에게 재시도 안내 토스트 표시

## [0.2.3.1] - 2026-07-13

### Fixed
- **이메일 가입 후 재로그인 시 "Email not confirmed" 오류에 사용자가 갇힘** — 원인: Supabase 이메일 인증이 활성인데 앱에 인증 흐름이 부재 — ① `signUp`에 `emailRedirectTo` 미지정(인증 링크가 Site URL 기본값으로 이동) ② 가입 후 "메일 인증 필요" 안내 없음 ③ 로그인의 미인증 오류가 영어 원문 노출 + 재발송 수단 없음. `emailRedirectTo`를 `/auth/callback?next=/auth/landing`으로 지정, 인증 대기 가입 시 안내 토스트 추가, 로그인에서 한국어 안내 + "인증 메일 재발송"(`supabase.auth.resend`) 액션 추가 (`app/signup/page.tsx`, `app/login/page.tsx`)

### Changed
- **이메일 인증 정책 확정** (PRODUCT-DECISIONS) — 최종 인증 활성 + 커스텀 SMTP, 지인 테스트 기간만 임시 비활성. 인증 재활성화 + SMTP 연결을 출시 Blocker에 추가

## [0.2.3.0] - 2026-07-13

### Fixed
- **구글 로그인 시 유저 프로필 대신 데모 목업(지우·도윤) 화면 표시** — 원인: 구글 OAuth 경로가 프로필 유무 확인 없이 `next=/home` 하드코딩이라 신규 사용자가 온보딩을 건너뛰고 localStorage 데모 fallback을 보게 됨. 인증 성공(이메일·구글, 로그인/가입 페이지 공통) 후 공통 판단 지점 `/auth/landing` 신설 — DB 프로필 있으면 `/home`(복원), 없으면 `/onboarding` (`app/auth/landing/page.tsx`, `app/login/page.tsx`, `app/signup/page.tsx`, `app/auth/callback/route.ts`)
- **온보딩 프로필 DB 저장이 조용히 실패** — 원인: 로컬 생성 id(`c-<timestamp>`)를 `children.id`(uuid PK)에 그대로 upsert해 uuid 파싱 오류. UUID 형식 id만 전달하고 그 외엔 DB가 생성하도록 수정 (`lib/profile.ts: profileToRow`)
- **가입 페이지 구글 버튼으로 기존 사용자가 재온보딩(아이 중복 등록)** — 판단 지점 통일로 해소

### Added
- **접속 흐름 상태 기반 재정의** (SPEC.md) — 도착지를 진입 경로가 아니라 사용자 상태(로그인 × 프로필 유무)로 결정. `lib/profile.ts`에 `fetchProfilesFromDb()`(no-auth/error/ok 구분 — 조회 실패를 빈 계정으로 오판 방지) 신설
- **게스트 → 가입 전환 시 로컬 프로필 DB 이전** (PRODUCT-DECISIONS §3 구현) — 판단 지점에서 DB가 비어 있으면 게스트 시절 로컬 프로필(데모 제외)을 1회 업로드 (`uploadLocalProfilesToDb`)
- **홈 진입 가드** — 로그인 상태 + 실프로필 없음이면 데모 대신 온보딩으로 유도. 온보딩의 "나중에 이어서 하기/먼저 둘러볼게요"는 탭 세션 동안 예외(sessionStorage). 데모 프로필은 비로그인 게스트 전용이 됨. 가짜 Supabase 목서버 + Chromium E2E 9개 시나리오(14개 체크)로 검증

## [0.2.2.0] - 2026-07-13

### Added
- **AI 게이트웨이 지원 — `ANTHROPIC_BASE_URL` 환경 변수** (`app/api/report/route.ts`) — 설정 시 AI 리포트의 Anthropic API 호출이 게이트웨이/프록시 엔드포인트를 경유(예: 사내 AI 게이트웨이 키로 과금 전환), 미설정 시 기존과 동일하게 Anthropic 기본 주소 사용. `.env.example`·README·SPEC에 사용법 문서화. 실호출 검증 완료(게이트웨이 경유 리포트 정상 생성)

## [0.2.1.0] - 2026-07-13

### Changed
- **AI 리포트 모델 전환: Claude Haiku 4.5 → Claude Sonnet 5** (`app/api/report/route.ts`) — 실측 환경 A/B 비교(프로필 3종)에서 Sonnet이 금지 규칙 준수(일반 조언 억제)·체질→증상 구체화·데이터 기반 행동 제안에서 우세함을 확인하고 전환. 파라미터: `thinking` 비활성(저지연 우선), `temperature` 제거(thinking 비활성 시 지정 불가), `max_tokens` 700→1000. 리포트 캐시 키 v6→v7로 구모델 캐시 무효화. 지연 +약 1초·비용 리포트당 약 0.7원(인트로 가격)은 수용
- **홈 화면 시각 리스타일 + "오늘의 종합 솔루션" 섹션 제거** (#59) — LineIcon·ItemIllustration 도입(OS 이모지 대체), 상태 3단계 토큰 정리. CharacterReport는 AI 리포트와 결론 중복·상충 위험으로 홈에서 제거(CEO 리뷰 P0-3, 컴포넌트·이미지 보존), 추천 아이템 섹션은 유지 결정(숨김 방침 철회). DESIGN.md 일러스트 예외 조항 삭제, SPEC·PRD 정합

### Added
- **Codex CLI 병행용 에이전트 지침** (#60) — `AGENTS.md`·`.agents/skills/` 14종 (CLAUDE.md·`.claude/skills/` 미러, 기계 치환 오류 교정)

## [0.2.0.6] - 2026-07-12

### Changed
- **PRD v2.7.2 — 연령 타깃 정정 (만 3~6세 우선 모집 철회, 만 1~8세 전체 유지)**: v2.4의 "베타 모집 만 3~6세 우선"을 폐기하고 전체 타깃을 축소하지 않되 검증 표본만 연령층별로 분리. ① §04에 전체 타깃·연령 범위 제품 가설(하한 1세·상한 8세·제도 경계를 인과로 단정 않음)·연령별(1~2·3~6·7~8) 사용 맥락·판단 규칙 표 신설, 페르소나(만 4세)를 "만 3~6세 군 대표 사례"로 명시 ② **연령군별 규칙 세트 분리를 정확도 설계 결정으로 명시**(§04·§08) — 안전·예측 규칙과 액션 카탈로그를 연령군 키로 분리, 얇은 군은 배제가 아니라 낮은 신뢰도 표시 ③ §05 입력 축 ②·골든 케이스를 3개 연령군 각각 커버로 확장 ④ §10 컨시어지 테스트를 연령층별 12~15가구(1~2세 3~4·3~6세 6~7·7~8세 3~4)로 재정의, §11 Go/No-go 표본 정합
- **PRODUCT-DECISIONS §3-6 신설**: 연령 타깃 결정 로그 — 만 1~8세 전체 유지 + 연령군별 규칙·검증 분리, v2.4 만 3~6세 우선 폐기. 상한 8세 제도 경계 인과 단정 금지 → 법령 확인·만 7~8세 부모 조사 To-Do

## [0.2.0.5] - 2026-07-12

### Changed
- **PRD v2.7.1 — 구현가능성·정합성 정밀 교정(코드 대조)**: 기능 추가·방향 변경 없이 코드 현실과 어긋난 표현만 교정. ① 재현성 모순 해소 — Eval 수락 기준 "3회 동일"을 규칙 안전 판정·필수 준비물로 한정(현재 `route.ts`가 `temperature:1.0`이라 LLM 문장 동일 불가), 머지 게이트를 "매 PR 결정적 레이어 전수 + LLM 출력 샘플링"으로 현실화 ② "확장"→"신규 구축" 정직화 — `lib/safety-rules.ts`·`scripts/eval-briefing.ts`(scripts/ 자체 없음)·`daily_briefings` 등 영속화(현 report 라우트 무상태)·레이트리밋이 전부 신규임을 명시, 반대로 일정×예보 매핑(`findSlot`)은 기존 재사용으로 정정 ③ 미정의 값 확정 — 브리핑 재생성 임계값 예시, 지역 매핑을 소수 지역(서울 자치구)으로 한정·`children.region_code` 컬럼 명시 ④ 일정 현실화 — 영속화 백본을 Phase 2 후반으로 분담, Phase 3 2주 확장 컨틴전시, 결정론 로직 단위 테스트 필수화 + 테스트 인프라 Phase 1 셋업 ⑤ 정합 잔재 정리 — "공개 베타" 잔재 4곳·작성일·CoT 용어("구조화 판단 파이프라인"으로 통일)·데이터 모델 중복 헤더 수정

## [0.2.0.4] - 2026-07-12

### Changed
- **PRD v2.7 — 내부·엔지니어링·디자인 3종 리뷰(HOLD SCOPE) 반영**: 범위 축소·정직한 재정렬. ① 6주 목표 하향(공개 베타 → 블로커 해소 + 7일 컨시어지 테스트 → 확산 판단) ② 자동 알림(PWA·웹 푸시) P0 제외 — 7일 테스트 동안 수동 발송, 자동화는 "테스트 이후 단계"로 이동, 알림 오픈율·퍼널 KPI 제외(엔지니어링 T7) ③ 로드맵 4단계는 문서에 명시하되 시뮬레이션·최적화는 "테스트 이후"로 정직하게 배치 — "골든 케이스=시뮬레이션"·"Family Memory=최적화" 오인 표현 교정, 점수·추천은 규칙 코드/LLM은 문장화만 원칙 명시 ④ 신뢰성 정합 P0에 지역 선택·데이터 정합(T2)·새벽 기상 경계 처리(T3) 추가, 조용한 서울 fallback 금지 ⑤ 화면 재정렬(S-002 알림설정→지역 선택, S-003 푸시→저녁 결과 피드백) ⑥ 계측 최소 이벤트 6종(T5)·입력 검증(T6)·최소 자동 테스트 추가 ⑦ 카카오·전날 밤·시뮬레이션·최적화를 "테스트 이후 단계"로 통합, Go/No-go를 다음 단계 착수 판정 + 행동 변화 기준(CEO 리뷰)으로 갱신

### Added
- `docs/reviews/2026-07-12-{ceo,eng,plan-design}-review-current-stage.md` — v2.7의 근거가 된 현재 단계 3종 리뷰(HOLD SCOPE)

## [0.2.0.3] - 2026-07-12

### Changed
- **PRD v2.6 — PRD 작성 가이드(강의안) 검토 반영**: ① 섹션 02에 AI 성숙도 로드맵(자동화→예측→시뮬레이션→최적화) 신설 — 4단계를 제품 구조에 매핑(자동화·예측=본 PRD P0, 시뮬레이션·최적화=데이터 확보 후) ② P0 알림 채널을 당일 아침 웹 푸시 1종으로 압축 — 전날 밤 알림·카카오톡을 "조건부 확장"(착수 게이트 명시)으로 분리 ③ P1(있으면 좋음) 계층 삭제 — 래퍼앱·변화 감지형 알림은 Non-Goals의 재검토 조건부 항목으로 이동 ④ Phase 4·5 완료 조건·일정 컨틴전시를 채널 압축에 맞게 조정

## [0.2.0.2] - 2026-07-12

### Changed
- **PRD v2.4 — 외부 검토 선별 반영**: Eval Pipeline P1→P0 승격(골든 케이스 20~30개 + 머지 게이트), `daily_briefings` 브리핑 단일 원본(아이당 일 1회·전 채널 재사용), 저녁 결과 피드백 1문항 + `action_feedback` 운영 테이블, PostHog 최소 계측 1주차 이동 + 알림 퍼널 이벤트, 민감정보 별도 동의·국외 처리 고지·데이터 최소화, 주 채널 1개+폴백 원칙·카카오 착수 게이트, 공개 베타 Go/No-go 기준 신설, 병행 컨시어지 테스트(1~3주차·15~20가구). 비외출일 브리핑 후순위화·포지셔닝 축소 권고는 기각(MANIFESTO 정합)
- **PRD v2.5 — 사전 가설검증 설문 원본 반영**: 조사 방식 정정("FGI" → 온라인 설문 Google Forms·2026-05·n=17), 사용 의향 89% → 88.2%(15/17), "88.2% = 해석 부담" 오인용 → "탐색 가용 시간 하루 10분 이내"로 의미 교정, 전 수치 분모(n/N) 병기, 기본 발송 시각 07:10 → 07:00(희망 시간대 1위 반영), 설문 데이터로 전날 밤 알림·카카오 선호·커머스 제외·의학 근거 신뢰 요인 근거 보강
- **PRODUCT-DECISIONS §3-3**: 카카오 채널 착수 게이트 추가 — 4주차 알림 퍼널 병목 실측 시에만 착수, 주 채널 1개 + 폴백(중복 발송 폐기)
- **문제정의 프레임워크 v2**: FGI 표기 정정, 가상 시나리오 "반박 불가능한 증거" → "대표 검증 시나리오"로 교정, 알림톡 표기 정합

### Added
- `docs/validation/2026-05-사전-가설검증-설문.md` — 설문 문항·분모·집계 원본 등재(조사 근거 장부)

## [0.2.0.1] - 2026-07-08

### Changed
- **PRD v2.3 — 외부 검토 반영**: 페르소나 나이(지우 4세)·PostHog Phase(6주차) 정합 수정, 용어 정의 섹션 신설(훅·Family Memory·CoT·OverEdge·FGI), FGI 출처·조사 성격 명시, 로그인 사용자 레이트리밋 정책(계정당 일 30회) 추가, 섹션 02 비즈니스 가치 서술 보강, 문서 하단 버전 히스토리 신설

## [0.2.0.0] - 2026-07-08

> 0.1.1.0(6/11) 이후 릴리스 기록 없이 쌓인 변경들을 소급 정리한 캐치업 릴리스.

### Added
- **이메일 로그인·비밀번호 재설정**: `signInWithPassword` 로그인, 재설정 메일 → `/reset-password` 플로우
- **DB 프로필 동기화**: 로그인 시 Supabase DB 프로필 자동 복원
- **AI 리포트 hook 문장**: 프롬프트 파일 분리(`lib/prompts/report.ts`) + few-shot 설계

### Changed
- **디자인 시스템 정합(전면)**: `--accent` CSS 변수 수정, 브랜드 오렌지 활성 상태 복원, 홈 섹션 헤더 타이포 스케일 상향(15px→22px), 44px 터치 타겟 확보, CharacterReport 일러스트 예외 승인 및 실데이터 연결
- **아이콘**: OS 이모지를 Lucide React 아이콘으로 교체 (DESIGN.md 준수)
- **건강팁 페이지**: 실API 연동 후 의도적으로 정적 콘텐츠로 되돌림 (판단 근거는 PR #33 참조)
- **문서 체계 정비**: 핵심 포지셔닝 교정("외출 준비"가 아니라 하루 첫 육아 판단 지원), PRD v2.2 신설, 제품 결정 문서(docs/PRODUCT-DECISIONS.md)·문제정의 프레임워크 v2·브리핑 엔진 PDCA 문서 추가
- **SPEC.md**: 기준선 문서 역할 명시, 한 줄 정의를 "판단 지원" 프레임으로 교정, N-01 알림 P0 승격·채널 우선순위(웹 푸시→카카오 나에게 보내기) 반영
- **DESIGN.md**: Product Context를 "환경 데이터를 아이 체질 기준으로 해석하는 육아 판단 지원" 프레임으로 교정

### Fixed
- **자외선 API**: 잘못된 지역 코드 체계·시각 파싱으로 항상 null을 반환하던 버그 수정
- **날씨 API**: 22시(KST) 이후 현재 날씨가 null로 반환되던 버그 수정
- **AI 리포트**: JSON 파싱 버그 수정 + 프롬프트 리라이팅, 오류 상태 처리 추가, 홈 최초 랜딩 빈 화면 제거

### Removed
- 미사용 파일 정리: `public/placeholder.svg`, `components/ui/use-toast.ts`(hooks/use-toast.ts와 중복) — 로컬 Trash Can으로 이동

## [0.1.1.0] - 2026-06-11

### Changed
- **AI 리포트 프롬프트**: 부모에게 전달하는 3인칭 문장으로 수정 — 기존 프롬프트가 아이에게 직접 말 거는 2인칭("지우야, 오늘은...") 문장을 생성하던 버그 수정
- **AI 리포트 컨텍스트**: 아이 일정(등원·야외활동·하원)과 시간대별 날씨를 프롬프트에 주입 — 일정 시간대 기온·강수확률·하늘 상태 기반으로 구체적인 조언 생성
- **기상청 API**: 오늘 3시간 간격 시간대별 예보(`hourlyForecast`) 반환 추가 — 06:00~21:00 슬롯
- **아이 프로파일**: `conditionEtc`(기타 건강 메모) 및 `schedule` 필드를 AI 리포트 API 페이로드에 포함
- **캐시 키**: v3 → v4로 업그레이드하여 구형 캐시 자동 무효화

### Fixed
- **AI 리포트 temperature**: Anthropic API 허용 범위(0~1.0) 초과 값(`1.2`) → `1.0`으로 수정

## [0.1.0.1] - 2026-06-10

### Fixed
- **꽃가루 API**: 한국환경공단 V3 API의 실제 오퍼레이션(`getOakPollenRiskIdxV3`, `getPinePollenRiskIdxV3`)으로 수정 — 기존 코드가 존재하지 않는 엔드포인트를 호출해 404 오류 발생
- **자외선 API**: KST 06:00 이전에는 어제 데이터로 자동 fallback — 기상청이 매일 06시에 당일 자외선 데이터를 발행하므로 그 이전 시간대에 오류 발생하던 문제 해소
- **자외선 API**: 네트워크 오류나 JSON 파싱 실패 시 날짜 fallback이 동작하지 않던 버그 수정 (try/catch 추가)
- **꽃가루 API**: `Number()` 변환 시 NaN 가드 추가 및 잘못된 `h12` fallback 필드 제거
- **지역 파라미터**: `region` 쿼리 파라미터가 정의된 지역 코드 목록에 없는 경우 서울로 fallback (비정상 입력 방어)
- **홈 화면**: AI 리포트 카드 디자인 개선 및 어투 개선
- **홈 화면**: AI 로딩 중 프로토타입 flash 제거 — 로딩 및 AI 생성 중 전체 skeleton 유지
- **프로파일**: 데모 프로파일 실데이터로 업데이트

## [0.1.0.0] - 2026-06-01

### Added
- 초기 릴리스: 날씨, 대기, 꽃가루, 자외선 환경 데이터 대시보드
- AI 기반 건강 리포트 생성 (Claude API)
- 지역별 환경 지수 조회 (서울 기본값, 17개 광역 지자체 지원)
